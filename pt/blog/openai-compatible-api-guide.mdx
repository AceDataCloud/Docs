---
title: "Guia Completo da API Compatível com OpenAI"
---

## O que é a Interface Compatível com OpenAI

O formato da API Chat Completions da OpenAI tornou-se o padrão de fato na indústria. Muitos grandes modelos de linguagem oferecem interfaces compatíveis com OpenAI, permitindo que os desenvolvedores utilizem o mesmo código para acessar diferentes modelos.

A Ace Data Cloud oferece interfaces compatíveis com OpenAI para modelos como Claude, Gemini, DeepSeek, Grok, Kimi, entre outros, bastando alterar a Base URL e o nome do modelo para alternar.

## Modelos Suportados

| Serviço | Endpoint | Modelo Exemplo |
|---------|----------|---------------|
| Claude  | `POST /v1/chat/completions` | claude-sonnet-4-6 |
| OpenAI  | `POST /openai/chat/completions` | gpt-4o |
| Gemini  | `POST /gemini/chat/completions` | gemini-2.5-flash |
| DeepSeek| `POST /deepseek/chat/completions` | deepseek-r1 |
| Grok    | `POST /grok/chat/completions` | grok-3 |
| Kimi    | `POST /kimi/chat/completions` | kimi-k2.5 |

## Forma Unificada de Chamada

Todos os modelos de chat utilizam o mesmo formato de requisição:

```python
import requests

def chat(endpoint, model, message):
    return requests.post(
        f"https://api.acedata.cloud{endpoint}",
        headers={
            "Authorization": "Bearer YOUR_API_TOKEN",
            "Content-Type": "application/json",
        },
        json={
            "model": model,
            "messages": [{"role": "user", "content": message}],
            "max_tokens": 1024,
            "temperature": 0.7,
        },
    ).json()

# Chamando diferentes modelos - basta alterar endpoint e nome do modelo
claude = chat("/v1/chat/completions", "claude-sonnet-4-6", "你好")
gpt = chat("/openai/chat/completions", "gpt-4o", "你好")
gemini = chat("/gemini/chat/completions", "gemini-2.5-flash", "你好")
deepseek = chat("/deepseek/chat/completions", "deepseek-r1", "你好")
```

## Saída em Streaming

Todas as APIs de chat suportam saída em streaming (Server-Sent Events):

```python
data = {
    "model": "claude-sonnet-4-6",
    "messages": [{"role": "user", "content": "写一首诗"}],
    "stream": True,
}

response = requests.post(
    "https://api.acedata.cloud/v1/chat/completions",
    headers={"Authorization": "Bearer YOUR_API_TOKEN"},
    json=data,
    stream=True,
)

for line in response.iter_lines():
    if line:
        print(line.decode())
```

## Compatibilidade com o SDK da OpenAI

Devido à compatibilidade do formato da interface, você pode usar diretamente o SDK oficial da OpenAI, bastando alterar o base_url:

```python
from openai import OpenAI

client = OpenAI(
    api_key="YOUR_API_TOKEN",
    base_url="https://api.acedata.cloud/v1",  # Claude
    # base_url="https://api.acedata.cloud/openai",  # OpenAI
    # base_url="https://api.acedata.cloud/gemini",  # Gemini
)

response = client.chat.completions.create(
    model="claude-sonnet-4-6",
    messages=[{"role": "user", "content": "你好"}],
)
print(response.choices[0].message.content)
```

## Resumo

Com a interface compatível com OpenAI da Ace Data Cloud, você pode:

- Usar um único código para acessar mais de 6 grandes modelos de linguagem
- Alternar modelos de forma flexível para comparar desempenho e custo
- Utilizar diretamente o SDK da OpenAI, sem custo de migração
- Contar com faturamento e monitoramento unificados

Acesse [platform.acedata.cloud](https://platform.acedata.cloud) para testar gratuitamente.
