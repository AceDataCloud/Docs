---
title: "QR Art JavaScript Guia Rápido"
---

## Introdução

QR Art é um serviço de imagem AI fornecido pela Ace Data Cloud, que gera QR codes artísticos com AI. Através da API unificada da Ace Data Cloud, você pode integrar rapidamente o QR Art usando JavaScript, implementando funcionalidades como QR code de link, QR code de texto, estilos predefinidos e parâmetros personalizados.

## Pré-requisitos

- Possuir uma conta na [Ace Data Cloud](https://platform.acedata.cloud) e obter o API Token
- Node.js 18+ ou ambiente de navegador moderno

## Uso Básico

O endpoint principal para chamar a API QR Art é:

```
POST https://api.acedata.cloud/qrart/generate
```

Exemplo completo de código JavaScript:

```javascript
const response = await fetch("https://api.acedata.cloud/qrart/generate", {
  method: "POST",
  headers: {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
  "type": "link",
  "content": "https://platform.acedata.cloud",
  "prompt": "sunset ocean waves",
  "preset": "neon-mech"
}),
});

const result = await response.json();
console.log(result);
```

Substitua `YOUR_API_TOKEN` pelo Token real que você obteve na plataforma Ace Data Cloud.

## Tratamento da Resposta

Recomenda-se verificar o código de status da resposta e tratar erros:

```javascript
if (response.ok) {
  const result = await response.json();
  console.log("Chamada bem-sucedida:", result);
} else {
  console.error(`Falha na chamada, código de status: ${response.status}`);
  const error = await response.text();
  console.error(error);
}
```

## Encapsulamento para Node.js

Recomenda-se encapsular em uma função reutilizável:

```javascript
async function callQRArt(data) {
  const response = await fetch("https://api.acedata.cloud/qrart/generate", {
    method: "POST",
    headers: {
      "Authorization": `Bearer ${process.env.ACE_API_TOKEN}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify(data),
  });
  if (!response.ok) throw new Error(`Erro na API: ${response.status}`);
  return response.json();
}
```

## Tratamento de Erros

Códigos de erro comuns:

| Código | Descrição                          |
|--------|----------------------------------|
| 401    | Falha na autenticação, verifique o API Token |
| 403    | Saldo insuficiente ou acesso negado |
| 429    | Limite de frequência de requisições excedido |
| 500    | Erro interno do servidor         |

## Próximos Passos

- Acesse o [Console da Ace Data Cloud](https://platform.acedata.cloud) para obter o API Token
- Consulte a [Documentação da API](https://docs.acedata.cloud/zh-CN/api-reference) para detalhes completos dos parâmetros
- Verifique os [Planos de Preços](https://platform.acedata.cloud/services) para informações sobre custos
