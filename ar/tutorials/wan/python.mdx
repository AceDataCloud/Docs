---
title: "مقدمة سريعة لـ Wan Python"
---

## مقدمة

Wan هو خدمة فيديو AI تقدمها Ace Data Cloud، وهو نموذج توليد فيديو Wanxiang من Alibaba. من خلال واجهة برمجة التطبيقات الموحدة لـ Ace Data Cloud، يمكنك استخدام Python لدمج Wan بسرعة، وتحقيق وظائف مثل تحويل النص إلى فيديو، تحويل الصور إلى فيديو، توليد فيديو بالاعتماد على فيديو مرجعي، ودعم دقات متعددة.

## المتطلبات المسبقة

- امتلاك حساب على [Ace Data Cloud](https://platform.acedata.cloud) والحصول على API Token
- بيئة Python 3.7 أو أعلى
- تثبيت مكتبة requests: `pip install requests`

## الاستخدام الأساسي

النقطة النهائية الرئيسية لاستدعاء API الخاص بـ Wan هي:

```
POST https://api.acedata.cloud/wan/videos
```

في هذا المثال نستخدم نموذج `wan2.6-t2v`.
النماذج المتاحة تشمل: `wan2.6-t2v`، `wan2.6-i2v`، `wan2.6-r2v`، `wan2.6-i2v-flash`.

مثال كامل على كود Python:

```python
import requests

url = "https://api.acedata.cloud/wan/videos"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "action": "text2video",
    "model": "wan2.6-t2v",
    "prompt": "Astronauts shuttle from space to volcano",
    "resolution": "720P"
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

يرجى استبدال `YOUR_API_TOKEN` بالتوكن الفعلي الذي حصلت عليه من منصة Ace Data Cloud.

## معالجة الاستجابة

بعد الاستدعاء الناجح، يرجع API بيانات بصيغة JSON. يُنصح بفحص رمز حالة HTTP:

```python
if response.status_code == 200:
    result = response.json()
    print("تم الاستدعاء بنجاح:", result)
else:
    print(f"فشل الاستدعاء، رمز الحالة: {response.status_code}")
    print(response.text)
```

## الاستخدام المتقدم

بالنسبة لـ API التي تدعم المهام غير المتزامنة، يمكن الحصول على النتائج عبر عنوان URL للرد:

```python
data['callback_url'] = 'https://your-server.com/callback'
response = requests.post(url, headers=headers, json=data)
# سيتم دفع النتائج إلى خادمك عبر عنوان URL للرد
```

## معالجة الأخطاء

رموز الأخطاء الشائعة:

| رمز الحالة | الوصف |
|------------|-------|
| 401 | فشل التوثيق، يرجى التحقق من API Token |
| 403 | رصيد غير كافٍ أو عدم وجود صلاحية الوصول |
| 429 | تجاوز معدل الطلبات المسموح به |
| 500 | خطأ داخلي في الخادم |

## نقاط نهاية أخرى

توفر خدمة Wan أيضًا النقاط النهائية التالية:

- `POST https://api.acedata.cloud/wan/tasks`

## الخطوات التالية

- انتقل إلى [لوحة تحكم Ace Data Cloud](https://platform.acedata.cloud) للحصول على API Token
- اطلع على [توثيق API](https://docs.acedata.cloud/zh-CN/api-reference) لفهم كامل المعلمات
- اطلع على [خطط التسعير](https://platform.acedata.cloud/services) لمعرفة التكاليف
