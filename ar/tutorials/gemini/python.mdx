---
title: "Gemini Python مقدمة سريعة"
---

## مقدمة

Gemini هي خدمة دردشة AI مقدمة من Ace Data Cloud، تعتمد على نماذج اللغة الكبيرة من سلسلة Google Gemini. من خلال واجهة برمجة التطبيقات الموحدة من Ace Data Cloud، يمكنك استخدام Python لدمج Gemini بسرعة، وتحقيق وظائف مثل المحادثات متعددة الأدوار، تعليمات النظام، الإخراج المتدفق، وضع الإخراج JSON، وغيرها.

## المتطلبات المسبقة

- امتلاك حساب في [Ace Data Cloud](https://platform.acedata.cloud) والحصول على رمز API
- بيئة Python 3.7 أو أعلى
- تثبيت مكتبة requests: `pip install requests`

## الاستخدام الأساسي

نقطة النهاية الرئيسية لاستدعاء Gemini API هي:

```
POST https://api.acedata.cloud/gemini/chat/completions
```

في هذا المثال نستخدم نموذج `gemini-2.5-flash`.
النماذج المتاحة تشمل: `gemini-2.5-flash`، `gemini-2.5-pro`، `gemini-2.0-flash`، `gemini-3.0-pro`.

مثال كامل على كود Python:

```python
import requests

url = "https://api.acedata.cloud/gemini/chat/completions"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "model": "gemini-2.5-flash",
    "messages": [
        {
            "role": "user",
            "content": "你好，请介绍一下你自己"
        }
    ],
    "max_tokens": 1024,
    "temperature": 0.7
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

يرجى استبدال `YOUR_API_TOKEN` بالرمز الفعلي الذي حصلت عليه من منصة Ace Data Cloud.

## معالجة الاستجابة

بعد الاستدعاء الناجح، تعيد API بيانات بصيغة JSON. يُنصح بفحص رمز حالة HTTP:

```python
if response.status_code == 200:
    result = response.json()
    print("تم الاستدعاء بنجاح:", result)
else:
    print(f"فشل الاستدعاء، رمز الحالة: {response.status_code}")
    print(response.text)
```

## الاستخدام المتقدم

بالنسبة لواجهة API الخاصة بالدردشة، يتم دعم الإخراج المتدفق للحصول على استجابات في الوقت الحقيقي:

```python
import requests

data['stream'] = True
response = requests.post(url, headers=headers, json=data, stream=True)
for line in response.iter_lines():
    if line:
        print(line.decode())
```

## معالجة الأخطاء

أكواد الأخطاء الشائعة:

| رمز الحالة | الوصف                     |
|------------|--------------------------|
| 401        | فشل التوثيق، يرجى التحقق من رمز API |
| 403        | رصيد غير كافٍ أو عدم وجود صلاحية الوصول |
| 429        | تجاوز معدل الطلبات        |
| 500        | خطأ داخلي في الخادم       |

## الخطوة التالية

- انتقل إلى [لوحة تحكم Ace Data Cloud](https://platform.acedata.cloud) للحصول على رمز API
- اطلع على [توثيق API](https://docs.acedata.cloud/zh-CN/api-reference) لفهم كامل المعاملات
- اطلع على [خطط التسعير](https://platform.acedata.cloud/services) لمعرفة التكاليف
