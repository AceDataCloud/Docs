---
title: "واجهة برمجة التطبيقات للتحقق من حالة رقم الهاتف"
description: "Identity Authentication API guide - Ace Data Cloud"
---

ستقدم هذه الوثيقة شرحًا لواجهة برمجة التطبيقات للتحقق من حالة رقم الهاتف، والتي يمكن استخدامها للتحقق من حالة رقم الهاتف، يمكنك إدخال رقم الهاتف لإجراء الاستعلام.

## عملية التقديم

لاستخدام واجهة برمجة التطبيقات للتحقق من حالة رقم الهاتف، يجب أولاً الذهاب إلى [وحدة التحكم في Ace Data Cloud](https://platform.acedata.cloud/console/applications) للحصول على رمز API الخاص بك، احتفظ به للاستخدام لاحقًا.

![](https://cdn.acedata.cloud/dvc3cg.jpg)

إذا لم تقم بتسجيل الدخول أو التسجيل بعد، سيتم تحويلك تلقائيًا إلى صفحة تسجيل الدخول لدعوتك للتسجيل وتسجيل الدخول، وبعد الانتهاء، سيتم إرجاعك تلقائيًا إلى الصفحة الحالية.

**يمكن استخدام رمز API واحد لاستدعاء جميع خدمات المنصة، دون الحاجة لتقديم طلب منفصل لكل خدمة.** عند التقديم لأول مرة، ستحصل على رصيد مجاني، يمكنك تجربته مجانًا؛ عند نفاد الرصيد، يمكنك إعادة شحن الرصيد العام في [وحدة التحكم](https://platform.acedata.cloud/console/coin).

> 📘 الوثائق الكاملة: [واجهة برمجة التطبيقات للتحقق من حالة رقم الهاتف →](https://platform.acedata.cloud/documents/identity-phone-check-1e)

## الاستخدام الأساسي

أولاً، يجب أن نفهم طريقة الاستخدام الأساسية، وهي إدخال رقم الهاتف، للحصول على النتيجة المعالجة، يجب أولاً تمرير حقل `mobile` ببساطة. بعد ذلك، يمكننا ملء المحتوى المقابل على الواجهة، كما هو موضح في الصورة:

<p><img src="https://cdn.acedata.cloud/r17llo.png" width="500" className="m-auto" /></p>

يمكننا أن نرى هنا أننا قمنا بتعيين رؤوس الطلب، بما في ذلك:

- `accept`: نوع الاستجابة التي ترغب في تلقيها، هنا يتم ملؤها بـ `application/json`، أي بتنسيق JSON.
- `authorization`: مفتاح استدعاء واجهة برمجة التطبيقات، يمكن اختياره مباشرة بعد التقديم.

كما تم تعيين جسم الطلب، بما في ذلك:

- `mobile`: رقم الهاتف الذي يحتاج إلى المعالجة، وهو حقل مطلوب.
- `encryption`: اختياري، معلمة تشفير الحقول الحساسة (إذا كان هناك حاجة لإرسال نص مشفر).

بعد الاختيار، يمكننا أن نلاحظ أن الجانب الأيمن قد أنشأ أيضًا كودًا مطابقًا، كما هو موضح في الصورة:

<p><img src="https://cdn.acedata.cloud/dpun55.png" width="500" className="m-auto" /></p>

يمكنك النقر على زر "Try" لإجراء الاختبار، كما هو موضح في الصورة أعلاه، هنا حصلنا على النتيجة التالية:

```json
{
  "result": "0",
  "description": "نجاح",
  "status_code": 0
}
```

تتضمن النتيجة العائدة عدة حقول، كما هو موضح أدناه:

- `result`، رمز نتيجة التحقق، حالة الرسوم كما يلي.
    - رموز نتائج الرسوم:
        - 0: نجاح
    - رموز النتائج غير المدفوعة:
        - -1: لم يتم العثور على نتيجة
        - -2: تنسيق رقم الهاتف غير صحيح
        - -3: خدمة مركز التحقق مشغولة
- `description`، وصف نتيجة العمل.
- `status_code`، رمز الحالة:
    - 0: طبيعي
    - 1: متوقف
    - 2: تم إلغاء الرقم
    - 3: رقم فارغ
    - 4: غير متصل بالشبكة
    - 99: حالة غير معروفة

يمكننا أن نرى أن حالة رقم الهاتف قد تم التحقق منها.

بالإضافة إلى ذلك، إذا كنت ترغب في إنشاء كود التكامل المقابل، يمكنك نسخه مباشرة، على سبيل المثال، كود CURL كما يلي:

```shell
curl -X POST 'https://api.acedata.cloud/identity/phone/check-1e' \
-H 'accept: application/json' \
-H 'authorization: Bearer {token}' \
-H 'content-type: application/json' \
-d '{
  "mobile": "***"
}'
```

كود التكامل بلغة Python كما يلي:

```python
import requests

url = "https://api.acedata.cloud/identity/phone/check-1e"

headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json"
}

payload = {
    "mobile": "***"
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

## معالجة الأخطاء

عند استدعاء واجهة برمجة التطبيقات، إذا واجهت خطأ، ستقوم واجهة برمجة التطبيقات بإرجاع رمز الخطأ والمعلومات المناسبة. على سبيل المثال:

- `400 token_mismatched`: طلب غير صحيح، ربما بسبب معلمات مفقودة أو غير صحيحة.
- `400 api_not_implemented`: طلب غير صحيح، ربما بسبب معلمات مفقودة أو غير صحيحة.
- `401 invalid_token`: غير مصرح، رمز تفويض غير صالح أو مفقود.
- `429 too_many_requests`: عدد كبير جدًا من الطلبات، لقد تجاوزت الحد الأقصى لمعدل الطلبات.
- `500 api_error`: خطأ في الخادم الداخلي، حدث خطأ ما على الخادم.

### مثال على استجابة الخطأ

```json
{
  "success": false,
  "error": {
    "code": "api_error",
    "message": "فشل في الاسترجاع"
  },
  "trace_id": "2cf86e86-22a4-46e1-ac2f-032c0f2a4e89"
}
```

## الخاتمة

من خلال هذه الوثيقة، لقد فهمت كيفية استخدام واجهة برمجة التطبيقات للتحقق من حالة رقم الهاتف للتحقق من حالة رقم الهاتف، يمكنك إدخال رقم الهاتف لإجراء الاستعلام. نأمل أن تساعدك هذه الوثيقة في تكامل واستخدام هذه الواجهة بشكل أفضل. إذا كان لديك أي استفسارات، فلا تتردد في الاتصال بفريق الدعم الفني لدينا.
