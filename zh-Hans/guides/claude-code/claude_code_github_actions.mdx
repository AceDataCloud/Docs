---
title: "GitHub Actions：用平台 Key 运行 Claude Code"
description: "Claude Code 集成指南 - Ace Data Cloud"
---

## 获取 Ace Data Cloud API Key

1. 打开 [控制台 → 应用列表](https://platform.acedata.cloud/console/applications)，完成登录。
2. 按计费方式选择应用：使用 Coding Plan 时进入 **Coding Application**；按量使用时选择有对应服务权限的通用/服务应用。Coding 套餐与通用余额分别管理。
3. 复制该应用的 **API Key / API Token**，不要复制 Application ID 或 `platform-` 管理 Token。下文 API Key 字段只填写密钥本身，不含 `Bearer ` 前缀。
4. 确认应用有效且有额度。活动和免费额度以控制台实时显示为准。

用同一把 Key 查询模型目录：

```bash
export ACEDATACLOUD_API_KEY="YOUR_ACEDATACLOUD_API_KEY"
curl --fail-with-body https://api.acedata.cloud/v1/models \
  -H "Authorization: Bearer $ACEDATACLOUD_API_KEY"
```

PowerShell 环境变量写法为 `$env:ACEDATACLOUD_API_KEY = "YOUR_ACEDATACLOUD_API_KEY"`；使用 Windows 自带 curl 时写 `curl.exe`。`/v1/models` 是模型发现入口，目录中出现某个模型不等于当前 Key 有对应 API 权限。还要核对所选协议、应用范围和客户端能力。

下文 `MODEL_ID` 必须替换为具体模型 ID。不要把真实 Key 提交到 Git、贴进日志或截图。

## 1. 保存仓库 Secret

目标仓库 **Settings → Secrets and variables → Actions → New repository secret**，名称 `ACEDATACLOUD_API_KEY`，值填应用 Key 本身。不要把 Key 放在 workflow 文本中。

## 2. 添加最小手动工作流

保存为 `.github/workflows/claude-acedata.yml`：

```yaml
name: Claude Code via Ace Data Cloud
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @anthropic-ai/claude-code@2.1.263
      - name: Verify model connection
        env:
          ANTHROPIC_BASE_URL: https://api.acedata.cloud
          ANTHROPIC_AUTH_TOKEN: ${{ secrets.ACEDATACLOUD_API_KEY }}
        run: |
          test -n "$ANTHROPIC_AUTH_TOKEN"
          claude --version
          claude -p --model claude-sonnet-4-6 --max-turns 1 "Reply only ADC_CLAUDE_OK"
```

在 Actions 选择此工作流，点击 **Run workflow**。验收应同时包含日志中的真实回复与平台使用历史。本轮在隔离本地环境验证了同版本 CLI、同一 Base URL/鉴权及模型，返回 `ADC_CLAUDE_OK`；本页没有宣称已在你的 GitHub 仓库执行工作流。

## 3. 逐步增加实际任务

连通后可把提示词替换为“阅读 README，输出三条改进建议，不改文件”。如要自动修改、提交或评论 PR，需额外设计 GitHub 权限、可信触发条件与提交策略。

`anthropics/claude-code-action@v1` 是另一种集成，带 GitHub App、事件和权限约定，不能把 CLI 的环境变量教程当作它所有输入字段的等价替代。需要该 Action 时按官方 GitHub Actions 文档配置并独立测试。旧教程的本地缓冲代理与 dummy API Key 不再作为默认起步方案。

## 实战与验收

先新建一个测试目录，在 `fixture.txt` 写入 `hello from Ace Data Cloud`，然后让客户端执行：

> 读取 fixture.txt，逐字引用其内容并解释文件用途。不要修改任何文件。

检查是否真的读取文件、是否引用正确内容；然后到 [平台使用历史](https://platform.acedata.cloud/console/usages) 按时间核对请求。对不支持文件工具的聊天模式，先附加该文件，再提同样的问题。不要用一次简单聊天成功推断编辑、图片、工具、自动补全全部可用。

| 现象 | 排查 |
|---|---|
| 401 | API Key 是否完整、是否重复 Bearer 前缀、进程是否读取到变量 |
| 403 / 额度错误 | Key 所属应用、套餐有效期、API 权限与余额 |
| 404 | Base URL 是否重复 `/v1` 或拼入了不该填写的完整接口路径；模型 ID 是否正确 |
| 聊天成功但 Agent 失败 | 检查当前模型的工具调用能力与客户端模式，先完成上面的只读测试 |
| 修改未生效 | 重启客户端；Remote/WSL/容器的配置和变量通常需要放在实际运行端 |

## 核对依据

配置于 2026-09-07 对照下列官方资料核对；除明确列出的实测外，不把所有系统与客户端版本都标为已跑通。

- [Claude Code GitHub Actions](https://code.claude.com/docs/en/github-actions)
- [Claude Code 设置](https://code.claude.com/docs/en/settings)
- [官方 Action 源码](https://github.com/anthropics/claude-code-action)

返回 [Coding Plan 配置中心](https://platform.acedata.cloud/documents/coding-plan-integration)。
