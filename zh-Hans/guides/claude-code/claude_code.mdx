---
title: "Claude Code 配置总览"
description: "Claude Code 集成指南 - Ace Data Cloud"
---

## 获取 Ace Data Cloud API Key

1. 打开 [控制台 → 应用列表](https://platform.acedata.cloud/console/applications)，完成登录。
2. 按计费方式选择应用：使用 Coding Plan 时进入 **Coding Application**；按量使用时选择有对应服务权限的通用/服务应用。Coding 套餐与通用余额分别管理。
3. 复制该应用的 **API Key / API Token**，不要复制 Application ID 或 `platform-` 管理 Token。下文 API Key 字段只填写密钥本身，不含 `Bearer ` 前缀。
4. 确认应用有效且有额度。活动和免费额度以控制台实时显示为准。

用同一把 Key 查询模型目录：

```bash
export ACEDATACLOUD_API_KEY="YOUR_ACEDATACLOUD_API_KEY"
curl --fail-with-body https://api.acedata.cloud/v1/models \
  -H "Authorization: Bearer $ACEDATACLOUD_API_KEY"
```

PowerShell 环境变量写法为 `$env:ACEDATACLOUD_API_KEY = "YOUR_ACEDATACLOUD_API_KEY"`；使用 Windows 自带 curl 时写 `curl.exe`。`/v1/models` 是模型发现入口，目录中出现某个模型不等于当前 Key 有对应 API 权限。还要核对所选协议、应用范围和客户端能力。

下文 `MODEL_ID` 必须替换为具体模型 ID。不要把真实 Key 提交到 Git、贴进日志或截图。


Claude Code 通过 Anthropic Messages 协议接入 Coding Plan，提供终端、IDE、桌面配置管理与 CI 等入口。

| 使用入口 | 配置指南 |
|---|---|
| Terminal / CLI | [Claude Code 终端配置](https://platform.acedata.cloud/documents/claude-code-terminal-integration) |
| VS Code | [Claude Code for VS Code 配置](https://platform.acedata.cloud/documents/claude-code-vscode-integrations) |

| JetBrains 插件 | [JetBrains 插件](https://platform.acedata.cloud/documents/claude-code-jetbrains-integration) |
| CC Switch 图形配置 | [CC Switch 图形配置](https://platform.acedata.cloud/documents/claude-code-cc-switch-integration) |
| GitHub Actions | [GitHub Actions](https://platform.acedata.cloud/documents/claude-code-github-actions-integration) |
| 远程 MCP 工具 | [远程 MCP 工具](https://platform.acedata.cloud/documents/claude-code-mcp-all) |

## 共通配置

- Base URL：`https://api.acedata.cloud`
- API Token：从 Coding Application 获取，并通过指南中的 `ANTHROPIC_AUTH_TOKEN` 配置
- Model ID：从实时模型目录选择
- 修改配置后：重新启动 Claude Code；VS Code 用户需要重载窗口

不要同时保留旧的 `ANTHROPIC_API_KEY`，也不要把真实 Token 写入团队共享文件。

返回 [Coding Plan 配置中心](https://platform.acedata.cloud/documents/coding-plan-integration)。

## 本轮核对与首次验收

2026-09-07 更新。选择上面的具体客户端教程，依次完成安装、Key 获取、Provider 配置、一次文本回复和一次文件/工具任务。Claude Code 2.1.263 已通过平台 Messages 返回固定测试文本，客户端支持边界请看各篇说明；套餐与模型目录以当前控制台为准。
