---
title: "reCAPTCHA JavaScript Brzi početak"
---

## Uvod

reCAPTCHA je usluga verifikacije koju pruža Ace Data Cloud, rešenje Google reCAPTCHA. Kroz jedinstveni API Ace Data Cloud-a, možete brzo integrisati reCAPTCHA koristeći JavaScript i ostvariti funkcionalnosti kao što su reCAPTCHA v2 Token, reCAPTCHA v2 prepoznavanje, reCAPTCHA v3 Token i druge.

## Preduslovi

- Imati nalog na [Ace Data Cloud](https://platform.acedata.cloud) i dobiti API Token
- Node.js 18+ ili moderan pregledač

## Osnovna upotreba

Glavna tačka poziva reCAPTCHA API-ja je:

```
POST https://api.acedata.cloud/captcha/token/recaptcha2
```

Kompletan primer JavaScript koda:

```javascript
const response = await fetch("https://api.acedata.cloud/captcha/token/recaptcha2", {
  method: "POST",
  headers: {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
  "website_key": "6Le-wvkSAAAAAPBMRTvw0Q4Muexq9bi0DJwx_mJ-",
  "website_url": "https://www.google.com/recaptcha/api2/demo"
}),
});

const result = await response.json();
console.log(result);
```

Zamenite `YOUR_API_TOKEN` stvarnim Tokenom koji ste dobili na Ace Data Cloud platformi.

## Obrada odgovora

Preporučuje se provera statusnog koda odgovora i rukovanje greškama:

```javascript
if (response.ok) {
  const result = await response.json();
  console.log("Poziv uspešan:", result);
} else {
  console.error(`Poziv neuspešan, statusni kod: ${response.status}`);
  const error = await response.text();
  console.error(error);
}
```

## Node.js enkapsulacija

Preporučuje se enkapsulacija u funkciju za višekratnu upotrebu:

```javascript
async function callreCAPTCHA(data) {
  const response = await fetch("https://api.acedata.cloud/captcha/token/recaptcha2", {
    method: "POST",
    headers: {
      "Authorization": `Bearer ${process.env.ACE_API_TOKEN}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify(data),
  });
  if (!response.ok) throw new Error(`API greška: ${response.status}`);
  return response.json();
}
```

## Rukovanje greškama

Česti statusni kodovi grešaka:

| Statusni kod | Objašnjenje                      |
|--------------|---------------------------------|
| 401          | Autentifikacija neuspešna, proverite API Token |
| 403          | Nedovoljno sredstava ili nema pristupa |
| 429          | Prekoračena frekvencija zahteva |
| 500          | Interna greška servera          |

## Sledeći koraci

- Posetite [Ace Data Cloud kontrolnu tablu](https://platform.acedata.cloud) da biste dobili API Token
- Pogledajte [API dokumentaciju](https://docs.acedata.cloud/zh-CN/api-reference) za kompletna objašnjenja parametara
- Pogledajte [cenovne planove](https://platform.acedata.cloud/services) za informacije o troškovima
