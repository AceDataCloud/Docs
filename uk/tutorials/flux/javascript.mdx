---
title: "Flux JavaScript Швидкий старт"
---

## Вступ

Flux — це AI сервіс зображень від Ace Data Cloud, модель генерації зображень Black Forest Labs Flux. Через уніфікований API Ace Data Cloud ви можете швидко інтегрувати Flux за допомогою JavaScript, реалізуючи функції генерації з тексту, редагування зображень, використання різних моделей, налаштування розмірів тощо.

## Вимоги

- Обліковий запис [Ace Data Cloud](https://platform.acedata.cloud) та отриманий API Token
- Node.js 18+ або сучасне браузерне середовище

## Базове використання

Основна кінцева точка для виклику Flux API:

```
POST https://api.acedata.cloud/flux/images
```

У цьому прикладі використовується модель `flux-pro-1.1`.
Доступні моделі: `flux-dev`, `flux-pro`, `flux-pro-1.1`, `flux-pro-1.1-ultra`, `flux-kontext-pro`, `flux-kontext-max`.

Повний приклад коду на JavaScript:

```javascript
const response = await fetch("https://api.acedata.cloud/flux/images", {
  method: "POST",
  headers: {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
  "action": "generate",
  "prompt": "A white siamese cat sitting on a windowsill",
  "model": "flux-pro-1.1",
  "size": "1024x1024"
}),
});

const result = await response.json();
console.log(result);
```

Будь ласка, замініть `YOUR_API_TOKEN` на ваш фактичний токен, отриманий на платформі Ace Data Cloud.

## Обробка відповіді

Рекомендується перевіряти статус відповіді та обробляти помилки:

```javascript
if (response.ok) {
  const result = await response.json();
  console.log("Виклик успішний:", result);
} else {
  console.error(`Виклик не вдався, статус: ${response.status}`);
  const error = await response.text();
  console.error(error);
}
```

## Обгортка для Node.js

Рекомендується створити повторно використовувану функцію:

```javascript
async function callFlux(data) {
  const response = await fetch("https://api.acedata.cloud/flux/images", {
    method: "POST",
    headers: {
      "Authorization": `Bearer ${process.env.ACE_API_TOKEN}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify(data),
  });
  if (!response.ok) throw new Error(`API error: ${response.status}`);
  return response.json();
}
```

## Обробка помилок

Поширені коди помилок:

| Код стану | Опис |
|-----------|-------|
| 401 | Аутентифікація не вдалася, перевірте API Token |
| 403 | Недостатньо коштів або відсутній доступ |
| 429 | Перевищено ліміт запитів |
| 500 | Внутрішня помилка сервера |

## Наступні кроки

- Перейдіть до [Ace Data Cloud Консолі](https://platform.acedata.cloud) для отримання API Token
- Ознайомтеся з [API документацією](https://docs.acedata.cloud/zh-CN/api-reference) для повного опису параметрів
- Перегляньте [Плани тарифікації](https://platform.acedata.cloud/services) для інформації про вартість
