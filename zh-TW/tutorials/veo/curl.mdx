---
title: "Veo cURL 快速入門"
---

## 簡介

Veo 是 Ace Data Cloud 提供的 AI 視頻服務，Google Veo 視頻生成模型。透過 Ace Data Cloud 統一 API，你可以使用 cURL 快速調用 Veo，實現文生視頻、圖生視頻、4K 解析度、多種比例等功能。

## 前置條件

- 擁有 [Ace Data Cloud](https://platform.acedata.cloud) 帳號並獲取 API Token
- 安裝了 cURL 的終端環境

## 基礎用法

調用 Veo API 的主端點為：

```
POST https://api.acedata.cloud/veo/videos
```

本示例使用 `veo3` 模型。
可用模型包括：`veo2`、`veo2-fast`、`veo3`、`veo3-fast`、`veo31`、`veo31-fast`。

完整 cURL 命令示例：

```bash
curl -X POST https://api.acedata.cloud/veo/videos \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"action": "text2video", "prompt": "A coffee mug rotating slowly on a marble counter", "model": "veo3"}'
```

請將 `YOUR_API_TOKEN` 替換為你在 Ace Data Cloud 平台獲取的實際 Token。

## 回應示例

成功調用後返回 JSON 格式資料，例如：

```json
{
  "status": "success",
  "data": {
    ...
  }
}
```

## 使用 jq 處理回應

推薦使用 jq 工具格式化輸出：

```bash
curl -s -X POST https://api.acedata.cloud/veo/videos \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"action": "text2video", "prompt": "A coffee mug rotating slowly on a marble counter", "model": "veo3"}' | jq .
```

## 錯誤處理

常見錯誤碼：

| 狀態碼 | 說明 |
|--------|------|
| 401 | 認證失敗，請檢查 API Token |
| 403 | 餘額不足或無權訪問 |
| 429 | 請求頻率超限 |
| 500 | 伺服器內部錯誤 |

## 下一步

- 前往 [Ace Data Cloud 控制台](https://platform.acedata.cloud) 獲取 API Token
- 查看 [API 文件](https://docs.acedata.cloud/zh-CN/api-reference) 了解完整參數說明
- 查看 [定價方案](https://platform.acedata.cloud/services) 了解費用
