---
title: "Image2Text JavaScript Schnellstart"
---

## Einführung

Image2Text ist ein Captcha-Dienst von Ace Data Cloud zur Erkennung von Bild-Captchas. Über die einheitliche API von Ace Data Cloud können Sie Image2Text schnell mit JavaScript integrieren und Funktionen wie Bild-Captcha-Erkennung, Base64-Eingabe und URL-Eingabe realisieren.

## Voraussetzungen

- Ein [Ace Data Cloud](https://platform.acedata.cloud) Konto und ein API Token
- Node.js 18+ oder eine moderne Browser-Umgebung

## Grundlegende Verwendung

Der Hauptendpunkt für den Aufruf der Image2Text API lautet:

```
POST https://api.acedata.cloud/captcha/recognition/image2text
```

Vollständiges JavaScript-Beispiel:

```javascript
const response = await fetch("https://api.acedata.cloud/captcha/recognition/image2text", {
  method: "POST",
  headers: {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
  "image": "data:image/png;base64,iVBORw0KGgoAAAANSUhEUg..."
}),
});

const result = await response.json();
console.log(result);
```

Bitte ersetzen Sie `YOUR_API_TOKEN` durch den tatsächlichen Token, den Sie auf der Ace Data Cloud Plattform erhalten haben.

## Antwortverarbeitung

Es wird empfohlen, den Antwortstatus zu prüfen und Fehler zu behandeln:

```javascript
if (response.ok) {
  const result = await response.json();
  console.log("Aufruf erfolgreich:", result);
} else {
  console.error(`Aufruf fehlgeschlagen, Statuscode: ${response.status}`);
  const error = await response.text();
  console.error(error);
}
```

## Node.js Wrapper

Empfohlen wird die Kapselung in eine wiederverwendbare Funktion:

```javascript
async function callImage2Text(data) {
  const response = await fetch("https://api.acedata.cloud/captcha/recognition/image2text", {
    method: "POST",
    headers: {
      "Authorization": `Bearer ${process.env.ACE_API_TOKEN}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify(data),
  });
  if (!response.ok) throw new Error(`API Fehler: ${response.status}`);
  return response.json();
}
```

## Fehlerbehandlung

Häufige Fehlercodes:

| Statuscode | Beschreibung                          |
|------------|-------------------------------------|
| 401        | Authentifizierung fehlgeschlagen, bitte API Token prüfen |
| 403        | Unzureichendes Guthaben oder kein Zugriff |
| 429        | Anfragerate überschritten            |
| 500        | Interner Serverfehler                |

## Nächste Schritte

- Besuchen Sie die [Ace Data Cloud Konsole](https://platform.acedata.cloud), um Ihren API Token zu erhalten
- Lesen Sie die [API-Dokumentation](https://docs.acedata.cloud/zh-CN/api-reference) für vollständige Parameterdetails
- Informieren Sie sich über die [Preismodelle](https://platform.acedata.cloud/services) für Kostenübersicht
