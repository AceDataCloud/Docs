---
title: "Kling Python Schnellstart"
---

## Einführung

Kling ist ein von Ace Data Cloud bereitgestellter KI-Video-Service, ein schnelles und flexibles Videogenerierungsmodell. Über die einheitliche API von Ace Data Cloud kannst du Kling mit Python schnell integrieren und Funktionen wie Text-zu-Video, Bild-zu-Video, Video-Fortsetzung, verschiedene Längen und Seitenverhältnisse nutzen.

## Voraussetzungen

- Ein [Ace Data Cloud](https://platform.acedata.cloud) Konto und ein API-Token
- Python 3.7 oder höher
- Installation der requests-Bibliothek: `pip install requests`

## Grundlegende Verwendung

Der Hauptendpunkt für den Aufruf der Kling API ist:

```
POST https://api.acedata.cloud/kling/videos
```

Dieses Beispiel verwendet das Modell `kling-v2-5-turbo`.
Verfügbare Modelle sind: `kling-v1`, `kling-v1-6`, `kling-v2-master`, `kling-v2-1-master`, `kling-v2-5-turbo`, `kling-video-o1`.

Vollständiges Python-Codebeispiel:

```python
import requests

url = "https://api.acedata.cloud/kling/videos"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "action": "text2video",
    "prompt": "A white cat walking in the snow",
    "model": "kling-v2-5-turbo",
    "duration": 5,
    "aspect_ratio": "16:9"
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

Bitte ersetze `YOUR_API_TOKEN` durch dein tatsächliches Token, das du auf der Ace Data Cloud Plattform erhalten hast.

## Antwortverarbeitung

Nach einem erfolgreichen Aufruf gibt die API Daten im JSON-Format zurück. Es wird empfohlen, den HTTP-Statuscode zu prüfen:

```python
if response.status_code == 200:
    result = response.json()
    print("Aufruf erfolgreich:", result)
else:
    print(f"Aufruf fehlgeschlagen, Statuscode: {response.status_code}")
    print(response.text)
```

## Erweiterte Verwendung

Für APIs, die asynchrone Aufgaben unterstützen, kannst du Ergebnisse über eine Callback-URL erhalten:

```python
data['callback_url'] = 'https://your-server.com/callback'
response = requests.post(url, headers=headers, json=data)
# Die Ergebnisse werden über die Callback-URL an deinen Server gesendet
```

## Fehlerbehandlung

Häufige Fehlercodes:

| Statuscode | Beschreibung |
|------------|--------------|
| 401 | Authentifizierung fehlgeschlagen, bitte API-Token prüfen |
| 403 | Unzureichendes Guthaben oder keine Zugriffsberechtigung |
| 429 | Anfragelimit überschritten |
| 500 | Interner Serverfehler |

## Weitere Endpunkte

Der Kling-Service bietet außerdem folgende Endpunkte an:

- `POST https://api.acedata.cloud/kling/tasks`
- `POST https://api.acedata.cloud/kling/motion`

## Nächste Schritte

- Besuche die [Ace Data Cloud Konsole](https://platform.acedata.cloud), um dein API-Token zu erhalten
- Sieh dir die [API-Dokumentation](https://docs.acedata.cloud/zh-CN/api-reference) für vollständige Parameterbeschreibungen an
- Informiere dich über die [Preismodelle](https://platform.acedata.cloud/services) für Kostenübersicht
