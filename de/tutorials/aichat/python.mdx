---
title: "AI Chat Python Schnellstart"
---

## Einführung

AI Chat ist ein von Ace Data Cloud bereitgestellter AI-Chat-Service mit Multi-Modell-Unterstützung. Über die einheitliche API von Ace Data Cloud können Sie AI Chat schnell mit Python integrieren und Funktionen wie Multi-Modell-Support, zustandsbehaftete Dialoge und vordefinierte Rollen realisieren.

## Voraussetzungen

- Ein [Ace Data Cloud](https://platform.acedata.cloud) Konto und ein API-Token
- Python 3.7 oder höher
- Installation der requests-Bibliothek: `pip install requests`

## Grundlegende Verwendung

Der Hauptendpunkt für den Aufruf der AI Chat API lautet:

```
POST https://api.acedata.cloud/aichat/conversations
```

Im Beispiel wird das Modell `gpt-4o` verwendet. Verfügbare Modelle sind: `gpt-4o`, `gpt-4`, `deepseek-r1`, `grok-3`.

Vollständiges Python-Beispiel:

```python
import requests

url = "https://api.acedata.cloud/aichat/conversations"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "model": "gpt-4o",
    "question": "什么是人工智能？",
    "stateful": true
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

Ersetzen Sie `YOUR_API_TOKEN` durch den tatsächlichen Token, den Sie auf der Ace Data Cloud Plattform erhalten haben.

## Antwortverarbeitung

Nach erfolgreichem Aufruf gibt die API Daten im JSON-Format zurück. Es wird empfohlen, den HTTP-Statuscode zu prüfen:

```python
if response.status_code == 200:
    result = response.json()
    print("Aufruf erfolgreich:", result)
else:
    print(f"Aufruf fehlgeschlagen, Statuscode: {response.status_code}")
    print(response.text)
```

## Erweiterte Verwendung

Für APIs, die asynchrone Aufgaben unterstützen, können Ergebnisse über eine Callback-URL empfangen werden:

```python
data['callback_url'] = 'https://your-server.com/callback'
response = requests.post(url, headers=headers, json=data)
# Das Ergebnis wird an Ihre Server-Callback-URL gesendet
```

## Fehlerbehandlung

Häufige Fehlercodes:

| Statuscode | Beschreibung                        |
|------------|-----------------------------------|
| 401        | Authentifizierung fehlgeschlagen, bitte API-Token prüfen |
| 403        | Unzureichendes Guthaben oder keine Zugriffsberechtigung |
| 429        | Anfragelimit überschritten         |
| 500        | Interner Serverfehler              |

## Nächste Schritte

- Besuchen Sie die [Ace Data Cloud Konsole](https://platform.acedata.cloud), um Ihren API-Token zu erhalten
- Lesen Sie die [API-Dokumentation](https://docs.acedata.cloud/zh-CN/api-reference) für vollständige Parameterinformationen
- Informieren Sie sich über die [Preismodelle](https://platform.acedata.cloud/services) für Kostenübersicht
