---
title: "Sora Videos Generation API Integrationsanleitung"
description: "Sora Video Generation 集成指南 - Ace Data Cloud"
---

Dieser Artikel beschreibt die Integrationsanleitung für die Sora Videos Generation API. Mit dieser API können Sie benutzerdefinierte Parameter eingeben, um offizielle Sora-Videos zu generieren. Die API unterstützt zwei Versionsmodi:

- **Version 1 (Klassischer Modus)**: Unterstützt Parameter wie `duration` (10/15/25 Sekunden), `orientation` (Querformat/Hochformat), `size` (small/large Auflösung), Referenzbilder `image_urls`, Charakter-**###** `character_url` usw.
- **Version 2 (Partner-Modus)**: Unterstützt `seconds` (4/8/12 Sekunden), pixelgenaue Auflösung `size` (z.B. 1280x720), Referenzbild `input_reference` usw.

## Antragsprozess

Um die API zu nutzen, müssen Sie zunächst auf der entsprechenden Seite der [Sora Videos Generation API](https://platform.acedata.cloud/documents/99a24421-2e22-4028-8201-e19cb834b67e) den Dienst beantragen. Nach dem Aufruf der Seite klicken Sie auf die Schaltfläche „Acquire“, wie im Bild gezeigt:

![](https://cdn.acedata.cloud/q6ytrc.png)

Falls Sie noch nicht eingeloggt oder registriert sind, werden Sie automatisch zur Login-Seite weitergeleitet, um sich zu registrieren oder anzumelden. Nach dem Login oder der Registrierung kehren Sie automatisch zur aktuellen Seite zurück.

Bei der ersten Beantragung erhalten Sie ein kostenloses Kontingent, mit dem Sie die API kostenlos nutzen können.

## Grundlegende Nutzung (Version 1)

Zunächst lernen wir die grundlegende Nutzung von Version 1 kennen: Sie geben den Prompt `prompt`, ein Array von Referenzbild-URLs `image_urls` sowie das Modell `model` ein, um das Ergebnis zu erhalten. Die Details sind wie folgt:

<p><img src="https://cdn.acedata.cloud/h8dyz3.png" width="500" className="m-auto" /></p>

Hier sehen wir die Request Headers:

- `accept`: Das gewünschte Antwortformat, hier `application/json` für JSON.
- `authorization`: Der API-Schlüssel, der nach Beantragung direkt aus der Dropdown-Liste ausgewählt werden kann.

Außerdem die Request Body Parameter:

- `model`: Das Modell zur Videoerzeugung, unterstützt `sora-2` (Standardmodus) und `sora-2-pro` (HD-Modus). `sora-2-pro` unterstützt Videos mit einer Dauer von 25 Sekunden, `sora-2` nur 10 oder 15 Sekunden (nur Version 1).
- `size`: Videoauflösung, `small` für Standardauflösung, `large` für HD-Auflösung (nur Version 1).
- `duration`: Videolänge, unterstützt 10, 15, 25 Sekunden, wobei 25 Sekunden nur von `sora-2-pro` unterstützt wird (nur Version 1).
- `orientation`: Bildformat, unterstützt `landscape` (Querformat), `portrait` (Hochformat) (nur Version 1).
- `image_urls`: Array von Referenzbild-URLs für die Bild-zu-Video-Generierung (nur Version 1).
- `character_url`: Charakter-**###** URL, im Video dürfen keine echten Personen erscheinen (nur Version 1).
- `character_start`/`character_end`: Start- und Endzeitpunkt des Charakters in Sekunden, Differenz 1-3 Sekunden (nur Version 1).
- `prompt`: Eingabeaufforderung (Pflichtfeld).
- `callback_url`: URL für asynchrone Rückrufe.
- `version`: API-Version, `1` (Standard) oder `2`.

Nach Auswahl sehen Sie auf der rechten Seite den generierten Code, wie im Bild gezeigt:

<p><img src="https://cdn.acedata.cloud/g04qjz.png" width="500" className="m-auto" /></p>

Klicken Sie auf „Try“, um den Test durchzuführen. Das Ergebnis sieht wie folgt aus:

```json
{
  "success": true,
  "task_id": "6bf7fb83-5814-4e3e-a4ad-bfa0c26c0b33",
  "trace_id": "96166698-4b66-478d-a26b-77a7269c9e01",
  "data": [
    {
      "id": "sora-2:task_01k7770rgsevxsmtpbn7xnm5gh",
      "video_url": "https://filesystem.site/gptimage/vg-assets/assets%2Ftask_01k7770rgsevxsmtpbn7xnm5gh%2Ftask_01k7770rgsevxsmtpbn7xnm5gh_genid_0bf958d3-cae7-4298-b7b6-99ae439a1ea6_25_10_10_14_06_975715%2Fvideos%2F00000%2Fsrc.mp4?st=2025-10-10T12%3A30%3A38Z&se=2025-10-16T13%3A30%3A38Z&sks=b&skt=2025-10-10T12%3A30%3A38Z&ske=2025-10-16T13%3A30%3A38Z&sktid=a48cca56-e6da-484e-a814-9c849652bcb3&skoid=8ebb0df1-a278-4e2e-9c20-f2d373479b3a&skv=2019-02-02&sv=2018-11-09&sr=b&sp=r&spr=https%2Chttp&sig=jigY6Z5qp8%2BTXYobaW0EAJ4%2Fbx6G7t6V1P0iyDeUq48%3D&az=oaivgprodscus",
      "state": "succeeded"
    }
  ]
}
```

Die Rückgabe enthält mehrere Felder:

- `success`: Status der Videoerstellungsaufgabe.
- `task_id`: ID der Videoerstellungsaufgabe.
- `trace_id`: Tracking-ID der Aufgabe.
- `data`: Ergebnisliste der Videoerstellung.
  - `id`: Video-ID der Aufgabe.
  - `video_url`: Video-URL der Aufgabe.
  - `state`: Status der Aufgabe.

Sie erhalten somit die Video-Informationen und können das generierte Sora-Video über die `video_url` im `data`-Feld abrufen.

Wenn Sie den entsprechenden Integrationscode generieren möchten, können Sie diesen direkt kopieren, z.B. CURL-Code:

```shell
curl -X POST 'https://api.acedata.cloud/sora/videos' \
-H 'accept: application/json' \
-H 'authorization: Bearer {token}' \
-H 'content-type: application/json' \
-d '{
  "size": "large",
  "duration": 15,
  "orientation": "landscape",
  "prompt": "cat running on the river",
  "model": "sora-2"
}'
```

## Bild-zu-Video-Aufgabe (Version 1)

Für Bild-zu-Video-Aufgaben muss das Parameter `image_urls` mit Referenzbild-URLs übergeben werden, um Folgendes zu spezifizieren:

- `image_urls`: Array der Referenzbild-URLs für die Bild-zu-Video-Aufgabe. Bitte keine echten Personen mit Gesicht verwenden, da dies zum Fehlschlag der Aufgabe führen kann.

Beispiel:

<p><img src="https://cdn.acedata.cloud/ch7x3t.png" width="500" className="m-auto" /></p>

Nach Eingabe wird folgender Code generiert:

<p><img src="https://cdn.acedata.cloud/z1ud8l.png" width="500" className="m-auto" /></p>

Beispielcode:

```python
import requests

url = "https://api.acedata.cloud/sora/videos"

headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json"
}

payload = {
    "size": "large",
    "duration": 15,
    "orientation": "landscape",
    "prompt": "cat running on the river",
    "model": "sora-2",
    "image_urls": ["https://cdn.acedata.cloud/11wfp4.png"]
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

Nach Ausführung erhalten Sie sofort ein Ergebnis:

```
{
  "success": true,
  "task_id": "dd392ff0-dcb7-4c7a-afd0-9bd4f65c803a",
  "trace_id": "04fd151c-e942-4c1c-a6ab-9a1b1fe54172",
  "data": [
    {
      "id": "sora-2:task_01k777af4hfmg9g7yfvwsc6zws",
      "video_url": "https://filesystem.site/gptimage/vg-assets/assets%2Ftask_01k777af4hfmg9g7yfvwsc6zws%2Ftask_01k777af4hfmg9g7yfvwsc6zws_genid_92bae0c5-1703-4a5f-9d9f-c9ed2f9e7176_25_10_10_14_12_924695%2Fvideos%2F00000%2Fsrc.mp4?st=2025-10-10T12%3A37%3A32Z&se=2025-10-16T13%3A37%3A32Z&sks=b&skt=2025-10-10T12%3A37%3A32Z&ske=2025-10-16T13%3A37%3A32Z&sktid=a48cca56-e6da-484e-a814-9c849652bcb3&skoid=aa5ddad1-c91a-4f0a-9aca-e20682cc8969&skv=2019-02-02&sv=2018-11-09&sr=b&sp=r&spr=https%2Chttp&sig=5j4dibeaSsDmEka5c%2B9CKHZhRPdqfClQ0tIh03TWXsM%3D&az=oaivgprodscus",
      "state": "succeeded"
    }
  ]
}
```

Das Ergebnis ist eine Bild-zu-Video-Generierung, ähnlich wie oben beschrieben.

## Charakter-Video-Aufgabe (Version 1)

Für Charakter-Video-Aufgaben muss das Parameter `character_url` mit einem Video-Link für die Charaktererstellung übergeben werden. Wichtig: Im Video dürfen keine echten Personen erscheinen, sonst schlägt die Aufgabe fehl. Folgende Inhalte können angegeben werden:

- `character_url`: Video-Link für die Charaktererstellung, keine echten Personen im Video erlaubt.

Beispiel:

<p><img src="https://cdn.acedata.cloud/2nhdr2.png" width="500" className="m-auto" /></p>

Nach Eingabe wird folgender Code generiert:

<p><img src="https://cdn.acedata.cloud/xp8scl.png" width="500" className="m-auto" /></p>

Beispielcode:

```python
import requests

url = "https://api.acedata.cloud/sora/videos"

headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json"
}

payload = {
    "size": "small",
    "duration": 10,
    "orientation": "landscape",
    "prompt": "cat running on the river",
    "character_url": "https://cdn.acedata.cloud/pdidf5.mp4",
    "model": "sora-2",
    "character_end": 3,
    "character_start": 1
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

Nach Ausführung erhalten Sie sofort ein Ergebnis:

```
{
  "success": true,
  "task_id": "d9bf5461-29b5-47fd-be90-1fe9197df259",
  "trace_id": "b7992643-9207-40d6-956b-7577728acc67",
  "data": [
    {
      "id": "sora-2:task_01k8ykrztefavaypw6xanw305b",
      "video_url": "https://filesystem.site/cdn/20251101/bee4eeeb4c4660b46dac4548a1ffbc.mp4",
      "state": "succeeded"
    }
  ]
}
```

Das Ergebnis ist eine Charakter-Video-Generierung, ähnlich wie oben beschrieben.

## Version 2 Modus

Neben dem oben beschriebenen Version 1 Modus unterstützt die API auch den Version 2 Modus, der durch Setzen des Parameters `version` auf `2` aktiviert wird. Version 2 unterstützt kürzere Videolängen und pixelgenaue Auflösungskontrolle.

### Version 2 Parameterbeschreibung

| Parameter       | Typ     | Pflichtfeld | Beschreibung                                   |
|-----------------|---------|-------------|-----------------------------------------------|
| `version`       | integer | Ja          | Muss auf `2` gesetzt werden                    |
| `prompt`        | string  | Ja          | Eingabeaufforderung für die Videoerstellung   |
| `model`         | string  | Nein        | `sora-2` (Standard) oder `sora-2-pro`          |
| `duration`      | integer | Nein        | Videolänge: `4` (Standard), `8`, `12` Sekunden |
| `size`          | string  | Nein        | Auflösung: `720x1280` (Standard), `1280x720`, `1024x1792`, `1792x1024` |
| `image_urls`    | array   | Nein        | Array von Referenzbild-URLs, nur das erste Bild wird verwendet, Bildgröße muss mit `size` übereinstimmen |
| `callback_url`  | string  | Nein        | URL für asynchrone Rückrufe                    |

### Grundlegendes Beispiel

```shell
curl -X POST 'https://api.acedata.cloud/sora/videos' \
-H 'accept: application/json' \
-H 'authorization: Bearer {token}' \
-H 'content-type: application/json' \
-d '{
  "version": 2,
  "prompt": "cat running on the river",
  "model": "sora-2",
  "duration": 8,
  "size": "1280x720"
}'
```

Entsprechender Python-Code:

```python
import requests

url = "https://api.acedata.cloud/sora/videos"

headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json"
}

payload = {
    "version": 2,
    "prompt": "cat running on the river",
    "model": "sora-2",
    "seconds": 8,
    "size": "1280x720"
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

Entsprechender JavaScript-Code:

```javascript
const response = await fetch('https://api.acedata.cloud/sora/videos', {
  method: 'POST',
  headers: {
    'accept': 'application/json',
    'authorization': 'Bearer {token}',
    'content-type': 'application/json'
  },
  body: JSON.stringify({
    version: 2,
    prompt: 'cat running on the river',
    model: 'sora-2',
    seconds: 8,
    size: '1280x720'
  })
});
const data = await response.json();
console.log(data);
```

Das Rückgabeformat ist identisch zu Version 1:

```json
{
  "success": true,
  "task_id": "6bf7fb83-5814-4e3e-a4ad-bfa0c26c0b33",
  "trace_id": "96166698-4b66-478d-a26b-77a7269c9e01",
  "data": [
    {
      "id": "c0cc8dad-0954-421f-be8d-02eb063b3263",
      "video_url": "https://platform.cdn.acedata.cloud/sora/xxxxx.mp4",
      "state": "succeeded"
    }
  ]
}
```

### Verwendung von Referenzbildern (Version 2)

Im Version 2 Modus können Sie über den Parameter `image_urls` Referenzbilder zur Steuerung der Videoerstellung übergeben (nur das erste Bild wird verwendet):

```python
import requests

url = "https://api.acedata.cloud/sora/videos"

headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json"
}

payload = {
    "version": 2,
    "prompt": "a person walking through a beautiful garden",
    "model": "sora-2",
    "duration": 4,
    "size": "1280x720",
    "image_urls": ["https://cdn.acedata.cloud/11wfp4.png"]
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

> **Hinweis**: Die Größe des Referenzbildes sollte mit dem Parameter `size` übereinstimmen, z.B. bei `size` = `1280x720` sollte das Referenzbild 1280×720 Pixel groß sein.

### Parametervergleich Version 1 und Version 2

| Parameter       | Version 1            | Version 2                                      |
|-----------------|---------------------|-----------------------------------------------|
| `version`       | 1 (Standard)         | 2                                             |
| `prompt`        | ✅                   | ✅                                             |
| `model`         | ✅ sora-2 / sora-2-pro | ✅ sora-2 / sora-2-pro                         |
| `duration`      | ✅ 10/15/25 Sekunden | ✅ 4/8/12 Sekunden                             |
| `orientation`   | ✅ landscape/portrait | ❌                                             |
| `size`          | ✅ small/large       | ✅ 720x1280/1280x720/1024x1792/1792x1024      |
| `image_urls`    | ✅ mehrere Referenzbilder | ✅ nur das erste Bild                          |
| `character_url` | ✅                   | ❌                                             |
| `callback_url`  | ✅                   | ✅                                             |

## Asynchrone Rückrufe

Da die Videoerstellung mit der Sora Videos Generation API relativ lange dauert (ca. 1-2 Minuten), bleibt die HTTP-Verbindung bei langen Wartezeiten offen, was zu zusätzlichem Ressourcenverbrauch führen kann. Daher unterstützt die API auch asynchrone Rückrufe.

Der Ablauf ist folgender: Der Client gibt beim API-Aufruf zusätzlich das Feld `callback_url` an. Die API antwortet sofort mit einem Ergebnis, das die `task_id` enthält, welche die aktuelle Aufgabe identifiziert. Nach Abschluss der Aufgabe sendet die API das Ergebnis per POST-JSON an die angegebene `callback_url`, inklusive der `task_id`, sodass die Aufgabe eindeutig zugeordnet werden kann.

Im Folgenden ein Beispiel zur Veranschaulichung.

Ein Webhook-Callback ist ein HTTP-Service, der HTTP-Anfragen empfangen kann. Entwickler sollten hier die URL ihres eigenen HTTP-Servers angeben. Für Demonstrationszwecke verwenden wir die öffentliche Webhook-Seite https://webhook.site/, auf der Sie eine Webhook-URL erhalten, wie im Bild gezeigt:

![](https://cdn.acedata.cloud/cjjfly.png)

Kopieren Sie diese URL, z.B. `https://webhook.site/eb238c4f-da3b-47a5-a922-a93aa5405daa`, und verwenden Sie sie als Webhook.

Dann setzen Sie das Feld `callback_url` auf diese Webhook-URL und füllen die übrigen Parameter aus, wie im Bild gezeigt:

<p><img src="https://cdn.acedata.cloud/v1m05g.png" width="500" className="m-auto" /></p>

Nach Ausführung erhalten Sie sofort ein Ergebnis:

```
{
  "task_id": "b8976e18-32dc-4718-9ed8-1ea090fcb6ea"
}
```

Nach kurzer Zeit können Sie auf `https://webhook.site/eb238c4f-da3b-47a5-a922-a93aa5405daa` das Ergebnis der Videoerstellung beobachten, wie im Bild gezeigt:

![](https://cdn.acedata.cloud/0j7nra.png)

Der Inhalt sieht wie folgt aus:

```json
{
    "success": true,
    "task_id": "b8976e18-32dc-4718-9ed8-1ea090fcb6ea",
    "trace_id": "fb751e1e-4705-49ea-9fd4-5024b7865ea2",
    "data": [
        {
            "id": "sora-2:task_01k777hjrbfrgs2060q5zvf2a5",
            "video_url": "https://filesystem.site/gptimage/vg-assets/assets%2Ftask_01k777hjrbfrgs2060q5zvf2a5%2Ftask_01k777hjrbfrgs2060q5zvf2a5_genid_b8e2e5d1-a579-49ca-a21c-cb3869685cce_25_10_10_14_15_147334%2Fvideos%2F00000%2Fsrc.mp4?st=2025-10-10T12%3A38%3A49Z&se=2025-10-16T13%3A38%3A49Z&sks=b&skt=2025-10-10T12%3A38%3A49Z&ske=2025-10-16T13%3A38%3A49Z&sktid=a48cca56-e6da-484e-a814-9c849652bcb3&skoid=aa5ddad1-c91a-4f0a-9aca-e20682cc8969&skv=2019-02-02&sv=2018-11-09&sr=b&sp=r&spr=https%2Chttp&sig=p4aMqXqkP%2FI1IhOVGCB9JL8vUUvfNBBF12ESpKhKXOk%3D&az=oaivgprodscus",
            "state": "succeeded"
        }
    ]
}
```

Das Ergebnis enthält das Feld `task_id` sowie weitere Felder, wie oben beschrieben, womit die Aufgabe eindeutig zugeordnet werden kann.

## Fehlerbehandlung

Bei Fehlern gibt die API entsprechende Fehlercodes und -meldungen zurück, z.B.:

- `400 token_mismatched`: Ungültige Anfrage, möglicherweise fehlende oder ungültige Parameter.
- `400 api_not_implemented`: Ungültige Anfrage, möglicherweise fehlende oder ungültige Parameter.
- `401 invalid_token`: Nicht autorisiert, ungültiges oder fehlendes Token.
- `429 too_many_requests`: Zu viele Anfragen, Limit überschritten.
- `500 api_error`: Interner Serverfehler.

### Beispiel für Fehlerantwort

```json
{
  "success": false,
  "error": {
    "code": "api_error",
    "message": "fetch failed"
  },
  "trace_id": "2cf86e86-22a4-46e1-ac2f-032c0f2a4e89"
}
```

## Fazit

Mit diesem Dokument haben Sie gelernt, wie Sie die Sora Videos Generation API verwenden, um durch Eingabe von Prompts und Referenzbildern Videos zu generieren. Wir hoffen, dass Ihnen diese Anleitung bei der Integration und Nutzung der API hilft. Bei Fragen wenden Sie sich bitte jederzeit an unser technisches Support-Team.
