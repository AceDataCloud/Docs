---
title: "Face Change cURL Guide rapide"
---

## Introduction

Face Change est un service d'IA pour les images et le traitement des visages proposé par Ace Data Cloud. Grâce à l'API unifiée d'Ace Data Cloud, vous pouvez utiliser cURL pour appeler rapidement Face Change et réaliser des fonctions telles que le remplacement de visage, l'embellissement facial, le changement d'âge, le changement de genre, la transformation en style animé, l'analyse faciale, etc.

## Prérequis

- Posséder un compte [Ace Data Cloud](https://platform.acedata.cloud) et obtenir un Token API
- Avoir un environnement terminal avec cURL installé

## Utilisation de base

Le point d'accès principal pour appeler l'API Face Change est :

```
POST https://api.acedata.cloud/face/swap
```

Exemple complet de commande cURL :

```bash
curl -X POST https://api.acedata.cloud/face/swap \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"source_image_url": "https://example.com/source-face.jpg", "target_image_url": "https://example.com/target-face.jpg"}'
```

Veuillez remplacer `YOUR_API_TOKEN` par le Token réel obtenu sur la plateforme Ace Data Cloud.

## Exemple de réponse

Une fois l'appel réussi, les données au format JSON sont retournées, par exemple :

```json
{
  "status": "success",
  "data": {
    ...
  }
}
```

## Traitement de la réponse avec jq

Il est recommandé d'utiliser l'outil jq pour formater la sortie :

```bash
curl -s -X POST https://api.acedata.cloud/face/swap \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"source_image_url": "https://example.com/source-face.jpg", "target_image_url": "https://example.com/target-face.jpg"}' | jq .
```

## Gestion des erreurs

Codes d'erreur courants :

| Code statut | Description                          |
|-------------|-----------------------------------|
| 401         | Échec d'authentification, vérifiez le Token API |
| 403         | Solde insuffisant ou accès refusé  |
| 429         | Limite de fréquence des requêtes dépassée |
| 500         | Erreur interne du serveur          |

## Étapes suivantes

- Rendez-vous sur la [console Ace Data Cloud](https://platform.acedata.cloud) pour obtenir un Token API
- Consultez la [documentation API](https://docs.acedata.cloud/zh-CN/api-reference) pour connaître tous les paramètres
- Consultez les [plans tarifaires](https://platform.acedata.cloud/services) pour les informations sur les coûts
