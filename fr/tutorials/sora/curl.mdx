---
title: "Introduction rapide à Sora cURL"
---

## Présentation

Sora est un service vidéo IA proposé par Ace Data Cloud, utilisant le modèle de génération vidéo OpenAI Sora. Grâce à l’API unifiée d’Ace Data Cloud, vous pouvez appeler rapidement Sora avec cURL pour créer des vidéos à partir de texte, avec différentes durées, orientations (paysage/portrait), contrôle des personnages, images de référence, et plus encore.

## Prérequis

- Posséder un compte [Ace Data Cloud](https://platform.acedata.cloud) et obtenir un jeton API
- Avoir un environnement terminal avec cURL installé

## Utilisation de base

Le point d’accès principal pour appeler l’API Sora est :

```
POST https://api.acedata.cloud/sora/videos
```

Cet exemple utilise le modèle `sora-2`.  
Les modèles disponibles sont : `sora-2`, `sora-2-pro`.

Exemple complet de commande cURL :

```bash
curl -X POST https://api.acedata.cloud/sora/videos \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"prompt": "A cat running across a sunny meadow", "model": "sora-2", "duration": 10, "orientation": "landscape"}'
```

Remplacez `YOUR_API_TOKEN` par le jeton réel obtenu sur la plateforme Ace Data Cloud.

## Exemple de réponse

Une fois l’appel réussi, une réponse au format JSON est retournée, par exemple :

```json
{
  "status": "success",
  "data": {
    ...
  }
}
```

## Traitement de la réponse avec jq

Il est recommandé d’utiliser l’outil jq pour formater la sortie :

```bash
curl -s -X POST https://api.acedata.cloud/sora/videos \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"prompt": "A cat running across a sunny meadow", "model": "sora-2", "duration": 10, "orientation": "landscape"}' | jq .
```

## Gestion des erreurs

Codes d’erreur courants :

| Code statut | Description                         |
|-------------|-----------------------------------|
| 401         | Échec d’authentification, vérifiez le jeton API |
| 403         | Solde insuffisant ou accès refusé |
| 429         | Limite de fréquence dépassée      |
| 500         | Erreur interne du serveur          |

## Étapes suivantes

- Rendez-vous sur la [console Ace Data Cloud](https://platform.acedata.cloud) pour obtenir un jeton API  
- Consultez la [documentation API](https://docs.acedata.cloud/zh-CN/api-reference) pour la description complète des paramètres  
- Consultez les [plans tarifaires](https://platform.acedata.cloud/services) pour connaître les coûts
