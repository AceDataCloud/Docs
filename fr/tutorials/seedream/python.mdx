---
title: "Seedream Python Guide de démarrage rapide"
---

## Introduction

Seedream est un service d’images AI fourni par Ace Data Cloud, utilisant le modèle de génération d’images Seedream de ByteDance. Grâce à l’API unifiée d’Ace Data Cloud, vous pouvez intégrer rapidement Seedream en Python pour réaliser des fonctions telles que la génération d’images à partir de texte, l’édition d’images, la prise en charge de plusieurs résolutions, le contrôle du filigrane, etc.

## Prérequis

- Posséder un compte [Ace Data Cloud](https://platform.acedata.cloud) et obtenir un token API
- Environnement Python 3.7 ou supérieur
- Installer la bibliothèque requests : `pip install requests`

## Utilisation de base

Le point d’accès principal de l’API Seedream est :

```
POST https://api.acedata.cloud/seedream/images
```

Cet exemple utilise le modèle `doubao-seedream-4-0-250828`.  
Les modèles disponibles incluent : `doubao-seedream-4-5-251128`, `doubao-seedream-4-0-250828`, `doubao-seedream-3-0-t2i-250415`, `doubao-seededit-3-0-i2i-250628`.

Exemple complet de code Python :

```python
import requests

url = "https://api.acedata.cloud/seedream/images"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "prompt": "Un chat siamois blanc assis sur le rebord de la fenêtre",
    "model": "doubao-seedream-4-0-250828",
    "size": "1K"
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

Veuillez remplacer `YOUR_API_TOKEN` par le token réel obtenu sur la plateforme Ace Data Cloud.

## Gestion de la réponse

Après un appel réussi, l’API retourne des données au format JSON. Il est recommandé de vérifier le code HTTP :

```python
if response.status_code == 200:
    result = response.json()
    print("Appel réussi :", result)
else:
    print(f"Échec de l’appel, code d’état : {response.status_code}")
    print(response.text)
```

## Utilisation avancée

Pour les API supportant les tâches asynchrones, vous pouvez obtenir les résultats via une URL de rappel (callback) :

```python
data['callback_url'] = 'https://your-server.com/callback'
response = requests.post(url, headers=headers, json=data)
# Les résultats seront envoyés à votre serveur via l’URL de callback
```

## Gestion des erreurs

Codes d’erreur courants :

| Code | Description |
|-------|-------------|
| 401   | Échec d’authentification, veuillez vérifier le token API |
| 403   | Solde insuffisant ou accès non autorisé |
| 429   | Limite de fréquence des requêtes dépassée |
| 500   | Erreur interne du serveur |

## Autres points d’accès

Le service Seedream propose également les points d’accès suivants :

- `POST https://api.acedata.cloud/seedream/tasks`

## Étapes suivantes

- Rendez-vous sur la [console Ace Data Cloud](https://platform.acedata.cloud) pour obtenir un token API  
- Consultez la [documentation API](https://docs.acedata.cloud/zh-CN/api-reference) pour la description complète des paramètres  
- Consultez la [page des tarifs](https://platform.acedata.cloud/services) pour connaître les coûts
