---
title: "Seedream cURL Guide de démarrage rapide"
---

## Introduction

Seedream est un service d'images AI fourni par Ace Data Cloud, utilisant le modèle de génération d'images Seedream de ByteDance. Grâce à l'API unifiée d'Ace Data Cloud, vous pouvez rapidement appeler Seedream avec cURL pour réaliser des fonctionnalités telles que la génération d'images à partir de texte, l'édition d'images, différentes résolutions, contrôle des filigranes, etc.

## Prérequis

- Posséder un compte [Ace Data Cloud](https://platform.acedata.cloud) et obtenir un Token API
- Avoir un environnement terminal avec cURL installé

## Utilisation de base

Le point d'accès principal de l'API Seedream est :

```
POST https://api.acedata.cloud/seedream/images
```

Cet exemple utilise le modèle `doubao-seedream-4-0-250828`.  
Les modèles disponibles incluent : `doubao-seedream-4-5-251128`, `doubao-seedream-4-0-250828`, `doubao-seedream-3-0-t2i-250415`, `doubao-seededit-3-0-i2i-250628`.

Exemple complet de commande cURL :

```bash
curl -X POST https://api.acedata.cloud/seedream/images \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"prompt": "一只白色的暹罗猫坐在窗台上", "model": "doubao-seedream-4-0-250828", "size": "1K"}'
```

Veuillez remplacer `YOUR_API_TOKEN` par le Token réel obtenu sur la plateforme Ace Data Cloud.

## Exemple de réponse

Une fois l'appel réussi, une réponse au format JSON est retournée, par exemple :

```json
{
  "status": "success",
  "data": {
    ...
  }
}
```

## Traitement de la réponse avec jq

Il est recommandé d'utiliser l'outil jq pour formater la sortie :

```bash
curl -s -X POST https://api.acedata.cloud/seedream/images \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"prompt": "一只白色的暹罗猫坐在窗台上", "model": "doubao-seedream-4-0-250828", "size": "1K"}' | jq .
```

## Gestion des erreurs

Codes d'erreur courants :

| Code statut | Description                          |
|-------------|-----------------------------------|
| 401         | Échec d'authentification, vérifiez le Token API |
| 403         | Solde insuffisant ou accès refusé  |
| 429         | Limite de fréquence dépassée       |
| 500         | Erreur interne du serveur          |

## Étapes suivantes

- Rendez-vous sur la [console Ace Data Cloud](https://platform.acedata.cloud) pour obtenir un Token API  
- Consultez la [documentation API](https://docs.acedata.cloud/zh-CN/api-reference) pour une description complète des paramètres  
- Consultez les [plans tarifaires](https://platform.acedata.cloud/services) pour connaître les coûts
