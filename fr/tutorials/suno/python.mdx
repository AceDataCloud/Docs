---
title: "Introduction rapide à Suno Python"
---

## Introduction

Suno est un service audio AI fourni par Ace Data Cloud, générant de la musique AI avec Suno. Grâce à l’API unifiée d’Ace Data Cloud, vous pouvez intégrer rapidement Suno avec Python pour réaliser des fonctions telles que la génération de musique à partir de texte, les paroles personnalisées, la continuation de chansons, les reprises, la séparation vocale, la génération de paroles, etc.

## Prérequis

- Posséder un compte [Ace Data Cloud](https://platform.acedata.cloud) et obtenir un token API
- Environnement Python 3.7 ou supérieur
- Installer la bibliothèque requests : `pip install requests`

## Utilisation de base

Le point d’accès principal de l’API Suno est :

```
POST https://api.acedata.cloud/suno/audios
```

Cet exemple utilise le modèle `chirp-v5`.  
Les modèles disponibles incluent : `chirp-v5`, `v4-5-plus`, `v4-5`, `v4`, `v3-5`.

Exemple complet de code Python :

```python
import requests

url = "https://api.acedata.cloud/suno/audios"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "action": "generate",
    "prompt": "A cheerful pop song about summer vacation",
    "model": "chirp-v5",
    "custom": false
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

Veuillez remplacer `YOUR_API_TOKEN` par le token réel obtenu sur la plateforme Ace Data Cloud.

## Traitement de la réponse

Après un appel réussi, l’API retourne des données au format JSON. Il est recommandé de vérifier le code de statut HTTP :

```python
if response.status_code == 200:
    result = response.json()
    print("Appel réussi :", result)
else:
    print(f"Échec de l’appel, code de statut : {response.status_code}")
    print(response.text)
```

## Utilisation avancée

Pour les API supportant les tâches asynchrones, vous pouvez obtenir les résultats via une URL de rappel :

```python
data['callback_url'] = 'https://your-server.com/callback'
response = requests.post(url, headers=headers, json=data)
# Les résultats seront envoyés à votre serveur via l’URL de rappel
```

## Gestion des erreurs

Codes d’erreur courants :

| Code de statut | Description                          |
|----------------|------------------------------------|
| 401            | Échec d’authentification, vérifiez le token API |
| 403            | Solde insuffisant ou accès refusé  |
| 429            | Limite de fréquence dépassée       |
| 500            | Erreur interne du serveur          |

## Autres points d’accès

Le service Suno propose également les points d’accès suivants :

- `POST https://api.acedata.cloud/suno/lyrics`
- `POST https://api.acedata.cloud/suno/persona`
- `POST https://api.acedata.cloud/suno/wav`
- `POST https://api.acedata.cloud/suno/mp4`
- `POST https://api.acedata.cloud/suno/tasks`

## Étapes suivantes

- Rendez-vous sur la [console Ace Data Cloud](https://platform.acedata.cloud) pour obtenir un token API
- Consultez la [documentation API](https://docs.acedata.cloud/zh-CN/api-reference) pour une description complète des paramètres
- Consultez la [page des tarifs](https://platform.acedata.cloud/services) pour connaître les coûts associés
