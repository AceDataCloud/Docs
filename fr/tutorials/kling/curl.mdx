---
title: "Introduction rapide à Kling cURL"
---

## Introduction

Kling est un service vidéo IA proposé par Ace Data Cloud, un modèle de génération vidéo rapide. Grâce à l'API unifiée d'Ace Data Cloud, vous pouvez utiliser cURL pour appeler rapidement Kling et réaliser des fonctions telles que la génération vidéo à partir de texte, la génération vidéo à partir d'images, la continuation vidéo, avec différentes durées et différents formats.

## Prérequis

- Posséder un compte [Ace Data Cloud](https://platform.acedata.cloud) et obtenir un jeton API
- Avoir un environnement terminal avec cURL installé

## Utilisation de base

Le point d'entrée principal de l'API Kling est :

```
POST https://api.acedata.cloud/kling/videos
```

Cet exemple utilise le modèle `kling-v2-5-turbo`.  
Les modèles disponibles incluent : `kling-v1`, `kling-v1-6`, `kling-v2-master`, `kling-v2-1-master`, `kling-v2-5-turbo`, `kling-video-o1`.

Exemple complet de commande cURL :

```bash
curl -X POST https://api.acedata.cloud/kling/videos \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"action": "text2video", "prompt": "A white cat walking in the snow", "model": "kling-v2-5-turbo", "duration": 5, "aspect_ratio": "16:9"}'
```

Veuillez remplacer `YOUR_API_TOKEN` par le jeton réel obtenu sur la plateforme Ace Data Cloud.

## Exemple de réponse

Une fois l'appel réussi, une réponse au format JSON est retournée, par exemple :

```json
{
  "status": "success",
  "data": {
    ...
  }
}
```

## Traitement de la réponse avec jq

Il est recommandé d'utiliser l'outil jq pour formater la sortie :

```bash
curl -s -X POST https://api.acedata.cloud/kling/videos \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"action": "text2video", "prompt": "A white cat walking in the snow", "model": "kling-v2-5-turbo", "duration": 5, "aspect_ratio": "16:9"}' | jq .
```

## Gestion des erreurs

Codes d'erreur courants :

| Code | Description |
|--------|-------------|
| 401 | Échec d'authentification, veuillez vérifier le jeton API |
| 403 | Solde insuffisant ou accès non autorisé |
| 429 | Limite de fréquence des requêtes dépassée |
| 500 | Erreur interne du serveur |

## Étapes suivantes

- Rendez-vous sur la [console Ace Data Cloud](https://platform.acedata.cloud) pour obtenir un jeton API  
- Consultez la [documentation API](https://docs.acedata.cloud/zh-CN/api-reference) pour connaître la description complète des paramètres  
- Consultez la [page des tarifs](https://platform.acedata.cloud/services) pour connaître les coûts
