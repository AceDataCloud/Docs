---
title: "Démarrage rapide cURL pour Producer"
---

## Introduction

Producer est un service audio AI fourni par Ace Data Cloud, FUZZ Producer pour la génération de musique AI. Grâce à l'API unifiée d'Ace Data Cloud, vous pouvez utiliser cURL pour appeler rapidement Producer et réaliser des fonctions telles que la génération de musique à partir de texte, les paroles personnalisées, la continuation de chansons, les reprises, la séparation de voix, la génération de clips vidéo, etc.

## Prérequis

- Posséder un compte [Ace Data Cloud](https://platform.acedata.cloud) et obtenir un Token API
- Avoir un environnement terminal avec cURL installé

## Utilisation de base

Le point d'accès principal pour appeler l'API Producer est :

```
POST https://api.acedata.cloud/producer/audios
```

Cet exemple utilise le modèle `FUZZ-2.0 Pro`.  
Les modèles disponibles incluent : `FUZZ-2.0 Pro`, `FUZZ-2.0`, `FUZZ-1.1 Pro`, `FUZZ-1.0 Pro`.

Exemple complet de commande cURL :

```bash
curl -X POST https://api.acedata.cloud/producer/audios \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"action": "generate", "prompt": "A relaxing jazz piece for a coffee shop", "model": "FUZZ-2.0 Pro", "lyric": "[Verse]\nSoft piano keys in the morning light", "custom": true}'
```

Veuillez remplacer `YOUR_API_TOKEN` par le Token réel obtenu sur la plateforme Ace Data Cloud.

## Exemple de réponse

Une fois l'appel réussi, les données au format JSON sont retournées, par exemple :

```json
{
  "status": "success",
  "data": {
    ...
  }
}
```

## Traitement de la réponse avec jq

Il est recommandé d'utiliser l'outil jq pour formater la sortie :

```bash
curl -s -X POST https://api.acedata.cloud/producer/audios \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"action": "generate", "prompt": "A relaxing jazz piece for a coffee shop", "model": "FUZZ-2.0 Pro", "lyric": "[Verse]\nSoft piano keys in the morning light", "custom": true}' | jq .
```

## Gestion des erreurs

Codes d'erreur courants :

| Code statut | Description                         |
|-------------|-----------------------------------|
| 401         | Échec d'authentification, vérifiez le Token API |
| 403         | Solde insuffisant ou accès refusé |
| 429         | Limite de fréquence dépassée      |
| 500         | Erreur interne du serveur         |

## Étapes suivantes

- Rendez-vous sur la [console Ace Data Cloud](https://platform.acedata.cloud) pour obtenir un Token API
- Consultez la [documentation API](https://docs.acedata.cloud/zh-CN/api-reference) pour la description complète des paramètres
- Consultez les [plans tarifaires](https://platform.acedata.cloud/services) pour connaître les coûts
