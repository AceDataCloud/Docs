---
title: "Claude Messages Count Tokens API 申请及使用"
description: "Claude AI API guide - Ace Data Cloud"
---

Claude Messages Count Tokens API peut calculer le nombre de tokens d'une message d'entrée sans réellement créer le message, y compris le comptage des tokens pour les outils, images et documents. Cela est très utile pour estimer les coûts ou vérifier si l'entrée dépasse les limites de contexte du modèle.

Ce document présente principalement le processus d'utilisation de l'API Claude Messages Count Tokens.

## 申请流程

Pour utiliser l'API Claude Messages Count Tokens, commencez par obtenir votre token API sur le [tableau de bord Ace Data Cloud](https://platform.acedata.cloud/console/applications) pour référence.

![](https://cdn.acedata.cloud/dvc3cg.jpg)

Si vous n'êtes pas encore connecté ou inscrit, vous serez automatiquement redirigé vers la page de connexion pour vous inscrire et vous connecter, après quoi vous serez automatiquement renvoyé à la page actuelle.

**Un seul token API suffit pour accéder à tous les services de la plateforme, sans avoir besoin de demander un token séparé pour chaque service.** La première demande vous donnera un quota gratuit pour une expérience sans frais ; si le quota est insuffisant, vous pouvez recharger le solde général dans le [tableau de bord](https://platform.acedata.cloud/console/coin).

> 📘 Documentation complète : [Claude Messages Count Tokens API →](https://platform.acedata.cloud/documents/claude-messages-count-tokens)

## 基本使用

Le chemin de requête de l'API Claude Messages Count Tokens est `/v1/messages/count_tokens`, en accord avec l'API officielle d'Anthropic. Nous devons fournir au moins deux paramètres obligatoires :

- `model` : choisissez le modèle Claude à utiliser, comme `claude-opus-4-8`, `claude-sonnet-4-6`, etc.
- `messages` : tableau de messages d'entrée, chaque message contenant `role` (rôle) et `content` (contenu).

Paramètres optionnels courants :

- `system` : message système, qui sera compté dans le nombre de tokens.
- `tools` : définition des outils, qui sera comptée dans le nombre de tokens.
- `thinking` : configuration de réflexion étendue.
- `tool_choice` : configuration de choix d'outils.
- `cache_control` : configuration de contrôle de cache au niveau supérieur ou de bloc de contenu.

`messages`, `system`, les appels d'outils, les images URL et les blocs de contenu document/PDF utilisent la même structure de requête stable que l'API Messages.

### cURL 示例

```bash
curl -X POST 'https://api.acedata.cloud/v1/messages/count_tokens' \
  -H 'accept: application/json' \
  -H 'authorization: Bearer {token}' \
  -H 'content-type: application/json' \
  -d '{
    "model": "claude-opus-4-8",
    "messages": [
      {
        "role": "user",
        "content": "Hello, Claude"
      }
    ]
  }'
```

### Python 示例

```python
import httpx

url = "https://api.acedata.cloud/v1/messages/count_tokens"
headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json",
}
payload = {
    "model": "claude-opus-4-8",
    "messages": [
        {
            "role": "user",
            "content": "Hello, Claude"
        }
    ],
}
response = httpx.post(url, headers=headers, json=payload)
print(response.json())
```

返回结果示例：

```json
{
  "input_tokens": 11
}
```

### 使用 Anthropic SDK

L'API Claude Messages Count Tokens accepte la structure de requête stable du SDK Anthropic et peut être appelée via la bibliothèque `anthropic`. La valeur de retour actuelle est calculée par l'estimateur local d'Ace Data Cloud et n'est pas le résultat précis du tokenizer officiel d'Anthropic ; le nombre réel de tokens pour les multimédias, les outils et les différents modèles Claude peut varier.

```python
from anthropic import Anthropic

client = Anthropic(
    api_key="{token}",
    base_url="https://api.acedata.cloud",
)

result = client.messages.count_tokens(
    model="claude-opus-4-8",
    messages=[
        {
            "role": "user",
            "content": "Hello, Claude"
        }
    ],
)
print(result.input_tokens)
```

## 包含工具的 Token 计数

Si votre requête inclut des définitions d'outils, ces outils seront également comptés dans le nombre de tokens :

```python
result = client.messages.count_tokens(
    model="claude-opus-4-8",
    messages=[
        {
            "role": "user",
            "content": "What is the weather in San Francisco?"
        }
    ],
    tools=[
        {
            "name": "get_weather",
            "description": "Get the current weather in a given location",
            "input_schema": {
                "type": "object",
                "properties": {
                    "location": {
                        "type": "string",
                        "description": "The city and state, e.g. San Francisco, CA"
                    }
                },
                "required": ["location"]
            }
        }
    ],
)
print(result.input_tokens)
```

## 包含系统提示词的 Token 计数

Les messages système seront également comptés dans le nombre de tokens :

```python
result = client.messages.count_tokens(
    model="claude-opus-4-8",
    system="You are a helpful assistant that speaks Chinese.",
    messages=[
        {
            "role": "user",
            "content": "Hello"
        }
    ],
)
print(result.input_tokens)
```

## 注意事项

- Cette API ne calcule que le nombre de tokens d'entrée et ne produira aucune sortie du modèle.
- Les résultats du comptage des tokens peuvent être utilisés pour une estimation approximative de l'échelle d'entrée, mais ne doivent pas être considérés comme une base pour une facturation précise, un contrôle d'accès à la fenêtre de contexte ou une comparaison avec le tokenizer du modèle.
- L'estimateur actuel ne calcule pas précisément les tokens visuels/documents pour les images et les PDF, et ne peut pas refléter les différences de tokenizer officiel entre les différents modèles Claude.
- Cette API est entièrement gratuite et ne consomme aucun quota.
