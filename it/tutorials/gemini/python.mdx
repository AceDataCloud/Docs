---
title: "Gemini Python Guida Rapida"
---

## Introduzione

Gemini è un servizio di chat AI fornito da Ace Data Cloud, basato sulla serie di modelli linguistici di Google Gemini. Attraverso l'API unificata di Ace Data Cloud, puoi integrare rapidamente Gemini con Python, realizzando funzionalità come conversazioni multi-turno, prompt di sistema, output in streaming, modalità di output JSON e altro.

## Prerequisiti

- Avere un account su [Ace Data Cloud](https://platform.acedata.cloud) e ottenere un API Token
- Ambiente Python 3.7 o superiore
- Installare la libreria requests: `pip install requests`

## Uso Base

L'endpoint principale per chiamare l'API Gemini è:

```
POST https://api.acedata.cloud/gemini/chat/completions
```

In questo esempio si utilizza il modello `gemini-2.5-flash`.
I modelli disponibili includono: `gemini-2.5-flash`, `gemini-2.5-pro`, `gemini-2.0-flash`, `gemini-3.0-pro`.

Esempio completo di codice Python:

```python
import requests

url = "https://api.acedata.cloud/gemini/chat/completions"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "model": "gemini-2.5-flash",
    "messages": [
        {
            "role": "user",
            "content": "你好，请介绍一下你自己"
        }
    ],
    "max_tokens": 1024,
    "temperature": 0.7
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

Sostituisci `YOUR_API_TOKEN` con il token reale ottenuto dalla piattaforma Ace Data Cloud.

## Gestione della Risposta

Dopo una chiamata riuscita, l'API restituisce dati in formato JSON. Si consiglia di controllare il codice di stato HTTP:

```python
if response.status_code == 200:
    result = response.json()
    print("Chiamata riuscita:", result)
else:
    print(f"Chiamata fallita, codice di stato: {response.status_code}")
    print(response.text)
```

## Uso Avanzato

Per le API di chat è supportato l'output in streaming per ricevere risposte in tempo reale:

```python
import requests

data['stream'] = True
response = requests.post(url, headers=headers, json=data, stream=True)
for line in response.iter_lines():
    if line:
        print(line.decode())
```

## Gestione degli Errori

Codici di errore comuni:

| Codice | Descrizione |
|--------|-------------|
| 401    | Autenticazione fallita, controlla il API Token |
| 403    | Saldo insufficiente o accesso non autorizzato |
| 429    | Frequenza di richieste superata |
| 500    | Errore interno del server |

## Passi Successivi

- Vai alla [console Ace Data Cloud](https://platform.acedata.cloud) per ottenere il tuo API Token
- Consulta la [documentazione API](https://docs.acedata.cloud/zh-CN/api-reference) per una descrizione completa dei parametri
- Consulta i [piani tariffari](https://platform.acedata.cloud/services) per informazioni sui costi
