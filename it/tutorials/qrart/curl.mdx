---
title: "QR Art cURL Guida Rapida"
---

## Introduzione

QR Art è un servizio di immagini AI fornito da Ace Data Cloud, per la generazione di codici QR artistici con AI. Attraverso l'API unificata di Ace Data Cloud, puoi utilizzare cURL per chiamare rapidamente QR Art e realizzare funzionalità come codici QR per link, codici QR testuali, stili preimpostati, parametri personalizzati e altro.

## Prerequisiti

- Possedere un account [Ace Data Cloud](https://platform.acedata.cloud) e ottenere un API Token
- Avere un ambiente terminale con cURL installato

## Uso Base

L'endpoint principale per chiamare l'API QR Art è:

```
POST https://api.acedata.cloud/qrart/generate
```

Esempio completo di comando cURL:

```bash
curl -X POST https://api.acedata.cloud/qrart/generate \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"type": "link", "content": "https://platform.acedata.cloud", "prompt": "sunset ocean waves", "preset": "neon-mech"}'
```

Sostituisci `YOUR_API_TOKEN` con il token reale ottenuto dalla piattaforma Ace Data Cloud.

## Esempio di Risposta

Dopo una chiamata riuscita, viene restituito un dato in formato JSON, ad esempio:

```json
{
  "status": "success",
  "data": {
    ...
  }
}
```

## Utilizzo di jq per Gestire la Risposta

Si consiglia di utilizzare lo strumento jq per formattare l'output:

```bash
curl -s -X POST https://api.acedata.cloud/qrart/generate \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"type": "link", "content": "https://platform.acedata.cloud", "prompt": "sunset ocean waves", "preset": "neon-mech"}' | jq .
```

## Gestione degli Errori

Codici di errore comuni:

| Codice Stato | Descrizione                          |
|--------------|------------------------------------|
| 401          | Autenticazione fallita, controlla API Token |
| 403          | Saldo insufficiente o accesso negato |
| 429          | Limite di frequenza richieste superato |
| 500          | Errore interno del server          |

## Passi Successivi

- Vai al [Pannello di Controllo Ace Data Cloud](https://platform.acedata.cloud) per ottenere il tuo API Token
- Consulta la [Documentazione API](https://docs.acedata.cloud/zh-CN/api-reference) per dettagli completi sui parametri
- Consulta i [Piani Tariffari](https://platform.acedata.cloud/services) per informazioni sui costi
