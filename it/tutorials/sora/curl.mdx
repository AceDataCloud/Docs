---
title: "Guida rapida a Sora cURL"
---

## Introduzione

Sora è un servizio video AI fornito da Ace Data Cloud, basato sul modello di generazione video OpenAI Sora. Attraverso l'API unificata di Ace Data Cloud, puoi utilizzare cURL per chiamare rapidamente Sora e realizzare video da testo, con diverse durate, orientamenti orizzontali o verticali, controllo dei personaggi, immagini di riferimento e altre funzionalità.

## Prerequisiti

- Avere un account su [Ace Data Cloud](https://platform.acedata.cloud) e ottenere un API Token
- Ambiente terminale con cURL installato

## Uso base

L'endpoint principale per chiamare l'API Sora è:

```
POST https://api.acedata.cloud/sora/videos
```

In questo esempio si utilizza il modello `sora-2`.
I modelli disponibili includono: `sora-2`, `sora-2-pro`.

Esempio completo di comando cURL:

```bash
curl -X POST https://api.acedata.cloud/sora/videos \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"prompt": "A cat running across a sunny meadow", "model": "sora-2", "duration": 10, "orientation": "landscape"}'
```

Sostituisci `YOUR_API_TOKEN` con il token reale ottenuto dalla piattaforma Ace Data Cloud.

## Esempio di risposta

Alla chiamata riuscita viene restituito un dato in formato JSON, ad esempio:

```json
{
  "status": "success",
  "data": {
    ...
  }
}
```

## Utilizzo di jq per elaborare la risposta

Si consiglia di utilizzare lo strumento jq per formattare l'output:

```bash
curl -s -X POST https://api.acedata.cloud/sora/videos \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"prompt": "A cat running across a sunny meadow", "model": "sora-2", "duration": 10, "orientation": "landscape"}' | jq .
```

## Gestione degli errori

Codici di errore comuni:

| Codice stato | Descrizione                      |
|--------------|---------------------------------|
| 401          | Autenticazione fallita, verifica API Token |
| 403          | Saldo insufficiente o accesso negato       |
| 429          | Limite di frequenza richieste superato      |
| 500          | Errore interno del server                   |

## Passi successivi

- Vai al [console Ace Data Cloud](https://platform.acedata.cloud) per ottenere il tuo API Token
- Consulta la [documentazione API](https://docs.acedata.cloud/zh-CN/api-reference) per la descrizione completa dei parametri
- Consulta i [piani tariffari](https://platform.acedata.cloud/services) per informazioni sui costi
