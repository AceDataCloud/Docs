---
title: "Introduzione rapida a Kling cURL"
---

## Introduzione

Kling è un servizio video AI fornito da Ace Data Cloud, un modello di generazione video rapido e intelligente. Attraverso l'API unificata di Ace Data Cloud, puoi utilizzare cURL per chiamare rapidamente Kling e realizzare funzionalità come video da testo, video da immagini, continuazione video, varie durate e diversi rapporti d'aspetto.

## Prerequisiti

- Avere un account [Ace Data Cloud](https://platform.acedata.cloud) e ottenere un API Token
- Ambiente terminale con cURL installato

## Uso base

L'endpoint principale per chiamare l'API Kling è:

```
POST https://api.acedata.cloud/kling/videos
```

Questo esempio utilizza il modello `kling-v2-5-turbo`.
I modelli disponibili includono: `kling-v1`, `kling-v1-6`, `kling-v2-master`, `kling-v2-1-master`, `kling-v2-5-turbo`, `kling-video-o1`.

Esempio completo di comando cURL:

```bash
curl -X POST https://api.acedata.cloud/kling/videos \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"action": "text2video", "prompt": "A white cat walking in the snow", "model": "kling-v2-5-turbo", "duration": 5, "aspect_ratio": "16:9"}'
```

Sostituisci `YOUR_API_TOKEN` con il token reale ottenuto dalla piattaforma Ace Data Cloud.

## Esempio di risposta

In caso di chiamata riuscita, viene restituito un dato in formato JSON, ad esempio:

```json
{
  "status": "success",
  "data": {
    ...
  }
}
```

## Utilizzo di jq per elaborare la risposta

Si consiglia di utilizzare lo strumento jq per formattare l'output:

```bash
curl -s -X POST https://api.acedata.cloud/kling/videos \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"action": "text2video", "prompt": "A white cat walking in the snow", "model": "kling-v2-5-turbo", "duration": 5, "aspect_ratio": "16:9"}' | jq .
```

## Gestione degli errori

Codici di errore comuni:

| Codice stato | Descrizione                          |
|--------------|------------------------------------|
| 401          | Autenticazione fallita, controlla il API Token |
| 403          | Saldo insufficiente o accesso negato |
| 429          | Limite di frequenza richieste superato |
| 500          | Errore interno del server          |

## Passi successivi

- Vai al [console Ace Data Cloud](https://platform.acedata.cloud) per ottenere il tuo API Token
- Consulta la [documentazione API](https://docs.acedata.cloud/zh-CN/api-reference) per la descrizione completa dei parametri
- Consulta i [piani tariffari](https://platform.acedata.cloud/services) per informazioni sui costi
