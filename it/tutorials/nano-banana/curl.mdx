---
title: "Nano Banana cURL Guida Rapida"
---

## Introduzione

Nano Banana è un servizio di immagini AI fornito da Ace Data Cloud, basato sul servizio di generazione immagini Gemini. Attraverso l'API unificata di Ace Data Cloud, puoi utilizzare cURL per chiamare rapidamente Nano Banana e realizzare funzionalità come generazione di immagini da testo, modifica di immagini, vari rapporti d'aspetto e alta risoluzione.

## Prerequisiti

- Avere un account su [Ace Data Cloud](https://platform.acedata.cloud) e ottenere un API Token
- Ambiente terminale con cURL installato

## Uso Base

L'endpoint principale per chiamare l'API Nano Banana è:

```
POST https://api.acedata.cloud/nano-banana/images
```

Questo esempio utilizza il modello `nano-banana-2`.  
I modelli disponibili includono: `nano-banana`, `nano-banana-2`, `nano-banana-pro`.

Esempio completo di comando cURL:

```bash
curl -X POST https://api.acedata.cloud/nano-banana/images \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"action": "generate", "prompt": "A white siamese cat", "model": "nano-banana-2", "aspect_ratio": "1:1"}'
```

Sostituisci `YOUR_API_TOKEN` con il token reale ottenuto dalla piattaforma Ace Data Cloud.

## Esempio di Risposta

Una chiamata riuscita restituisce dati in formato JSON, ad esempio:

```json
{
  "status": "success",
  "data": {
    ...
  }
}
```

## Utilizzo di jq per elaborare la risposta

Si consiglia di usare lo strumento jq per formattare l'output:

```bash
curl -s -X POST https://api.acedata.cloud/nano-banana/images \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"action": "generate", "prompt": "A white siamese cat", "model": "nano-banana-2", "aspect_ratio": "1:1"}' | jq .
```

## Gestione degli Errori

Codici di errore comuni:

| Codice Stato | Descrizione                          |
|--------------|------------------------------------|
| 401          | Autenticazione fallita, controlla il API Token |
| 403          | Saldo insufficiente o accesso negato |
| 429          | Limite di frequenza richieste superato |
| 500          | Errore interno del server           |

## Passi Successivi

- Vai al [Pannello di Controllo Ace Data Cloud](https://platform.acedata.cloud) per ottenere il tuo API Token  
- Consulta la [Documentazione API](https://docs.acedata.cloud/zh-CN/api-reference) per una descrizione completa dei parametri  
- Consulta i [Piani Tariffari](https://platform.acedata.cloud/services) per informazioni sui costi
