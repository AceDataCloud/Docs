---
title: "Suno Python Guida Rapida"
---

## Introduzione

Suno è un servizio audio AI fornito da Ace Data Cloud, generazione musicale AI Suno. Attraverso l'API unificata di Ace Data Cloud, puoi integrare rapidamente Suno con Python per realizzare funzionalità come generazione di musica da testo, testi personalizzati, continuazione di canzoni, cover, separazione vocale, generazione di testi e altro.

## Prerequisiti

- Avere un account [Ace Data Cloud](https://platform.acedata.cloud) e ottenere un API Token
- Ambiente Python 3.7 o superiore
- Installare la libreria requests: `pip install requests`

## Uso Base

L'endpoint principale per chiamare l'API Suno è:

```
POST https://api.acedata.cloud/suno/audios
```

Questo esempio utilizza il modello `chirp-v5`.  
I modelli disponibili includono: `chirp-v5`, `v4-5-plus`, `v4-5`, `v4`, `v3-5`.

Esempio completo di codice Python:

```python
import requests

url = "https://api.acedata.cloud/suno/audios"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "action": "generate",
    "prompt": "A cheerful pop song about summer vacation",
    "model": "chirp-v5",
    "custom": false
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

Sostituisci `YOUR_API_TOKEN` con il Token reale ottenuto dalla piattaforma Ace Data Cloud.

## Gestione della Risposta

Dopo una chiamata riuscita, l'API restituisce dati in formato JSON. Si consiglia di controllare il codice di stato HTTP:

```python
if response.status_code == 200:
    result = response.json()
    print("Chiamata riuscita:", result)
else:
    print(f"Chiamata fallita, codice stato: {response.status_code}")
    print(response.text)
```

## Uso Avanzato

Per le API che supportano attività asincrone, è possibile ottenere i risultati tramite un URL di callback:

```python
data['callback_url'] = 'https://your-server.com/callback'
response = requests.post(url, headers=headers, json=data)
# I risultati saranno inviati al tuo server tramite l'URL di callback
```

## Gestione degli Errori

Codici di errore comuni:

| Codice Stato | Descrizione                         |
|--------------|-----------------------------------|
| 401          | Autenticazione fallita, controlla API Token |
| 403          | Saldo insufficiente o accesso negato |
| 429          | Limite di frequenza richieste superato |
| 500          | Errore interno del server         |

## Altri Endpoint

Il servizio Suno offre anche i seguenti endpoint:

- `POST https://api.acedata.cloud/suno/lyrics`
- `POST https://api.acedata.cloud/suno/persona`
- `POST https://api.acedata.cloud/suno/wav`
- `POST https://api.acedata.cloud/suno/mp4`
- `POST https://api.acedata.cloud/suno/tasks`

## Passi Successivi

- Vai alla [Console Ace Data Cloud](https://platform.acedata.cloud) per ottenere il tuo API Token
- Consulta la [Documentazione API](https://docs.acedata.cloud/zh-CN/api-reference) per i dettagli completi sui parametri
- Consulta i [Piani Tariffari](https://platform.acedata.cloud/services) per informazioni sui costi
