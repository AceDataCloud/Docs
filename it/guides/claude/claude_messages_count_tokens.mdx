---
title: "Richiesta e utilizzo dell'API Claude Messages Count Tokens"
description: "Claude AI API guide - Ace Data Cloud"
---

L'API Claude Messages Count Tokens può calcolare il numero di Token di input di un Message senza creare effettivamente un messaggio, incluso il conteggio dei Token di strumenti, immagini e documenti e altri contenuti. Questo è molto utile quando è necessario stimare i costi o verificare se l'input supera i limiti di contesto del modello.

Questo documento presenta principalmente il flusso di utilizzo dell'API Claude Messages Count Tokens.

## Processo di richiesta

Per utilizzare l'API Claude Messages Count Tokens, per prima cosa vai alla [console Ace Data Cloud](https://platform.acedata.cloud/console/applications) per ottenere il tuo API Token, da conservare come riserva.

![](https://cdn.acedata.cloud/dvc3cg.jpg)

Se non hai ancora effettuato l'accesso o la registrazione, verrai automaticamente reindirizzato alla pagina di accesso per invitarti a registrarti e accedere; al termine, tornerai automaticamente alla pagina corrente.

**Un solo API Token può chiamare tutti i servizi della piattaforma, senza bisogno di richiederne uno separatamente per ciascun servizio.** La prima richiesta offre credito gratuito, consentendo un'esperienza gratuita; quando il credito è insufficiente, puoi ricaricare il saldo generale nella [console](https://platform.acedata.cloud/console/coin).

> 📘 Documentazione completa: [Claude Messages Count Tokens API →](https://platform.acedata.cloud/documents/claude-messages-count-tokens)

## Utilizzo di base

Il percorso di richiesta dell'API Claude Messages Count Tokens è `/v1/messages/count_tokens`, coerente con l'API ufficiale di Anthropic. Dobbiamo fornire almeno due parametri obbligatori:

- `model`: seleziona il modello Claude da utilizzare, come l'ultimo modello di punta `claude-fable-5-1`; il precedente `claude-fable-5` rimane compatibile e disponibile.
- `messages`: l'array dei messaggi di input, ogni messaggio include `role` (ruolo) e `content` (contenuto).

Parametri opzionali comuni:

- `system`: prompt di sistema, verrà incluso nel numero di Token.
- `tools`: definizioni degli strumenti, verranno incluse nel numero di Token.
- `thinking`: configurazione del pensiero esteso.
- `tool_choice`: configurazione della selezione dello strumento.
- `cache_control`: configurazione del controllo della cache a livello superiore o di blocco di contenuto.

`messages`, `system`, il replay delle chiamate agli strumenti, le immagini URL e i blocchi di contenuto document/PDF utilizzano la stessa struttura di richiesta stabile dell'API Messages.

### Esempio cURL

```bash
curl -X POST 'https://api.acedata.cloud/v1/messages/count_tokens' \
  -H 'accept: application/json' \
  -H 'authorization: Bearer {token}' \
  -H 'content-type: application/json' \
  -d '{
    "model": "claude-fable-5-1",
    "messages": [
      {
        "role": "user",
        "content": "Hello, Claude"
      }
    ]
  }'
```

### Esempio Python

```python
import httpx

url = "https://api.acedata.cloud/v1/messages/count_tokens"
headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json",
}
payload = {
    "model": "claude-fable-5-1",
    "messages": [
        {
            "role": "user",
            "content": "Hello, Claude"
        }
    ],
}
response = httpx.post(url, headers=headers, json=payload)
print(response.json())
```

Esempio di risultato restituito:

```json
{
  "input_tokens": 11
}
```

### Utilizzo dell'SDK Anthropic

L'API Claude Messages Count Tokens accetta la struttura di richiesta stabile dell'SDK Anthropic e può essere chiamata tramite la libreria `anthropic`. L'interfaccia utilizza la capacità nativa Count Tokens corrispondente al modello selezionato per conteggiare l'input e può essere utilizzata per verificare la dimensione dell'input prima di inviare una richiesta Messages.

```python
from anthropic import Anthropic

client = Anthropic(
    api_key="{token}",
    base_url="https://api.acedata.cloud",
)

result = client.messages.count_tokens(
    model="claude-opus-4-8",
    messages=[
        {
            "role": "user",
            "content": "Hello, Claude"
        }
    ],
)
print(result.input_tokens)
```

## Conteggio dei Token con strumenti inclusi

Se la tua richiesta include definizioni di strumenti, anche questi strumenti verranno inclusi nel numero di Token:

```python
result = client.messages.count_tokens(
    model="claude-opus-4-8",
    messages=[
        {
            "role": "user",
            "content": "What is the weather in San Francisco?"
        }
    ],
    tools=[
        {
            "name": "get_weather",
            "description": "Get the current weather in a given location",
            "input_schema": {
                "type": "object",
                "properties": {
                    "location": {
                        "type": "string",
                        "description": "The city and state, e.g. San Francisco, CA"
                    }
                },
                "required": ["location"]
            }
        }
    ],
)
print(result.input_tokens)
```

## Conteggio dei Token con prompt di sistema incluso

Anche il prompt di sistema verrà incluso nel numero di Token:

```python
result = client.messages.count_tokens(
    model="claude-opus-4-8",
    system="You are a helpful assistant that speaks Chinese.",
    messages=[
        {
            "role": "user",
            "content": "Hello"
        }
    ],
)
print(result.input_tokens)
```

## Note

- Questa API calcola solo il numero di Token di input e non produrrà alcun output del modello.
- Il risultato del conteggio dei Token può essere utilizzato per stimare la dimensione dell'input e verificare la finestra di contesto; la fatturazione finale si basa su `usage` nella risposta Messages effettiva.
- Immagini, PDF, definizioni di strumenti, prompt di sistema e configurazione thinking verranno inclusi nel risultato secondo le regole di input del modello selezionato.
- Questa API è completamente gratuita e non consuma alcun credito.
