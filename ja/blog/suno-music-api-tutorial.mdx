---
title: "Suno 音楽生成 API 詳細チュートリアル"
---

## Suno について

Suno は現在最も強力な AI 音楽生成プラットフォームの一つです。Ace Data Cloud の Suno API を使えば、簡単な API 呼び出しで高品質な音楽を生成できます。

## API エンドポイント

| エンドポイント | 機能 |
|------|------|
| `POST /suno/audios` | 音楽生成（メインエンドポイント） |
| `POST /suno/lyrics` | 歌詞生成 |
| `POST /suno/persona` | 歌手スタイル管理 |
| `POST /suno/wav` | WAV 形式取得 |
| `POST /suno/mp4` | MP4 形式取得 |
| `POST /suno/tasks` | タスクステータス確認 |

## 基本生成

### テキスト記述による生成

```python
import requests

response = requests.post(
    "https://api.acedata.cloud/suno/audios",
    headers={
        "Authorization": "Bearer YOUR_API_TOKEN",
        "Content-Type": "application/json",
    },
    json={
        "action": "generate",
        "prompt": "A cheerful pop song about summer vacation",
        "model": "chirp-v5",
    },
)
print(response.json())
```

### カスタム歌詞生成

```python
response = requests.post(
    "https://api.acedata.cloud/suno/audios",
    headers={
        "Authorization": "Bearer YOUR_API_TOKEN",
        "Content-Type": "application/json",
    },
    json={
        "action": "generate",
        "model": "chirp-v5",
        "custom": True,
        "lyric": "[Verse]\n夏日的阳光洒满了海岸\n微风中传来浪花的声音\n\n[Chorus]\n让我们一起去远方\n追逐那自由的梦想",
        "style": "pop upbeat",
        "title": "夏日冒险",
    },
)
```

## 上級機能

### 曲の続き生成

```python
response = requests.post(
    "https://api.acedata.cloud/suno/audios",
    headers={
        "Authorization": "Bearer YOUR_API_TOKEN",
        "Content-Type": "application/json",
    },
    json={
        "action": "extend",
        "audio_id": "your-existing-audio-id",
        "model": "chirp-v5",
    },
)
```

### 歌詞生成

```python
response = requests.post(
    "https://api.acedata.cloud/suno/lyrics",
    headers={
        "Authorization": "Bearer YOUR_API_TOKEN",
        "Content-Type": "application/json",
    },
    json={
        "prompt": "写一首关于冬天的歌",
    },
)
```

## 利用可能なモデル

| モデル | 特徴 |
|------|------|
| chirp-v5 | 最新版、最高音質 |
| v4-5-plus | 強化版 |
| v4-5 | 安定版 |
| v4 | クラシック版 |

## まとめ

Suno API は Ace Data Cloud プラットフォームを通じて、完全な音楽生成機能を提供します。シンプルなテキスト生成からカスタム歌詞作成まで、数行のコードで実現可能です。

[platform.acedata.cloud](https://platform.acedata.cloud) で登録して体験してください。
