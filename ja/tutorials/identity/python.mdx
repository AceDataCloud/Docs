---
title: "Identity Python クイックスタート"
---

## はじめに

Identity は Ace Data Cloud が提供する認証サービスで、身分証・銀行カード・携帯電話番号の実名認証を行います。Ace Data Cloud の統一 API を通じて、Python で簡単に Identity を統合し、身分証 OCR、身分証照合、銀行カード照合、携帯電話番号照合などの機能を実現できます。

## 前提条件

- [Ace Data Cloud](https://platform.acedata.cloud) アカウントを所有し、API トークンを取得していること
- Python 3.7 以上の環境
- requests ライブラリのインストール：`pip install requests`

## 基本的な使い方

Identity API のメインエンドポイントは以下です：

```
POST https://api.acedata.cloud/identity/idcard/ocr
```

完全な Python コード例：

```python
import requests

url = "https://api.acedata.cloud/identity/idcard/ocr"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "image_url": "https://example.com/idcard-front.jpg"
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

`YOUR_API_TOKEN` は Ace Data Cloud プラットフォームで取得した実際のトークンに置き換えてください。

## レスポンス処理

正常に呼び出された場合、API は JSON 形式のデータを返します。HTTP ステータスコードを確認することを推奨します：

```python
if response.status_code == 200:
    result = response.json()
    print("呼び出し成功:", result)
else:
    print(f"呼び出し失敗、ステータスコード: {response.status_code}")
    print(response.text)
```

## 応用的な使い方

非同期タスクをサポートする API については、コールバック URL を通じて結果を取得できます：

```python
data['callback_url'] = 'https://your-server.com/callback'
response = requests.post(url, headers=headers, json=data)
# 結果はコールバック URL を通じてあなたのサーバーにプッシュされます
```

## エラー処理

よくあるエラーコード：

| ステータスコード | 説明 |
|------------------|------|
| 401 | 認証失敗、API トークンを確認してください |
| 403 | 残高不足またはアクセス権限なし |
| 429 | リクエスト頻度制限超過 |
| 500 | サーバー内部エラー |

## その他のエンドポイント

Identity サービスは以下のエンドポイントも提供しています：

- `POST https://api.acedata.cloud/identity/idcard/check-1e`
- `POST https://api.acedata.cloud/identity/bankcard/check-1e`
- `POST https://api.acedata.cloud/identity/phone/check-1e`

## 次のステップ

- [Ace Data Cloud コンソール](https://platform.acedata.cloud) にアクセスして API トークンを取得
- [API ドキュメント](https://docs.acedata.cloud/zh-CN/api-reference) で完全なパラメータ説明を確認
- [料金プラン](https://platform.acedata.cloud/services) を確認して費用を把握
