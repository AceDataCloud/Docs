---
title: "Быстрый старт с Identity cURL"
---

## Введение

Identity — это сервис аутентификации, предоставляемый Ace Data Cloud, для проверки подлинности удостоверений личности, банковских карт и номеров телефонов. С помощью унифицированного API Ace Data Cloud вы можете быстро использовать cURL для вызова Identity и реализовывать функции OCR удостоверений личности, проверки удостоверений личности, банковских карт и номеров телефонов.

## Предварительные требования

- Наличие аккаунта [Ace Data Cloud](https://platform.acedata.cloud) и получение API Token
- Терминальная среда с установленным cURL

## Основное использование

Основная конечная точка для вызова Identity API:

```
POST https://api.acedata.cloud/identity/idcard/ocr
```

Полный пример команды cURL:

```bash
curl -X POST https://api.acedata.cloud/identity/idcard/ocr \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"image_url": "https://example.com/idcard-front.jpg"}'
```

Пожалуйста, замените `YOUR_API_TOKEN` на фактический токен, полученный на платформе Ace Data Cloud.

## Пример ответа

При успешном вызове возвращаются данные в формате JSON, например:

```json
{
  "status": "success",
  "data": {
    ...
  }
}
```

## Обработка ответа с помощью jq

Рекомендуется использовать инструмент jq для форматирования вывода:

```bash
curl -s -X POST https://api.acedata.cloud/identity/idcard/ocr \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"image_url": "https://example.com/idcard-front.jpg"}' | jq .
```

## Обработка ошибок

Распространённые коды ошибок:

| Код состояния | Описание                            |
|---------------|-----------------------------------|
| 401           | Ошибка аутентификации, проверьте API Token |
| 403           | Недостаточно средств или нет доступа |
| 429           | Превышена частота запросов        |
| 500           | Внутренняя ошибка сервера         |

## Следующие шаги

- Перейдите в [консоль Ace Data Cloud](https://platform.acedata.cloud) для получения API Token
- Ознакомьтесь с [документацией API](https://docs.acedata.cloud/zh-CN/api-reference) для полного описания параметров
- Изучите [тарифные планы](https://platform.acedata.cloud/services) для информации о стоимости
