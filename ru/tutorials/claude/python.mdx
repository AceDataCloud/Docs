---
title: "Быстрый старт с Claude Python"
---

## Введение

Claude — это AI-сервис для общения, предоставляемый Ace Data Cloud, основанный на серии больших языковых моделей Anthropic Claude. С помощью унифицированного API Ace Data Cloud вы можете быстро интегрировать Claude с помощью Python для реализации многошагового диалога, системных подсказок, потокового вывода, режима вывода JSON и других функций.

## Предварительные требования

- Наличие аккаунта [Ace Data Cloud](https://platform.acedata.cloud) и получение API Token
- Среда Python 3.7 и выше
- Установка библиотеки requests: `pip install requests`

## Основное использование

Основной эндпоинт для вызова Claude API:

```
POST https://api.acedata.cloud/v1/chat/completions
```

В этом примере используется модель `claude-sonnet-4-6`.
Доступные модели включают: `claude-sonnet-4-6`, `claude-opus-4-5-20251101`, `claude-3-5-sonnet`.

Полный пример кода на Python:

```python
import requests

url = "https://api.acedata.cloud/v1/chat/completions"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "model": "claude-sonnet-4-6",
    "messages": [
        {
            "role": "user",
            "content": "你好，请介绍一下你自己"
        }
    ],
    "max_tokens": 1024,
    "temperature": 0.7
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

Замените `YOUR_API_TOKEN` на ваш реальный токен, полученный на платформе Ace Data Cloud.

## Обработка ответа

После успешного вызова API возвращает данные в формате JSON. Рекомендуется проверять HTTP статус код:

```python
if response.status_code == 200:
    result = response.json()
    print("Вызов успешен:", result)
else:
    print(f"Вызов не удался, статус код: {response.status_code}")
    print(response.text)
```

## Расширенное использование

Для API диалогов поддерживается потоковый вывод для получения ответов в реальном времени:

```python
import requests

data['stream'] = True
response = requests.post(url, headers=headers, json=data, stream=True)
for line in response.iter_lines():
    if line:
        print(line.decode())
```

## Обработка ошибок

Распространённые коды ошибок:

| Код статуса | Описание                          |
|-------------|----------------------------------|
| 401         | Ошибка аутентификации, проверьте API Token |
| 403         | Недостаточно средств или нет доступа |
| 429         | Превышен лимит частоты запросов  |
| 500         | Внутренняя ошибка сервера        |

## Другие эндпоинты

Сервис Claude также предоставляет следующие эндпоинты:

- `POST https://api.acedata.cloud/v1/messages`
- `POST https://api.acedata.cloud/v1/messages/count_tokens`

## Следующие шаги

- Перейдите в [Ace Data Cloud консоль](https://platform.acedata.cloud) для получения API Token
- Ознакомьтесь с [документацией API](https://docs.acedata.cloud/zh-CN/api-reference) для полного описания параметров
- Изучите [тарифные планы](https://platform.acedata.cloud/services) для информации о стоимости
