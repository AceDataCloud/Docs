---
title: "Быстрый старт с Gemini Python"
---

## Введение

Gemini — это AI-чат-сервис, предоставляемый Ace Data Cloud, основанный на серии больших языковых моделей Google Gemini. С помощью унифицированного API Ace Data Cloud вы можете быстро интегрировать Gemini с помощью Python для реализации многоходовых диалогов, системных подсказок, потокового вывода, режима вывода JSON и других функций.

## Предварительные требования

- Аккаунт [Ace Data Cloud](https://platform.acedata.cloud) и получение API Token
- Среда Python 3.7 и выше
- Установка библиотеки requests: `pip install requests`

## Базовое использование

Основной эндпоинт для вызова API Gemini:

```
POST https://api.acedata.cloud/gemini/chat/completions
```

В этом примере используется модель `gemini-2.5-flash`.  
Доступные модели: `gemini-2.5-flash`, `gemini-2.5-pro`, `gemini-2.0-flash`, `gemini-3.0-pro`.

Полный пример кода на Python:

```python
import requests

url = "https://api.acedata.cloud/gemini/chat/completions"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "model": "gemini-2.5-flash",
    "messages": [
        {
            "role": "user",
            "content": "你好，请介绍一下你自己"
        }
    ],
    "max_tokens": 1024,
    "temperature": 0.7
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

Замените `YOUR_API_TOKEN` на ваш реальный токен, полученный на платформе Ace Data Cloud.

## Обработка ответа

При успешном вызове API возвращает данные в формате JSON. Рекомендуется проверять HTTP статус-код:

```python
if response.status_code == 200:
    result = response.json()
    print("Вызов успешен:", result)
else:
    print(f"Вызов не удался, статус-код: {response.status_code}")
    print(response.text)
```

## Продвинутое использование

Для API чата поддерживается потоковый вывод для получения ответов в реальном времени:

```python
import requests

data['stream'] = True
response = requests.post(url, headers=headers, json=data, stream=True)
for line in response.iter_lines():
    if line:
        print(line.decode())
```

## Обработка ошибок

Распространённые коды ошибок:

| Код состояния | Описание                         |
|---------------|---------------------------------|
| 401           | Ошибка аутентификации, проверьте API Token |
| 403           | Недостаточно средств или нет доступа |
| 429           | Превышен лимит частоты запросов |
| 500           | Внутренняя ошибка сервера       |

## Следующие шаги

- Перейдите в [консоль Ace Data Cloud](https://platform.acedata.cloud) для получения API Token  
- Ознакомьтесь с [документацией API](https://docs.acedata.cloud/zh-CN/api-reference) для полного описания параметров  
- Изучите [тарифные планы](https://platform.acedata.cloud/services) для информации о стоимости
