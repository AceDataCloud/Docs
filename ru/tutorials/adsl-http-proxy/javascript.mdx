---
title: "ADSL HTTP Proxy JavaScript Быстрый старт"
---

## Введение

ADSL HTTP Proxy — это сервис сетевого прокси, предоставляемый Ace Data Cloud, для извлечения динамических IP-адресов ADSL. С помощью единого API Ace Data Cloud вы можете быстро интегрировать ADSL HTTP Proxy на JavaScript и реализовать функции HTTP/SOCKS прокси, мультиоператорность, мультиобласть, настраиваемую длительность, белый список IP и другие возможности.

## Предварительные требования

- Наличие аккаунта [Ace Data Cloud](https://platform.acedata.cloud) и получение API Token
- Node.js 18+ или современная среда браузера

## Базовое использование

Основной endpoint для вызова API ADSL HTTP Proxy:

```
POST https://api.acedata.cloud/adsl/extract
```

Полный пример кода на JavaScript:

```javascript
const response = await fetch("https://api.acedata.cloud/adsl/extract", {
  method: "POST",
  headers: {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
  "number": 5,
  "type": "http",
  "duration": 300,
  "province": "广东",
  "isp": "电信"
}),
});

const result = await response.json();
console.log(result);
```

Замените `YOUR_API_TOKEN` на реальный токен, полученный на платформе Ace Data Cloud.

## Обработка ответа

Рекомендуется проверять статус ответа и обрабатывать ошибки:

```javascript
if (response.ok) {
  const result = await response.json();
  console.log("Вызов успешен:", result);
} else {
  console.error(`Вызов не удался, статус: ${response.status}`);
  const error = await response.text();
  console.error(error);
}
```

## Обертка для Node.js

Рекомендуется оформить в виде переиспользуемой функции:

```javascript
async function callADSLHTTPProxy(data) {
  const response = await fetch("https://api.acedata.cloud/adsl/extract", {
    method: "POST",
    headers: {
      "Authorization": `Bearer ${process.env.ACE_API_TOKEN}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify(data),
  });
  if (!response.ok) throw new Error(`API error: ${response.status}`);
  return response.json();
}
```

## Обработка ошибок

Распространённые коды ошибок:

| Код статуса | Описание                          |
|-------------|---------------------------------|
| 401         | Ошибка аутентификации, проверьте API Token |
| 403         | Недостаточно средств или нет доступа |
| 429         | Превышена частота запросов       |
| 500         | Внутренняя ошибка сервера        |

## Следующие шаги

- Перейдите в [Ace Data Cloud консоль](https://platform.acedata.cloud) для получения API Token
- Ознакомьтесь с [документацией API](https://docs.acedata.cloud/zh-CN/api-reference) для полного описания параметров
- Изучите [тарифные планы](https://platform.acedata.cloud/services) для информации о стоимости
