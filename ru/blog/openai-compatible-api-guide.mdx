---
title: "Полное руководство по совместимому с OpenAI API"
---

## Что такое совместимый с OpenAI интерфейс

Формат Chat Completions API от OpenAI стал фактическим отраслевым стандартом. Многие крупные языковые модели предоставляют интерфейсы, совместимые с OpenAI, что позволяет разработчикам использовать один и тот же код для подключения к разным моделям.

Ace Data Cloud предоставляет совместимый с OpenAI интерфейс Chat Completions для моделей Claude, Gemini, DeepSeek, Grok, Kimi и других — достаточно изменить базовый URL и название модели для переключения.

## Поддерживаемые модели

| Сервис | Эндпоинт | Пример модели |
|--------|----------|--------------|
| Claude | `POST /v1/chat/completions` | claude-sonnet-4-6 |
| OpenAI | `POST /openai/chat/completions` | gpt-4o |
| Gemini | `POST /gemini/chat/completions` | gemini-2.5-flash |
| DeepSeek | `POST /deepseek/chat/completions` | deepseek-r1 |
| Grok | `POST /grok/chat/completions` | grok-3 |
| Kimi | `POST /kimi/chat/completions` | kimi-k2.5 |

## Унифицированный способ вызова

Все чат-модели используют одинаковый формат запроса:

```python
import requests

def chat(endpoint, model, message):
    return requests.post(
        f"https://api.acedata.cloud{endpoint}",
        headers={
            "Authorization": "Bearer YOUR_API_TOKEN",
            "Content-Type": "application/json",
        },
        json={
            "model": model,
            "messages": [{"role": "user", "content": message}],
            "max_tokens": 1024,
            "temperature": 0.7,
        },
    ).json()

# Вызов разных моделей — достаточно изменить эндпоинт и имя модели
claude = chat("/v1/chat/completions", "claude-sonnet-4-6", "你好")
gpt = chat("/openai/chat/completions", "gpt-4o", "你好")
gemini = chat("/gemini/chat/completions", "gemini-2.5-flash", "你好")
deepseek = chat("/deepseek/chat/completions", "deepseek-r1", "你好")
```

## Потоковый вывод

Все чат API поддерживают потоковый вывод (Server-Sent Events):

```python
data = {
    "model": "claude-sonnet-4-6",
    "messages": [{"role": "user", "content": "写一首诗"}],
    "stream": True,
}

response = requests.post(
    "https://api.acedata.cloud/v1/chat/completions",
    headers={"Authorization": "Bearer YOUR_API_TOKEN"},
    json=data,
    stream=True,
)

for line in response.iter_lines():
    if line:
        print(line.decode())
```

## Совместимость с OpenAI SDK

Благодаря совместимому формату интерфейса, вы можете напрямую использовать официальный SDK OpenAI, просто изменив base_url:

```python
from openai import OpenAI

client = OpenAI(
    api_key="YOUR_API_TOKEN",
    base_url="https://api.acedata.cloud/v1",  # Claude
    # base_url="https://api.acedata.cloud/openai",  # OpenAI
    # base_url="https://api.acedata.cloud/gemini",  # Gemini
)

response = client.chat.completions.create(
    model="claude-sonnet-4-6",
    messages=[{"role": "user", "content": "你好"}],
)
print(response.choices[0].message.content)
```

## Итоги

С помощью совместимого с OpenAI интерфейса Ace Data Cloud вы можете:

- Подключаться к более чем 6 крупным языковым моделям с одним набором кода
- Гибко переключать модели для сравнения качества и стоимости
- Использовать официальный SDK OpenAI без затрат на миграцию
- Единая система биллинга и мониторинга

Перейдите на [platform.acedata.cloud](https://platform.acedata.cloud) для бесплатного тестирования.
