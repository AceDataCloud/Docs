---
title: "Запрос и использование API Claude Messages Count Tokens"
description: "Claude AI API guide - Ace Data Cloud"
---

Claude Messages Count Tokens API позволяет вычислить количество входных Token для одного Message без фактического создания сообщения, включая подсчёт Token для таких содержимых, как инструменты, изображения и документы. Это очень полезно, когда необходимо оценить стоимость или проверить, не превышает ли ввод ограничение контекста модели.

В этом документе в основном описывается процесс использования Claude Messages Count Tokens API.

## Процесс запроса

Чтобы использовать Claude Messages Count Tokens API, сначала получите ваш API Token в [консоли Ace Data Cloud](https://platform.acedata.cloud/console/applications) и сохраните его для дальнейшего использования.

![](https://cdn.acedata.cloud/dvc3cg.jpg)

Если вы ещё не вошли в систему или не зарегистрировались, вы будете автоматически перенаправлены на страницу входа, где вам будет предложено зарегистрироваться и войти, после завершения вы автоматически вернётесь на текущую страницу.

**Один API Token позволяет вызывать все сервисы платформы, без необходимости подавать отдельную заявку для каждого сервиса.** При первом запросе предоставляется бесплатный лимит, позволяющий бесплатно протестировать сервис; при недостаточном лимите можно пополнить общий баланс в [консоли](https://platform.acedata.cloud/console/coin).

> 📘 Полная документация: [Claude Messages Count Tokens API →](https://platform.acedata.cloud/documents/claude-messages-count-tokens)

## Базовое использование

Путь запроса Claude Messages Count Tokens API — `/v1/messages/count_tokens`, он соответствует официальному API Anthropic. Необходимо предоставить как минимум два обязательных параметра:

- `model`: выберите используемую модель Claude; `claude-opus-5-5` можно использовать для Messages Token Count, например для новейшей флагманской модели `claude-fable-5-1`; исходная `claude-fable-5` по-прежнему сохранена для совместимости.
- `messages`: массив входных сообщений, каждое сообщение содержит `role` (роль) и `content` (содержимое).

Часто используемые необязательные параметры:

- `system`: системный промпт, учитывается в количестве Token.
- `tools`: определения инструментов, учитываются в количестве Token.
- `thinking`: конфигурация расширенного мышления.
- `tool_choice`: конфигурация выбора инструмента.
- `cache_control`: конфигурация управления кешем верхнего уровня или на уровне блока содержимого.

`messages`, `system`, воспроизведение вызовов инструментов, URL-изображения и блоки содержимого document/PDF используют ту же стабильную структуру запросов, что и Messages API.

### Пример cURL

```bash
curl -X POST 'https://api.acedata.cloud/v1/messages/count_tokens' \
  -H 'accept: application/json' \
  -H 'authorization: Bearer {token}' \
  -H 'content-type: application/json' \
  -d '{
    "model": "claude-fable-5-1",
    "messages": [
      {
        "role": "user",
        "content": "Hello, Claude"
      }
    ]
  }'
```

### Пример Python

```python
import httpx

url = "https://api.acedata.cloud/v1/messages/count_tokens"
headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json",
}
payload = {
    "model": "claude-fable-5-1",
    "messages": [
        {
            "role": "user",
            "content": "Hello, Claude"
        }
    ],
}
response = httpx.post(url, headers=headers, json=payload)
print(response.json())
```

Пример возвращаемого результата:

```json
{
  "input_tokens": 11
}
```

### Использование Anthropic SDK

Claude Messages Count Tokens API принимает стабильную структуру запросов Anthropic SDK и может вызываться через библиотеку `anthropic`. Интерфейс подсчитывает ввод, используя нативную возможность Count Tokens соответствующей выбранной модели, и может использоваться для проверки размера ввода перед отправкой запроса Messages.

```python
from anthropic import Anthropic

client = Anthropic(
    api_key="{token}",
    base_url="https://api.acedata.cloud",
)

result = client.messages.count_tokens(
    model="claude-opus-4-8",
    messages=[
        {
            "role": "user",
            "content": "Hello, Claude"
        }
    ],
)
print(result.input_tokens)
```

## Подсчёт Token с инструментами

Если ваш запрос содержит определения инструментов, эти инструменты также будут учтены в количестве Token:

```python
result = client.messages.count_tokens(
    model="claude-opus-4-8",
    messages=[
        {
            "role": "user",
            "content": "What is the weather in San Francisco?"
        }
    ],
    tools=[
        {
            "name": "get_weather",
            "description": "Get the current weather in a given location",
            "input_schema": {
                "type": "object",
                "properties": {
                    "location": {
                        "type": "string",
                        "description": "The city and state, e.g. San Francisco, CA"
                    }
                },
                "required": ["location"]
            }
        }
    ],
)
print(result.input_tokens)
```

## Подсчёт Token с системным промптом

Системный промпт также будет учтён в количестве Token:

```python
result = client.messages.count_tokens(
    model="claude-opus-4-8",
    system="You are a helpful assistant that speaks Chinese.",
    messages=[
        {
            "role": "user",
            "content": "Hello"
        }
    ],
)
print(result.input_tokens)
```

## Примечания

- Этот API вычисляет только количество входных Token и не генерирует никакого вывода модели.
- Результат подсчёта Token можно использовать для оценки размера ввода и проверки окна контекста; окончательная тарификация определяется по `usage` в фактическом ответе Messages.
- Изображения, PDF, определения инструментов, системные промпты и конфигурация thinking будут учтены в результате в соответствии с правилами ввода выбранной модели.
- Этот API полностью бесплатен и не расходует никакие лимиты.
