---
title: "Qwen Image 生成与编辑 API"
description: "Qwen Image 3 整合指南 - Ace Data Cloud"
---

`POST https://api.acedata.cloud/qwen-image/images` 透過同一接口完成文生圖和 1–3 張參考圖編輯，支持 `qwen-image-3.0` 與 `qwen-image-3.0-pro`。

## 選擇模型

| 模型                   | 適合場景       | 官方標準價                   |
| -------------------- | ---------- | ----------------------- |
| `qwen-image-3.0`     | 批量創作、快速迭代  | 輸出 $0.030/張             |
| `qwen-image-3.0-pro` | 複雜排版、高精度成片 | 1K $0.040/張；2K $0.075/張 |

參考圖按真實輸入數量計費，官方標準價 $0.003/張。

## 同步生成

```bash
curl -X POST 'https://api.acedata.cloud/qwen-image/images' \
  -H 'Authorization: Bearer YOUR_API_KEY' \
  -H 'Content-Type: application/json' \
  -d '{
    "model": "qwen-image-3.0",
    "prompt": "A simple red circle centered on a clean white background.",
    "size": "1024*1024",
    "n": 1,
    "watermark": false
  }'
```

成功響應包含永久 CDN 圖片地址、真實 usage 和本次費用：

```json
{
  "success": true,
  "task_id": "565a7f55-72c6-43ed-b274-268ff046e5b4",
  "trace_id": "bb923cec-1550-43b4-8df2-288ac0977b4b",
  "data": [
    {"image_url": "https://platform2.cdn.acedata.cloud/qwen-image/40d5c76b-2f84-4b04-9ea0-b706417ae622.png"}
  ],
  "usage": {
    "input_image_count": 0,
    "output_image_count": 1,
    "output_image_type": "qima_output_1k",
    "output_width": 1024,
    "output_height": 1024
  },
  "cost": {"amount": 0.322, "currency": "credit", "list_amount": 0.35}
}
```

## 參考圖編輯

增加 `image_urls`，支持 1–3 張公開可訪問圖片：

```json
{
  "model": "qwen-image-3.0-pro",
  "prompt": "保留主體，將背景改成暖**###**海報風格",
  "image_urls": [
    "https://platform2.cdn.acedata.cloud/qwen-image/40d5c76b-2f84-4b04-9ea0-b706417ae622.png"
  ],
  "size": "2048*2048",
  "n": 1,
  "watermark": false
}
```

`prompt_extend_mode=agent` 僅用於文生圖。`n` 範圍為 1–6；寬高比須在 1:8 至 8:1 之間。

## 異步任務

設置 `async: true` 後接口立即返回 `task_id`。使用 `/qwen-image/tasks` 查詢終態，或傳入 `callback_url` 接收完成通知。生成只結算一次，任務查詢不重複計費。
