---
title: "Kompletny przewodnik po API kompatybilnym z OpenAI"
---

## Czym jest interfejs kompatybilny z OpenAI

Format API Chat Completions od OpenAI stał się faktycznym standardem branżowym. Wiele dużych modeli językowych oferuje interfejsy kompatybilne z OpenAI, co pozwala deweloperom korzystać z różnych modeli za pomocą tego samego kodu.

Ace Data Cloud udostępnia interfejsy Chat Completions kompatybilne z OpenAI dla modeli takich jak Claude, Gemini, DeepSeek, Grok, Kimi i innych — wystarczy zmienić Base URL i nazwę modelu, aby przełączyć się między nimi.

## Obsługiwane modele

| Usługa | Endpoint | Przykładowy model |
|--------|----------|-------------------|
| Claude | `POST /v1/chat/completions` | claude-sonnet-4-6 |
| OpenAI | `POST /openai/chat/completions` | gpt-4o |
| Gemini | `POST /gemini/chat/completions` | gemini-2.5-flash |
| DeepSeek | `POST /deepseek/chat/completions` | deepseek-r1 |
| Grok | `POST /grok/chat/completions` | grok-3 |
| Kimi | `POST /kimi/chat/completions` | kimi-k2.5 |

## Ujednolicony sposób wywołania

Wszystkie modele czatu używają tego samego formatu żądania:

```python
import requests

def chat(endpoint, model, message):
    return requests.post(
        f"https://api.acedata.cloud{endpoint}",
        headers={
            "Authorization": "Bearer YOUR_API_TOKEN",
            "Content-Type": "application/json",
        },
        json={
            "model": model,
            "messages": [{"role": "user", "content": message}],
            "max_tokens": 1024,
            "temperature": 0.7,
        },
    ).json()

# Wywołanie różnych modeli — wystarczy zmienić endpoint i nazwę modelu
claude = chat("/v1/chat/completions", "claude-sonnet-4-6", "你好")
gpt = chat("/openai/chat/completions", "gpt-4o", "你好")
gemini = chat("/gemini/chat/completions", "gemini-2.5-flash", "你好")
deepseek = chat("/deepseek/chat/completions", "deepseek-r1", "你好")
```

## Strumieniowe wyjście

Wszystkie API czatu obsługują strumieniowe wyjście (Server-Sent Events):

```python
data = {
    "model": "claude-sonnet-4-6",
    "messages": [{"role": "user", "content": "写一首诗"}],
    "stream": True,
}

response = requests.post(
    "https://api.acedata.cloud/v1/chat/completions",
    headers={"Authorization": "Bearer YOUR_API_TOKEN"},
    json=data,
    stream=True,
)

for line in response.iter_lines():
    if line:
        print(line.decode())
```

## Kompatybilność z OpenAI SDK

Dzięki kompatybilnemu formatowi interfejsu możesz bezpośrednio korzystać z oficjalnego SDK OpenAI, wystarczy zmienić base_url:

```python
from openai import OpenAI

client = OpenAI(
    api_key="YOUR_API_TOKEN",
    base_url="https://api.acedata.cloud/v1",  # Claude
    # base_url="https://api.acedata.cloud/openai",  # OpenAI
    # base_url="https://api.acedata.cloud/gemini",  # Gemini
)

response = client.chat.completions.create(
    model="claude-sonnet-4-6",
    messages=[{"role": "user", "content": "你好"}],
)
print(response.choices[0].message.content)
```

## Podsumowanie

Dzięki interfejsowi kompatybilnemu z OpenAI od Ace Data Cloud możesz:

- Korzystać z ponad 6 dużych modeli językowych za pomocą jednego zestawu kodu
- Elastycznie przełączać modele, porównując efekty i koszty
- Bezpośrednio używać OpenAI SDK bez konieczności migracji
- Mieć ujednolicony system rozliczeń i monitoringu

Wypróbuj za darmo na [platform.acedata.cloud](https://platform.acedata.cloud).
