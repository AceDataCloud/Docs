---
title: "Sora Python szybki start"
---

## Wprowadzenie

Sora to usługa AI wideo oferowana przez Ace Data Cloud, model generowania wideo OpenAI Sora. Dzięki zunifikowanemu API Ace Data Cloud możesz szybko zintegrować Sora za pomocą Pythona, realizując funkcje takie jak generowanie wideo z tekstu, różne długości, orientacja pozioma i pionowa, kontrola postaci, obrazy referencyjne i inne.

## Wymagania wstępne

- Konto w [Ace Data Cloud](https://platform.acedata.cloud) oraz uzyskany token API
- Środowisko Python 3.7 lub nowsze
- Zainstalowana biblioteka requests: `pip install requests`

## Podstawowe użycie

Główny endpoint API Sora to:

```
POST https://api.acedata.cloud/sora/videos
```

W tym przykładzie używamy modelu `sora-2`. Dostępne modele to: `sora-2`, `sora-2-pro`.

Pełny przykład kodu w Python:

```python
import requests

url = "https://api.acedata.cloud/sora/videos"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "prompt": "A cat running across a sunny meadow",
    "model": "sora-2",
    "duration": 10,
    "orientation": "landscape"
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

Zamień `YOUR_API_TOKEN` na rzeczywisty token uzyskany na platformie Ace Data Cloud.

## Obsługa odpowiedzi

Po pomyślnym wywołaniu API zwraca dane w formacie JSON. Zaleca się sprawdzenie kodu statusu HTTP:

```python
if response.status_code == 200:
    result = response.json()
    print("Wywołanie zakończone sukcesem:", result)
else:
    print(f"Wywołanie nie powiodło się, kod statusu: {response.status_code}")
    print(response.text)
```

## Zaawansowane użycie

Dla API obsługującego zadania asynchroniczne można uzyskać wynik przez callback URL:

```python
data['callback_url'] = 'https://your-server.com/callback'
response = requests.post(url, headers=headers, json=data)
# Wynik zostanie przesłany na twój serwer przez callback URL
```

## Obsługa błędów

Typowe kody błędów:

| Kod statusu | Opis                              |
|-------------|----------------------------------|
| 401         | Błąd uwierzytelniania, sprawdź token API |
| 403         | Niewystarczające środki lub brak dostępu |
| 429         | Przekroczono limit zapytań       |
| 500         | Błąd wewnętrzny serwera          |

## Inne endpointy

Usługa Sora oferuje również następujące endpointy:

- `POST https://api.acedata.cloud/sora/tasks`

## Kolejne kroki

- Przejdź do [konsoli Ace Data Cloud](https://platform.acedata.cloud), aby uzyskać token API
- Zapoznaj się z [dokumentacją API](https://docs.acedata.cloud/zh-CN/api-reference) dla pełnego opisu parametrów
- Sprawdź [plany cenowe](https://platform.acedata.cloud/services) aby poznać koszty
