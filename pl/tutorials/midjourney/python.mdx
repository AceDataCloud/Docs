---
title: "Szybki start z Midjourney w Pythonie"
---

## Wprowadzenie

Midjourney to usługa AI do generowania obrazów oferowana przez Ace Data Cloud. Dzięki zunifikowanemu API Ace Data Cloud możesz szybko zintegrować Midjourney w Pythonie, realizując funkcje takie jak generowanie obrazów na podstawie tekstu, edycja obrazów, opisywanie obrazów, generowanie wideo z obrazów, kontrola Seed oraz asynchroniczne wywołania zwrotne.

## Wymagania wstępne

- Konto w [Ace Data Cloud](https://platform.acedata.cloud) oraz uzyskany token API
- Środowisko Python 3.7 lub nowsze
- Zainstalowana biblioteka requests: `pip install requests`

## Podstawowe użycie

Główny endpoint API Midjourney to:

```
POST https://api.acedata.cloud/midjourney/imagine
```

Pełny przykład kodu w Pythonie:

```python
import requests

url = "https://api.acedata.cloud/midjourney/imagine"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "prompt": "A serene Japanese garden with cherry blossoms",
    "action": "generate",
    "mode": "fast",
    "translation": true
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

Proszę zastąpić `YOUR_API_TOKEN` rzeczywistym tokenem uzyskanym na platformie Ace Data Cloud.

## Obsługa odpowiedzi

Po pomyślnym wywołaniu API zwraca dane w formacie JSON. Zaleca się sprawdzenie kodu statusu HTTP:

```python
if response.status_code == 200:
    result = response.json()
    print("Wywołanie zakończone sukcesem:", result)
else:
    print(f"Wywołanie nie powiodło się, kod statusu: {response.status_code}")
    print(response.text)
```

## Zaawansowane użycie

Dla API obsługujących zadania asynchroniczne można otrzymać wynik poprzez URL wywołania zwrotnego:

```python
data['callback_url'] = 'https://your-server.com/callback'
response = requests.post(url, headers=headers, json=data)
# Wynik zostanie przesłany na Twój serwer poprzez callback URL
```

## Obsługa błędów

Typowe kody błędów:

| Kod statusu | Opis |
|-------------|------|
| 401 | Nieautoryzowany, sprawdź token API |
| 403 | Niewystarczające środki lub brak dostępu |
| 429 | Przekroczono limit zapytań |
| 500 | Błąd wewnętrzny serwera |

## Inne endpointy

Usługa Midjourney udostępnia również następujące endpointy:

- `POST https://api.acedata.cloud/midjourney/seed`
- `POST https://api.acedata.cloud/midjourney/edits`
- `POST https://api.acedata.cloud/midjourney/videos`
- `POST https://api.acedata.cloud/midjourney/describe`

## Kolejne kroki

- Przejdź do [konsoli Ace Data Cloud](https://platform.acedata.cloud), aby uzyskać token API
- Zapoznaj się z [dokumentacją API](https://docs.acedata.cloud/zh-CN/api-reference) w celu poznania pełnej specyfikacji parametrów
- Sprawdź [cennik](https://platform.acedata.cloud/services) aby poznać koszty korzystania z usługi
