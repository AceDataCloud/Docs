---
title: "Wskazówki dotyczące integracji API weryfikacji czterech elementów karty bankowej"
description: "Identity Authentication API guide - Ace Data Cloud"
---

W niniejszym dokumencie przedstawiono sposób integracji API weryfikacji czterech elementów karty bankowej, które można wykorzystać do wprowadzenia numeru karty bankowej, imienia i nazwiska, numeru dokumentu tożsamości oraz numeru telefonu, aby zweryfikować prawdziwość i spójność informacji.

## Proces aplikacji

Aby skorzystać z API weryfikacji czterech elementów karty bankowej, najpierw przejdź do [konsoli Ace Data Cloud](https://platform.acedata.cloud/console/applications), aby uzyskać swój token API, który należy zachować na przyszłość.

![](https://cdn.acedata.cloud/dvc3cg.jpg)

Jeśli nie jesteś zalogowany lub zarejestrowany, automatycznie zostaniesz przekierowany na stronę logowania, aby zarejestrować się i zalogować, a po zakończeniu zostaniesz automatycznie przekierowany z powrotem na bieżącą stronę.

**Jeden token API wystarczy do korzystania ze wszystkich usług platformy, nie ma potrzeby składania osobnych wniosków dla każdej usługi.** Przy pierwszym wniosku przyznawana jest darmowa kwota, aby można było skorzystać z bezpłatnej wersji próbnej; w przypadku niewystarczającego salda można doładować saldo ogólne w [konsoli](https://platform.acedata.cloud/console/coin).

> 📘 Pełna dokumentacja: [API weryfikacji czterech elementów karty bankowej →](https://platform.acedata.cloud/documents/identity-bankcard-check-4e)

## Podstawowe użycie

Najpierw zapoznaj się z podstawowym sposobem użycia, polegającym na wprowadzeniu numeru karty bankowej, aby uzyskać przetworzony wynik w postaci obrazu, co wymaga prostego przekazania pola `bank_card`. Następnie możemy wypełnić odpowiednie treści na interfejsie, jak pokazano na rysunku:

<p><img src="https://cdn.acedata.cloud/1pl0y5.png" width="500" className="m-auto" /></p>

Można zauważyć, że ustawiliśmy nagłówki żądania, w tym:

- `accept`: format odpowiedzi, który chcemy otrzymać, tutaj wpisujemy `application/json`, czyli format JSON.
- `authorization`: klucz do wywołania API, po złożeniu wniosku można go bezpośrednio wybrać z rozwijanej listy.

Dodatkowo ustawiono ciało żądania, w tym:

- `bank_card`: numer karty bankowej do przetworzenia, jest to parametr obowiązkowy.
- `id_card`: numer dokumentu tożsamości, jest to parametr obowiązkowy.
- `name`: imię i nazwisko użytkownika, jest to parametr obowiązkowy.
- `phone`: zarezerwowany numer telefonu.
- `cert_type`: typ dokumentu tożsamości, musi być zgodny z dokumentem tożsamości.
- `encryption`: opcjonalny, parametr szyfrowania dla wrażliwych pól (jeśli wymagane jest przesłanie zaszyfrowanych danych).

Po dokonaniu wyboru można zauważyć, że po prawej stronie wygenerowano odpowiedni kod, jak pokazano na rysunku:

<p><img src="https://cdn.acedata.cloud/slajgv.png" width="500" className="m-auto" /></p>

Klikając przycisk „Try”, można przeprowadzić test, jak pokazano na powyższym rysunku, uzyskując następujący wynik:

```json
{
  "result": "0",
  "description": "Autoryzacja zakończona sukcesem"
}
```

Zwracany wynik zawiera wiele pól, które są opisane poniżej:

- `result`, kod wyniku autoryzacji, sytuacja dotycząca opłat jest następująca.
    - Kody wyników płatnych:
        - 0: Autoryzacja zakończona sukcesem
        - -1: Autoryzacja nie powiodła się
        - -4: Błędne informacje o posiadaczu karty
        - -5: Nieaktywowana płatność bez karty
        - -6: Ta karta została zatrzymana
        - -7: Nieprawidłowy numer karty
        - -8: Ta karta nie ma odpowiadającego wydawcy
        - -9: Ta karta nie została zainicjowana lub jest kartą uśpioną
        - -10: Karta oszukańcza, zatrzymana karta
        - -11: Ta karta została zgłoszona jako zgubiona
        - -12: Ta karta wygasła
        - -13: Karta z ograniczeniami
        - -14: Przekroczono dozwoloną liczbę błędów hasła
        - -15: Wydawca karty nie obsługuje tej transakcji
    - Kody wyników bezpłatnych:
        - -2: Weryfikacja imienia nie powiodła się
        - -3: Błędny numer karty bankowej
        - -16: Usługa centrum weryfikacji jest zajęta
        - -17: Przekroczono dozwoloną liczbę weryfikacji, spróbuj ponownie następnego dnia
- `description`, opis wyniku biznesowego.

Można zauważyć, że numer karty bankowej, imię i nazwisko, numer dokumentu tożsamości oraz numer telefonu mają prawdziwość i spójność.

Dodatkowo, jeśli chcesz wygenerować odpowiedni kod integracyjny, możesz go bezpośrednio skopiować, na przykład kod CURL wygląda następująco:

```shell
curl -X POST 'https://api.acedata.cloud/identity/bankcard/check-4e' \
-H 'accept: application/json' \
-H 'authorization: Bearer {token}' \
-H 'content-type: application/json' \
-d '{
  "name": "***",
  "phone": "***",
  "id_card": "***",
  "bank_card": "***"
}'
```

Kod integracyjny w Pythonie wygląda następująco:

```python
import requests

url = "https://api.acedata.cloud/identity/bankcard/check-4e"

headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json"
}

payload = {
    "name": "胡印福",
    "phone": "***",
    "id_card": "***",
    "bank_card": "***"
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

## Obsługa błędów

Podczas wywoływania API, jeśli wystąpią błędy, API zwróci odpowiedni kod błędu i informacje. Na przykład:

- `400 token_mismatched`: Zły wniosek, prawdopodobnie z powodu brakujących lub nieprawidłowych parametrów.
- `400 api_not_implemented`: Zły wniosek, prawdopodobnie z powodu brakujących lub nieprawidłowych parametrów.
- `401 invalid_token`: Nieautoryzowany, nieprawidłowy lub brakujący token autoryzacji.
- `429 too_many_requests`: Zbyt wiele żądań, przekroczono limit.
- `500 api_error`: Błąd wewnętrzny serwera, coś poszło nie tak na serwerze.

### Przykład odpowiedzi błędu

```json
{
  "success": false,
  "error": {
    "code": "api_error",
    "message": "pobieranie nie powiodło się"
  },
  "trace_id": "2cf86e86-22a4-46e1-ac2f-032c0f2a4e89"
}
```

## Wnioski

Dzięki temu dokumentowi zrozumiałeś, jak korzystać z API weryfikacji czterech elementów karty bankowej do wprowadzenia numeru karty bankowej, imienia i nazwiska, numeru dokumentu tożsamości oraz numeru telefonu, aby zweryfikować prawdziwość i spójność informacji. Mamy nadzieję, że ten dokument pomoże Ci lepiej zintegrować i korzystać z tego API. W przypadku jakichkolwiek pytań, prosimy o kontakt z naszym zespołem wsparcia technicznego.
