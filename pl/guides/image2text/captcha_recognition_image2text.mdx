---
title: "Dokumentacja integracji API rozpoznawania cyfrowych kodów weryfikacyjnych"
description: "Recognition of English numerical verification codes API guide - Ace Data Cloud"
---

W artykule przedstawiono sposób integracji API rozpoznawania cyfrowych kodów weryfikacyjnych, które opiera się na technologii głębokiego uczenia i może być używane do rozpoznawania zmiennych długości kodów alfanumerycznych. Wprowadź zawartość obrazu kodu weryfikacyjnego, a otrzymasz wynik kodu.

## Proces aplikacji

Aby korzystać z API rozpoznawania cyfrowych kodów weryfikacyjnych, najpierw przejdź do [konsoli Ace Data Cloud](https://platform.acedata.cloud/console/applications), aby uzyskać swój token API, który należy zachować na przyszłość.

![](https://cdn.acedata.cloud/dvc3cg.jpg)

Jeśli nie jesteś zalogowany lub zarejestrowany, automatycznie zostaniesz przekierowany na stronę logowania, aby zarejestrować się i zalogować, a po zakończeniu zostaniesz automatycznie przekierowany z powrotem na bieżącą stronę.

**Jeden token API wystarczy do wywołania wszystkich usług platformy, nie ma potrzeby składania osobnych wniosków dla każdej usługi.** Przy pierwszym wniosku otrzymasz darmowy limit, aby móc go przetestować; w przypadku niewystarczającego limitu możesz doładować saldo ogólne w [konsoli](https://platform.acedata.cloud/console/coin).

> 📘 Pełna dokumentacja: [API rozpoznawania cyfrowych kodów weryfikacyjnych →](https://platform.acedata.cloud/documents/captcha-recognition-image2text)

## Podstawowe użycie

Najpierw zapoznaj się z podstawowym sposobem użycia, polegającym na wprowadzeniu obrazu kodu weryfikacyjnego o zmiennej długości, aby uzyskać przetworzony wynik. Najpierw musisz prosto przekazać pole `image`, które jest konkretnym obrazem kodu alfanumerycznego, jak pokazano na rysunku:

<p><img src="https://cdn.acedata.cloud/c50hi9.png" width="500" className="m-auto" /></p>

Następnie musimy przekonwertować obraz kodu weryfikacyjnego na kodowanie Base64. Do konwersji kodowania Base64 zaleca się użycie wtyczki do przeglądarki Google Chrome FeHelper, a szczegółowe instrukcje można znaleźć na poniższym obrazku:

<p><img src="https://cdn.acedata.cloud/zy2jwh.png" width="500" className="m-auto" /></p>

<p><img src="https://cdn.acedata.cloud/pr73gn.png" width="500" className="m-auto" /></p>

<p><img src="https://cdn.acedata.cloud/ic4dbw.png" width="500" className="m-auto" /></p>

Następnie możesz skopiować kodowanie Base64 uzyskane z wtyczki FeHelper, pamiętaj, że nie zawiera ono prefiksu data:image/png;base64, a konkretna zawartość wygląda następująco:

<p><img src="https://cdn.acedata.cloud/5h4x4w.png" width="500" className="m-auto" /></p>

Można zauważyć, że ustawiliśmy nagłówki żądania, w tym:

- `accept`: jakiego formatu odpowiedzi oczekujesz, tutaj wpisz `application/json`, czyli format JSON.
- `authorization`: klucz do wywołania API, po złożeniu wniosku można go bezpośrednio wybrać z rozwijanej listy.

Dodatkowo ustawiono ciało żądania, w tym:

- `image`: zakodowany w Base64 obraz kodu weryfikacyjnego (bez prefiksu data:image/png;base64).

Po dokonaniu wyboru można zauważyć, że po prawej stronie wygenerowano odpowiedni kod, jak pokazano na rysunku:

<p><img src="https://cdn.acedata.cloud/202y3d.png" width="500" className="m-auto" /></p>

Kliknij przycisk „Try”, aby przeprowadzić test, jak pokazano na powyższym obrazku, a otrzymamy następujący wynik:

```json
{
  "text": "7364",
  "elapsed": 31.6
}
```

Zwrócony wynik zawiera wiele pól, które są opisane poniżej:

- `text`, treść tekstowa przetworzonego obrazu kodu weryfikacyjnego o zmiennej długości.
- `started_at`, `finished_at`: czas rozpoczęcia przetwarzania i uzyskania wyniku dla tego żądania, znacznik czasu Unix (sekundy, liczba zmiennoprzecinkowa).
- `elapsed`: całkowity czas przetwarzania (sekundy).

Można zauważyć, że uzyskaliśmy wynik weryfikacji przetworzonego obrazu kodu weryfikacyjnego o zmiennej długości, wystarczy, że na podstawie treści tekstowej w polu `text` przejdziemy weryfikację.

Jeśli chcesz wygenerować odpowiedni kod do integracji, możesz go bezpośrednio skopiować, na przykład kod CURL wygląda następująco:

```shell
curl -X POST 'https://api.acedata.cloud/captcha/recognition/image2text' \
-H 'accept: application/json' \
-H 'authorization: Bearer {token}' \
-H 'content-type: application/json' \
-d '{
  "image": "iVBORw0KGgoAAAANSUhEUgAAAgUAAAE3CAYAAAA6xjI2AAAAAX..."
}'
```

Kod do integracji w Pythonie wygląda następująco:

```python
import requests

url = "https://api.acedata.cloud/captcha/recognition/image2text"

headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json"
}

payload = {
    "image": "iVBORw0KGgoAAAANSUhEUgAAAgUAAAE3CAYAAAA6xjI2AAAAAX..."
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

## Tryb asynchroniczny (async)

Domyślnie API działa w trybie synchronicznym: jedno żądanie będzie czekać, aż przetwarzanie wyniku rozpoznawania zostanie zakończone, zanim zwróci odpowiedź. Jeśli wykonujesz rotację wielu rozwiązań (multi-solver rotation) i chcesz „natychmiast uzyskać task_id po złożeniu zadania, a następnie przejść do innych rozwiązań, a później wrócić po wyniki”, możesz przekazać `async: true` w ciele żądania.

Po przekazaniu `async: true` interfejs natychmiast zwróci `task_id`, a nie będzie czekać:

```shell
curl -X POST 'https://api.acedata.cloud/captcha/recognition/image2text' \
-H 'accept: application/json' \
-H 'authorization: Bearer {token}' \
-H 'content-type: application/json' \
-d '{
  "image": "iVBORw0KGgoAAAANSUhEUgAAAgUAAAE3CAYAAAA6xjI2AAAAAX...",
  "async": true
}'
```

```json
{
  "task_id": "61138bb6-19aa-11ec-a9c8-0242ac110002",
  "trace_id": "2efa9340-b21b-4e26-9e14-4aac95f343ab"
}
```

Aby aktywnie sprawdzić postęp, możesz użyć tego `task_id`, aby zapytać `POST /captcha/tasks` (zaleca się co 3-5 sekund). Ten interfejs nie uruchomi ani nie przyspieszy przetwarzania zadań; nawet jeśli nie zapytasz, stracisz połączenie lub wyjdziesz z klienta, serwer nadal będzie przetwarzać:

```shell
curl -X POST 'https://api.acedata.cloud/captcha/tasks' \
-H 'accept: application/json' \
-H 'authorization: Bearer {token}' \
-H 'content-type: application/json' \
-d '{
  "task_id": "61138bb6-19aa-11ec-a9c8-0242ac110002"
}'
```

W trakcie przetwarzania zwróci `status: processing`:

```json
{ "success": true, "task_id": "61138bb6-19aa-11ec-a9c8-0242ac110002", "status": "processing" }
```

Po zakończeniu przetwarzania zwróci `status: ready` oraz wynik rozpoznawania `text` (struktura pól jest całkowicie zgodna z trybem synchronicznym):

```json
{
  "success": true,
  "task_id": "61138bb6-19aa-11ec-a9c8-0242ac110002",
  "status": "ready",
  "text": "7364"
}
```

Opis rozliczeń: w trybie asynchronicznym tworzenie zadań i odczytywanie statusu „w trakcie przetwarzania” nie jest obciążane opłatami; **opłata jest naliczana tylko przy pierwszym odczycie wyniku sukcesu przez klienta** (zgodnie z obecnym zachowaniem i cenami w trybie synchronicznym). Serwer samodzielnie przyspiesza przetwarzanie zadań, ale nie nalicza opłat, jeśli zadanie zakończy się wcześniej w tle. Zadania, które nie zakończą się sukcesem w ciągu 120 sekund, zostaną zakończone jako HTTP 504 `timeout`, bez opłat. `/captcha/tasks` nie odpowiada za przyspieszanie zadań.

## Obsługa błędów

Podczas wywoływania API, jeśli wystąpi błąd, API zwróci odpowiedni kod błędu i informacje. Na przykład:
- `400 token_mismatched`：Zły wniosek, prawdopodobnie z powodu brakujących lub nieprawidłowych parametrów.
- `400 api_not_implemented`：Zły wniosek, prawdopodobnie z powodu brakujących lub nieprawidłowych parametrów.
- `401 invalid_token`：Nieautoryzowany, nieprawidłowy lub brakujący token autoryzacyjny.
- `429 too_many_requests`：Zbyt wiele żądań, przekroczyłeś limit szybkości.
- `500 api_error`：Błąd wewnętrzny serwera, coś poszło nie tak na serwerze.

### Przykład odpowiedzi błędu

```json
{
  "success": false,
  "error": {
    "code": "api_error",
    "message": "fetch failed"
  },
  "trace_id": "2cf86e86-22a4-46e1-ac2f-032c0f2a4e89"
}
```

## Wnioski

Dzięki temu dokumentowi zrozumiałeś, jak korzystać z API do rozpoznawania cyfrowych angielskich kodów weryfikacyjnych, które mogą być używane do rozpoznawania zmiennych długości angielskich cyfrowych kodów weryfikacyjnych. Wprowadź zawartość obrazu kodu weryfikacyjnego, a wynik kodu weryfikacyjnego zostanie wyjściem. Mamy nadzieję, że ten dokument pomoże Ci lepiej zintegrować i korzystać z tego API. W razie jakichkolwiek pytań, prosimy o kontakt z naszym zespołem wsparcia technicznego.
