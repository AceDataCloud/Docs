---
title: "Grok Videos Generation API Integrationsbeskrivning"
description: "Grok API guide - Ace Data Cloud"
---

Detta dokument kommer att introducera Grok Videos Generation API:s integrationsbeskrivning, som kan generera Grok Imagine (xAI) videor genom att mata in textpromptar, bilder och valfria referensbilder.

## Ansökningsprocess

För att använda Grok Videos Generation API, börja med att gå till [Ace Data Cloud-konsolen](https://platform.acedata.cloud/console/applications) för att få din API-token, som du ska spara.

![](https://cdn.acedata.cloud/5hmkdg.jpg)

Om du inte har loggat in eller registrerat dig, kommer du automatiskt att omdirigeras till inloggningssidan för att registrera dig och logga in, och efter att ha slutfört detta kommer du automatiskt att återvända till den aktuella sidan.

**En API-token kan användas för att anropa alla tjänster på plattformen, det behövs ingen separat ansökan för varje tjänst.** Första ansökan ger en gratis kvot för att prova; om kvoten är otillräcklig kan du ladda på allmän balans i [konsolen](https://platform.acedata.cloud/console/coin).

> 📘 Fullständig dokumentation: [Grok Videos Generation API →](https://platform.acedata.cloud/documents/grok-videos)

## Modellbeskrivning

Denna API stöder två modeller:

- `grok-imagine-video-1.5-fast` (standard): Stöder text-till-video (endast ange `prompt`) och bild-till-video (ange `image_url`), med en maximal längd på 30 sekunder, till ett lägre pris.
- `grok-imagine-video-1.5`: Stöder endast bild-till-video, **måste** ange `image_url`, med en maximal längd på 15 sekunder.

## Grundläggande användning

Först, förstå den grundläggande användningen, ange textprompten `prompt`, modellen `model` och andra parametrar för att generera motsvarande video.

Här kan vi se att vi har ställt in Request Headers, inklusive:

- `accept`: Vilket format av svar du vill ta emot, här anges `application/json`, det vill säga JSON-format.
- `authorization`: Nyckeln för att anropa API:t, efter ansökan kan du direkt välja från rullgardinsmenyn.

Dessutom har vi ställt in Request Body, inklusive:

- `prompt`: Textprompten som beskriver det innehåll du vill generera i videon. Användning av `grok-imagine-video-1.5-fast` för text-till-video är **obligatoriskt**; när `image_url` anges är det valfritt.
- `model`: Modellen för att generera videon, kan vara `grok-imagine-video-1.5-fast` (standard) eller `grok-imagine-video-1.5`.
- `image_url`: Länken till inmatningsbilden för bild-till-video. När `model` är `grok-imagine-video-1.5` är detta **obligatoriskt**.
- `reference_image_urls`: Valfri array av referensbildlänkar som används för att styra videons stil eller innehåll.
- `aspect_ratio`: Bredd-höjd-förhållandet för den genererade videon, kan vara `1:1` / `16:9` / `9:16` / `4:3` / `3:4` / `3:2` / `2:3`.
- `resolution`: Utdataupplösning, kan vara `480p` (standard), `720p` eller `1080p`.
- `duration`: Längden på den genererade videon (sekunder). `grok-imagine-video-1.5-fast` har ett intervall på 1–30, `grok-imagine-video-1.5` har ett intervall på 1–15, standard är 6. Rekommenderas att använda 6 sekunder eller 10 sekunder, dessa två standardlängder är relativt stabila.
- `callback_url`: Asynkron återkopplingsadress, när den är inställd kommer API:t omedelbart att returnera `task_id`, och när uppgiften är klar kommer resultatet att POST:as till den adressen.
- `async`: Valfritt, sätts till `true` om gränssnittet omedelbart ska returnera `task_id`, utan att behöva ange `callback_url`, och sedan kan resultatet hämtas genom att fråga motsvarande uppgiftsgränssnitt.

Klicka på "Try" knappen för att testa, resultatet blir liknande följande:

```json
{
  "success": true,
  "task_id": "b8976e18-32dc-4718-9ed8-1ea090fcb6ea",
  "trace_id": "fb751e1e-4705-49ea-9fd4-5024b7865ea2",
  "data": [
    {
      "id": "grok-imagine-video-1.5-fast:41eb9a5f-3b2d-4d1e-9f5a-6c2f1a0b9e77",
      "video_url": "https://cdn.acedata.cloud/c8cbf53aa0.mp4",
      "state": "succeeded"
    }
  ]
}
```

Det returnerade resultatet har flera fält, som beskrivs nedan:

- `success`: Om denna video-genereringsförfrågan var framgångsrik.
- `task_id`: ID för denna video-genereringsuppgift.
- `trace_id`: Spårnings-ID för denna förfrågan, används för att felsöka problem.
- `data`: Lista över genererade videoresultat.
  - `id`: Unik identifierare för den genererade videon.
  - `video_url`: Länkadressen till den genererade videon.
  - `state`: Status för video-genereringsuppgiften, kan vara `pending` / `succeeded` / `failed`.

Vi behöver bara hämta den genererade videon via `video_url` länken i `data` i resultatet.

Motsvarande CURL-kod ser ut så här:

```shell
curl -X POST 'https://api.acedata.cloud/grok/videos' \
-H 'authorization: Bearer ${bearer_token}' \
-H 'accept: application/json' \
-H 'content-type: application/json' \
-d '{
  "prompt": "A cinematic shot of a kitten chasing a butterfly in a sunlit garden",
  "model": "grok-imagine-video-1.5-fast",
  "resolution": "480p",
  "duration": 6
}'
```

Motsvarande Python-kod ser ut så här:

```python
import requests

url = "https://api.acedata.cloud/grok/videos"

headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json"
}

payload = {
    "prompt": "A cinematic shot of a kitten chasing a butterfly in a sunlit garden",
    "model": "grok-imagine-video-1.5-fast",
    "resolution": "480p",
    "duration": 6
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

## Bild-till-video

Om du vill generera en video baserat på en inmatningsbild kan du ange `image_url`. När du använder `grok-imagine-video-1.5` måste detta fält anges:

```json
{
  "prompt": "The character slowly turns around and smiles at the camera",
  "model": "grok-imagine-video-1.5",
  "image_url": "https://cdn.acedata.cloud/5hmkdg.jpg",
  "resolution": "720p",
  "duration": 6
}
```

## Referensbilder för vägledning

Om du vill använda en eller flera referensbilder för att styra videons stil eller innehåll kan du ange en array av bildlänkar i `reference_image_urls`:

```json
{
  "prompt": "A character dancing in the same art style",
  "model": "grok-imagine-video-1.5-fast",
  "reference_image_urls": [
    "https://cdn.acedata.cloud/vunnjf.png"
  ]
}
```

## Asynkron återkoppling

Videogenerering kräver viss behandlingstid. Om du inte vill hålla en lång anslutning och vänta kan du ange `callback_url`, då kommer API:t omedelbart att returnera `task_id`, och när uppgiften är klar kommer det slutgiltiga resultatet att POST:as till den adressen:

```json
{
  "prompt": "A cinematic shot of a kitten chasing a butterfly in a sunlit garden",
  "model": "grok-imagine-video-1.5-fast",
  "duration": 6,
  "callback_url": "https://your-domain.com/callback/grok"
}
```

Det omedelbart returnerade resultatet ser ut så här:

```json
{
  "task_id": "b8976e18-32dc-4718-9ed8-1ea090fcb6ea"
}
```

## Fråga uppgiftsresultat
Om du har använt asynkrona återkopplingar eller vill aktivt kontrollera uppgiftens status kan du använda [Grok Tasks API](https://platform.acedata.cloud/documents/grok-tasks) (`POST https://api.acedata.cloud/grok/tasks`) för att hämta den senaste statusen och resultaten för uppgiften baserat på `task_id`.

## Avgiftsinformation

Avgiftsstrukturen för denna tjänst bestäms av `model`:

- `grok-imagine-video-1.5-fast`: Avgift baserat på längd, oavsett upplösning — `1–10` sekunder, `11–20` sekunder, `21–30` sekunder motsvarar olika prisklasser.
- `grok-imagine-video-1.5`: Avgift baserat på "utgångssekunder", totalpris = enhetspris × `duration`, `1080p` är dyrare än `480p`/`720p`.

Specifika enhetspriser anges på prissidan. Misslyckade förfrågningar debiteras inte och påverkar inte den kostnadsfria kvoten.

## Felhantering

När en förfrågan uppstår problem kommer API:et att returnera motsvarande felkod och beskrivning, vanliga är följande:

- `400`: Förfrågningsparametrar är felaktiga, till exempel saknas `prompt` för text-till-video, eller `grok-imagine-video-1.5` saknar `image_url`, eller `duration` överskrider gränsen (`grok-imagine-video-1.5-fast` är 1–30, `grok-imagine-video-1.5` är 1–15).
- `401`: Autentisering misslyckades, token är ogiltig eller matchar inte API:et.
- `403`: Otillräcklig balans, eller prompten har avvisats av innehållsgranskning.
- `429`: För många förfrågningar, vänligen försök igen senare.
- `500`: Video generering misslyckades eller tjänsten är i ett felaktigt tillstånd.
