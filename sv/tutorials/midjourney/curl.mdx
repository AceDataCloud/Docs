---
title: "Midjourney cURL Snabbstart"
---

## Introduktion

Midjourney är en AI-bildtjänst som tillhandahålls av Ace Data Cloud, Midjourney AI bildgenerering. Genom Ace Data Clouds enhetliga API kan du snabbt använda cURL för att anropa Midjourney och uppnå funktioner som text-till-bild, bildredigering, bildbeskrivning, bild-till-video, Seed-kontroll, asynkron callback med mera.

## Förutsättningar

- Ha ett konto på [Ace Data Cloud](https://platform.acedata.cloud) och erhåll en API-token
- Ha en terminalmiljö med cURL installerat

## Grundläggande användning

Huvudendpoint för att anropa Midjourney API är:

```
POST https://api.acedata.cloud/midjourney/imagine
```

Fullständigt exempel på cURL-kommando:

```bash
curl -X POST https://api.acedata.cloud/midjourney/imagine \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"prompt": "A serene Japanese garden with cherry blossoms", "action": "generate", "mode": "fast", "translation": true}'
```

Byt ut `YOUR_API_TOKEN` mot den faktiska token du fått från Ace Data Cloud-plattformen.

## Exempel på svar

Vid lyckad anrop returneras data i JSON-format, till exempel:

```json
{
  "status": "success",
  "data": {
    ...
  }
}
```

## Använda jq för att bearbeta svar

Det rekommenderas att använda verktyget jq för att formatera utdata:

```bash
curl -s -X POST https://api.acedata.cloud/midjourney/imagine \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"prompt": "A serene Japanese garden with cherry blossoms", "action": "generate", "mode": "fast", "translation": true}' | jq .
```

## Felsökning

Vanliga felkoder:

| Statuskod | Beskrivning |
|-----------|-------------|
| 401       | Autentisering misslyckades, kontrollera API-token |
| 403       | Otillräcklig balans eller ingen åtkomst |
| 429       | För många förfrågningar (rate limit) |
| 500       | Intern serverfel |

## Nästa steg

- Gå till [Ace Data Cloud kontrollpanel](https://platform.acedata.cloud) för att hämta API-token
- Läs [API-dokumentationen](https://docs.acedata.cloud/zh-CN/api-reference) för fullständig parameterbeskrivning
- Se [prissättningsplaner](https://platform.acedata.cloud/services) för kostnadsinformation
