---
title: "DeepSeek JavaScript Snabbstart"
---

## Introduktion

DeepSeek är en AI-chattjänst från Ace Data Cloud, baserad på DeepSeek-seriens stora språkmodeller. Genom Ace Data Clouds enhetliga API kan du snabbt integrera DeepSeek med JavaScript för att möjliggöra flerrundiga konversationer, systempromptar, strömmande output, JSON-utdata och djupgående inferens.

## Förutsättningar

- Ett konto hos [Ace Data Cloud](https://platform.acedata.cloud) och en API-token
- Node.js 18+ eller en modern webbläsarmiljö

## Grundläggande användning

Huvudendpoint för att anropa DeepSeek API är:

```
POST https://api.acedata.cloud/deepseek/chat/completions
```

Detta exempel använder modellen `deepseek-r1`.
Tillgängliga modeller inkluderar: `deepseek-r1`, `deepseek-r1-0528`, `deepseek-v3`, `deepseek-v3-250324`.

Fullständigt JavaScript-exempel:

```javascript
const response = await fetch("https://api.acedata.cloud/deepseek/chat/completions", {
  method: "POST",
  headers: {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
  "model": "deepseek-r1",
  "messages": [
    {
      "role": "user",
      "content": "你好，请介绍一下你自己"
    }
  ],
  "max_tokens": 1024,
  "temperature": 0.7
}),
});

const result = await response.json();
console.log(result);
```

Byt ut `YOUR_API_TOKEN` mot din faktiska token från Ace Data Cloud-plattformen.

## Hantering av svar

Det rekommenderas att kontrollera svarskoden och hantera fel:

```javascript
if (response.ok) {
  const result = await response.json();
  console.log("Anrop lyckades:", result);
} else {
  console.error(`Anrop misslyckades, statuskod: ${response.status}`);
  const error = await response.text();
  console.error(error);
}
```

## Node.js-wrapper

Det rekommenderas att kapsla in i en återanvändbar funktion:

```javascript
async function callDeepSeek(data) {
  const response = await fetch("https://api.acedata.cloud/deepseek/chat/completions", {
    method: "POST",
    headers: {
      "Authorization": `Bearer ${process.env.ACE_API_TOKEN}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify(data),
  });
  if (!response.ok) throw new Error(`API error: ${response.status}`);
  return response.json();
}
```

## Felhantering

Vanliga felkoder:

| Statuskod | Beskrivning |
|-----------|-------------|
| 401       | Autentisering misslyckades, kontrollera API-token |
| 403       | Otillräcklig saldo eller ingen åtkomst |
| 429       | För många förfrågningar |
| 500       | Internt serverfel |

## Nästa steg

- Besök [Ace Data Cloud-konsolen](https://platform.acedata.cloud) för att hämta din API-token
- Läs [API-dokumentationen](https://docs.acedata.cloud/zh-CN/api-reference) för fullständig parameterbeskrivning
- Se [prissättningsplaner](https://platform.acedata.cloud/services) för kostnadsinformation
