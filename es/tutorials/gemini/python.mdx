---
title: "Gemini Python Guía Rápida"
---

## Introducción

Gemini es un servicio de chat AI proporcionado por Ace Data Cloud, basado en la serie de modelos de lenguaje grande Google Gemini. A través de la API unificada de Ace Data Cloud, puedes integrar Gemini rápidamente con Python para lograr funciones como diálogo multi-turno, mensajes de sistema, salida en streaming, modo de salida JSON, entre otras.

## Requisitos Previos

- Tener una cuenta en [Ace Data Cloud](https://platform.acedata.cloud) y obtener un API Token
- Entorno Python 3.7 o superior
- Instalar la librería requests: `pip install requests`

## Uso Básico

El endpoint principal para llamar a la API de Gemini es:

```
POST https://api.acedata.cloud/gemini/chat/completions
```

Este ejemplo usa el modelo `gemini-2.5-flash`.  
Modelos disponibles incluyen: `gemini-2.5-flash`, `gemini-2.5-pro`, `gemini-2.0-flash`, `gemini-3.0-pro`.

Ejemplo completo de código Python:

```python
import requests

url = "https://api.acedata.cloud/gemini/chat/completions"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "model": "gemini-2.5-flash",
    "messages": [
        {
            "role": "user",
            "content": "你好，请介绍一下你自己"
        }
    ],
    "max_tokens": 1024,
    "temperature": 0.7
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

Por favor, reemplaza `YOUR_API_TOKEN` con el Token real que obtuviste en la plataforma Ace Data Cloud.

## Manejo de Respuestas

Después de una llamada exitosa, la API devuelve datos en formato JSON. Se recomienda verificar el código de estado HTTP:

```python
if response.status_code == 200:
    result = response.json()
    print("Llamada exitosa:", result)
else:
    print(f"Llamada fallida, código de estado: {response.status_code}")
    print(response.text)
```

## Uso Avanzado

Para la API de chat, se soporta salida en streaming para obtener respuestas en tiempo real:

```python
import requests

data['stream'] = True
response = requests.post(url, headers=headers, json=data, stream=True)
for line in response.iter_lines():
    if line:
        print(line.decode())
```

## Manejo de Errores

Códigos de error comunes:

| Código de Estado | Descripción                           |
|------------------|-------------------------------------|
| 401              | Autenticación fallida, verifica el API Token |
| 403              | Saldo insuficiente o acceso denegado |
| 429              | Límite de frecuencia de solicitudes excedido |
| 500              | Error interno del servidor          |

## Próximos Pasos

- Visita la [Consola de Ace Data Cloud](https://platform.acedata.cloud) para obtener tu API Token
- Consulta la [Documentación API](https://docs.acedata.cloud/zh-CN/api-reference) para conocer todos los parámetros
- Revisa los [Planes de Precios](https://platform.acedata.cloud/services) para información sobre costos
