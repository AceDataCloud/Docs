---
title: "Introducción rápida a hCaptcha con Python"
---

## Introducción

hCaptcha es un servicio de captcha proporcionado por Ace Data Cloud. Con la solución hCaptcha y la API unificada de Ace Data Cloud, puedes integrar rápidamente hCaptcha usando Python para obtener tokens de hCaptcha, reconocimiento de imágenes de hCaptcha y otras funcionalidades.

## Requisitos previos

- Tener una cuenta en [Ace Data Cloud](https://platform.acedata.cloud) y obtener un API Token
- Entorno Python 3.7 o superior
- Instalar la librería requests: `pip install requests`

## Uso básico

El endpoint principal para llamar a la API de hCaptcha es:

```
POST https://api.acedata.cloud/captcha/token/hcaptcha
```

Ejemplo completo de código en Python:

```python
import requests

url = "https://api.acedata.cloud/captcha/token/hcaptcha"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "website_key": "a5f74b19-9e45-40e0-b45d-47ff91b7a6c2",
    "website_url": "https://accounts.hcaptcha.com/demo"
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

Por favor, reemplaza `YOUR_API_TOKEN` con el token real que obtuviste en la plataforma Ace Data Cloud.

## Manejo de la respuesta

Después de una llamada exitosa, la API devuelve datos en formato JSON. Se recomienda verificar el código de estado HTTP:

```python
if response.status_code == 200:
    result = response.json()
    print("Llamada exitosa:", result)
else:
    print(f"Llamada fallida, código de estado: {response.status_code}")
    print(response.text)
```

## Uso avanzado

Para APIs que soportan tareas asíncronas, puedes obtener resultados mediante una URL de callback:

```python
data['callback_url'] = 'https://your-server.com/callback'
response = requests.post(url, headers=headers, json=data)
# Los resultados serán enviados a tu servidor a través de la URL de callback
```

## Manejo de errores

Códigos de error comunes:

| Código | Descripción                          |
|--------|------------------------------------|
| 401    | Autenticación fallida, verifica el API Token |
| 403    | Saldo insuficiente o acceso denegado |
| 429    | Límite de frecuencia de solicitudes excedido |
| 500    | Error interno del servidor          |

## Otros endpoints

El servicio hCaptcha también ofrece los siguientes endpoints:

- `POST https://api.acedata.cloud/captcha/recognition/hcaptcha`

## Próximos pasos

- Visita la [Consola de Ace Data Cloud](https://platform.acedata.cloud) para obtener tu API Token
- Consulta la [Documentación API](https://docs.acedata.cloud/zh-CN/api-reference) para detalles completos de los parámetros
- Revisa los [Planes de precios](https://platform.acedata.cloud/services) para información sobre costos
