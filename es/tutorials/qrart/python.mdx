---
title: "Introducción rápida a QR Art con Python"
---

## Introducción

QR Art es un servicio de imágenes AI proporcionado por Ace Data Cloud, que genera códigos QR artísticos con inteligencia artificial. A través de la API unificada de Ace Data Cloud, puedes integrar rápidamente QR Art con Python para crear códigos QR de enlaces, textos, estilos predefinidos, parámetros personalizados y más.

## Requisitos previos

- Tener una cuenta en [Ace Data Cloud](https://platform.acedata.cloud) y obtener un API Token
- Entorno Python 3.7 o superior
- Instalar la librería requests: `pip install requests`

## Uso básico

El endpoint principal para llamar a la API de QR Art es:

```
POST https://api.acedata.cloud/qrart/generate
```

Ejemplo completo de código Python:

```python
import requests

url = "https://api.acedata.cloud/qrart/generate"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "type": "link",
    "content": "https://platform.acedata.cloud",
    "prompt": "sunset ocean waves",
    "preset": "neon-mech"
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

Por favor, reemplaza `YOUR_API_TOKEN` con el Token real que obtuviste en la plataforma Ace Data Cloud.

## Manejo de la respuesta

Tras una llamada exitosa, la API devuelve datos en formato JSON. Se recomienda verificar el código de estado HTTP:

```python
if response.status_code == 200:
    result = response.json()
    print("Llamada exitosa:", result)
else:
    print(f"Llamada fallida, código de estado: {response.status_code}")
    print(response.text)
```

## Uso avanzado

Para APIs que soportan tareas asíncronas, puedes obtener resultados mediante una URL de callback:

```python
data['callback_url'] = 'https://your-server.com/callback'
response = requests.post(url, headers=headers, json=data)
# Los resultados serán enviados a tu servidor a través de la URL de callback
```

## Manejo de errores

Códigos de error comunes:

| Código | Descripción                              |
|--------|----------------------------------------|
| 401    | Autenticación fallida, verifica el API Token |
| 403    | Saldo insuficiente o acceso denegado   |
| 429    | Límite de frecuencia de solicitudes excedido |
| 500    | Error interno del servidor              |

## Otros endpoints

El servicio QR Art también ofrece los siguientes endpoints:

- `POST https://api.acedata.cloud/qrart/tasks`

## Próximos pasos

- Visita la [consola de Ace Data Cloud](https://platform.acedata.cloud) para obtener tu API Token
- Consulta la [documentación API](https://docs.acedata.cloud/zh-CN/api-reference) para conocer todos los parámetros
- Revisa los [planes de precios](https://platform.acedata.cloud/services) para detalles sobre costos
