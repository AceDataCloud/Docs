---
title: "Suno Wav API 대접 설명"
description: "Suno Music Generation 集成指南 - Ace Data Cloud"
---

SUNO는 우리가 음악의 wav 형식 파일을 가져올 수 있도록 허용합니다. 본 문서는 관련 API의 대접 방법을 설명합니다.

이 API의 핵심 입력 매개변수는 `audio_id`이며, 이는 공식적으로 생성된 곡 ID입니다; 선택적으로 `callback_url` 비동기 콜백 주소도 지원합니다.

여기서 입력하는 `audio_id`는 `ec13e502-d043-4eb2-92ee-e900c6da69d1`입니다.

```python
import requests

url = "https://api.acedata.cloud/suno/wav"

headers = {
    "accept": "application/json",
    "authorization": "Bearer aa287fa4cc54401087a9fab3f99630af",
    "content-type": "application/json"
}

payload = {
    "audio_id": "ec13e502-d043-4eb2-92ee-e900c6da69d1"
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

결과는 다음과 같습니다:

```json
{
  "success": true,
  "task_id": "19787138-49e9-413a-8611-156c375aa99f",
  "trace_id": "ec232cf9-8b75-42df-a8a3-fe6d7d6fd02e",
  "data": [
    {
      "file_url": "https://cdn1.suno.ai/ec13e502-d043-4eb2-92ee-e900c6da69d1.wav"
    }
  ]
}
```

볼 수 있듯이, `data`의 `file_url` 필드는 가져온 음악의 wav 형식 파일로, 공개적으로 접근 가능한 CDN 주소이며, mp3 접미사를 지원합니다.
