---
title: "Suno Wav API 연동 설명"
description: "Suno Music Generation 集成指南 - Ace Data Cloud"
---

SUNO는 음악의 wav 형식 파일을 가져올 수 있도록 허용하며, 본 문서에서는 관련 API 연동 방법을 설명합니다.

이 API의 핵심 입력 파라미터는 `audio_id`로, 이는 공식에서 생성한 곡 ID입니다. 선택적으로 `callback_url` 비동기 콜백 주소도 지원합니다.

여기서 입력하는 `audio_id`는 `4e43116a-bf09-472c-8e1c-655eabf02682`입니다.

```python
import requests

url = "https://api.acedata.cloud/suno/wav"

headers = {
    "accept": "application/json",
    "authorization": "Bearer aa287fa4cc54401087a9fab3f99630af",
    "content-type": "application/json"
}

payload = {
    "audio_id": "4e43116a-bf09-472c-8e1c-655eabf02682"
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

결과는 다음과 같습니다:

```json
{
  "success": true,
  "task_id": "6a5a2099-d6d3-4930-9709-a30ac5dc7de5",
  "trace_id": "3fa70e81-6bb7-4ca8-b718-dd16a4eda7e8",
  "data": [
    {
      "file_url": "https://platform.cdn.acedata.cloud/suno/6a5a2099-d6d3-4930-9709-a30ac5dc7de5.wav"
    }
  ]
}
```

보시다시피, `data`의 `file_url` 필드는 가져온 음악의 wav 형식 파일로, 공개 접근 가능한 CDN 주소입니다.

> **WAV 링크의 영속성에 대하여**
>
> 상위 Suno CDN의 WAV 파일(`https://cdn1.suno.ai/{audio_id}.wav`)은 며칠간만 보관되며 이후 회수되어 접근 시 403 오류가 발생합니다.
> 링크 만료를 방지하기 위해, 본 API는 반환 전에 상위 WAV 파일을 자동으로 자체 CDN(`https://platform.cdn.acedata.cloud/suno/{task_id}.wav`)에 전송 저장하며, 반환되는 `file_url`은 전송 저장된 안정적인 주소로 상위 CDN 만료로 인한 접근 불가 문제를 방지합니다.
> 전송 저장 실패 시(매우 드문 경우) 원본 상위 URL로 되돌아가며, 기존 동작과 동일하게 처리됩니다.
