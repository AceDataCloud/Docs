---
title: "Suno Python Quick Start"
---

## Introduction

Suno is an AI audio service provided by Ace Data Cloud, offering Suno AI music generation. Through the unified API of Ace Data Cloud, you can quickly integrate Suno using Python to achieve features such as text-to-music generation, custom lyrics, song continuation, cover singing, vocal separation, lyrics generation, and more.

## Prerequisites

- Have an [Ace Data Cloud](https://platform.acedata.cloud) account and obtain an API Token
- Python 3.7 or above environment
- Install the requests library: `pip install requests`

## Basic Usage

The main endpoint for calling the Suno API is:

```
POST https://api.acedata.cloud/suno/audios
```

This example uses the `chirp-v5` model.  
Available models include: `chirp-v5`, `v4-5-plus`, `v4-5`, `v4`, `v3-5`.

Complete Python code example:

```python
import requests

url = "https://api.acedata.cloud/suno/audios"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "action": "generate",
    "prompt": "A cheerful pop song about summer vacation",
    "model": "chirp-v5",
    "custom": false
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

Please replace `YOUR_API_TOKEN` with the actual Token you obtained from the Ace Data Cloud platform.

## Response Handling

After a successful call, the API returns data in JSON format. It is recommended to check the HTTP status code:

```python
if response.status_code == 200:
    result = response.json()
    print("Call succeeded:", result)
else:
    print(f"Call failed, status code: {response.status_code}")
    print(response.text)
```

## Advanced Usage

For APIs that support asynchronous tasks, you can obtain results via a callback URL:

```python
data['callback_url'] = 'https://your-server.com/callback'
response = requests.post(url, headers=headers, json=data)
# Results will be pushed to your server via the callback URL
```

## Error Handling

Common error codes:

| Status Code | Description                          |
|-------------|------------------------------------|
| 401         | Authentication failed, please check your API Token |
| 403         | Insufficient balance or no access  |
| 429         | Request rate limit exceeded         |
| 500         | Internal server error               |

## Other Endpoints

The Suno service also provides the following endpoints:

- `POST https://api.acedata.cloud/suno/lyrics`
- `POST https://api.acedata.cloud/suno/persona`
- `POST https://api.acedata.cloud/suno/wav`
- `POST https://api.acedata.cloud/suno/mp4`
- `POST https://api.acedata.cloud/suno/tasks`

## Next Steps

- Go to the [Ace Data Cloud Console](https://platform.acedata.cloud) to obtain your API Token
- Check the [API Documentation](https://docs.acedata.cloud/zh-CN/api-reference) for full parameter descriptions
- Review the [Pricing Plans](https://platform.acedata.cloud/services) for cost details
