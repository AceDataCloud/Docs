---
title: "Seedream Python Quick Start"
---

## Introduction

Seedream is an AI image service provided by Ace Data Cloud, featuring the ByteDance Seedream image generation model. Through the unified API of Ace Data Cloud, you can quickly integrate Seedream using Python to achieve text-to-image generation, image editing, multiple resolutions, watermark control, and other functions.

## Prerequisites

- Have an [Ace Data Cloud](https://platform.acedata.cloud) account and obtain an API Token
- Python 3.7 or above environment
- Install the requests library: `pip install requests`

## Basic Usage

The main endpoint for calling the Seedream API is:

```
POST https://api.acedata.cloud/seedream/images
```

This example uses the `doubao-seedream-4-0-250828` model.  
Available models include: `doubao-seedream-4-5-251128`, `doubao-seedream-4-0-250828`, `doubao-seedream-3-0-t2i-250415`, `doubao-seededit-3-0-i2i-250628`.

Complete Python code example:

```python
import requests

url = "https://api.acedata.cloud/seedream/images"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "prompt": "A white Siamese cat sitting on the windowsill",
    "model": "doubao-seedream-4-0-250828",
    "size": "1K"
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

Please replace `YOUR_API_TOKEN` with the actual Token you obtained from the Ace Data Cloud platform.

## Response Handling

Upon a successful call, the API returns data in JSON format. It is recommended to check the HTTP status code:

```python
if response.status_code == 200:
    result = response.json()
    print("Call succeeded:", result)
else:
    print(f"Call failed, status code: {response.status_code}")
    print(response.text)
```

## Advanced Usage

For APIs supporting asynchronous tasks, you can get results via a callback URL:

```python
data['callback_url'] = 'https://your-server.com/callback'
response = requests.post(url, headers=headers, json=data)
# Results will be pushed to your server via the callback URL
```

## Error Handling

Common error codes:

| Status Code | Description                      |
|-------------|--------------------------------|
| 401         | Authentication failed, please check your API Token |
| 403         | Insufficient balance or no access permission |
| 429         | Request rate limit exceeded     |
| 500         | Internal server error           |

## Other Endpoints

The Seedream service also provides the following endpoint:

- `POST https://api.acedata.cloud/seedream/tasks`

## Next Steps

- Go to the [Ace Data Cloud Console](https://platform.acedata.cloud) to obtain your API Token
- Check the [API Documentation](https://docs.acedata.cloud/zh-CN/api-reference) for full parameter details
- Review the [Pricing Plans](https://platform.acedata.cloud/services) for cost information
