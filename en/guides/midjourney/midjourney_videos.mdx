---
title: "Midjourney Videos API Integration Guide"
description: "Midjourney generation integration guide - Ace Data Cloud"
---

This document introduces the Midjourney Videos API integration guide, which allows generating official Midjourney videos by inputting custom parameters.

## Application Process

To use the API, you need to apply for the corresponding service on the [Midjourney Videos API](https://platform.acedata.cloud/documents/midjourney-videos) page. After entering the page, click the "Acquire" button as shown below:

![](https://cdn.acedata.cloud/q6ytrc.png)

If you are not logged in or registered, you will be automatically redirected to the login page to register and log in. After logging in or registering, you will be automatically returned to the current page.

A free quota is granted upon the first application, allowing you to use the API for free.

## Basic Usage

First, understand the basic usage method: by inputting the prompt `prompt`, generation action `action`, and an array of reference images for the first and last frames `image_url`, you can obtain the processed result. You need to simply pass an `action` field with the value `generate`. It mainly includes two behaviors: generate video (`generate`) and extend video (`extend`). Details are as follows:

<p><img src="https://cdn.acedata.cloud/pi72m9.png" width="500" className="m-auto" /></p>

Here you can see we set the Request Headers, including:

- `accept`: the desired response format, here set to `application/json`, i.e., JSON format.
- `authorization`: the API key for calling the API, which can be selected from a dropdown after application.

Also, the Request Body includes:

- `image_url`: the reference image URL for the first frame of the generated video.
- `end_image_url`: optional, specifies the reference image for the last frame of the generated video.
- `video_id`: required when extending a video, specifies the video ID.
- `video_index`: specifies which video to extend under the given `video_id`, index starts from 0, default is 0.
- `action`: the behavior for this video generation task, mainly includes two behaviors: generate video (`generate`) and extend video (`extend`).
- `prompt`: the prompt.
- `mode`: video generation speed mode, default is fast.
- `resolution`: video resolution, default is 720p.
- `loop`: whether to generate a looping video, default is false.
- `callback_url`: URL for receiving callback results.

After selection, you can see the corresponding code generated on the right, as shown below:

<p><img src="https://cdn.acedata.cloud/y0cw0p.png" width="500" className="m-auto" /></p>

Click the "Try" button to test. As shown above, we get the following result:

```json
{
  "image_url": "https://storage.fonedis.cc/upload_1751816808164156352.png",
  "image_width": 560,
  "image_height": 688,
  "progress": 100,
  "video_id": "1751816807896311",
  "video_urls": [
    "https://storage.fonedis.cc//video/1c67c36c-8177-4f19-ad72-1dc1567265a6/0_0.mp4",
    "https://storage.fonedis.cc//video/1c67c36c-8177-4f19-ad72-1dc1567265a6/0_1.mp4",
    "https://storage.fonedis.cc//video/1c67c36c-8177-4f19-ad72-1dc1567265a6/0_2.mp4",
    "https://storage.fonedis.cc//video/1c67c36c-8177-4f19-ad72-1dc1567265a6/0_3.mp4"
  ],
  "task_id": "037955e0-deee-4050-baa8-1416300d67e2",
  "success": true
}
```

The returned result contains multiple fields, described as follows:

- `success`: the status of the video generation task.
- `task_id`: the ID of the video generation task.
- `image_url`: the cover image of the video generation task.
- `image_width`: the width of the cover image.
- `image_height`: the height of the cover image.
- `video_id`: the video ID of the video generation task.
- `video_urls`: an array of video URLs generated by the task.

You can see we obtained satisfactory video information. We only need to use the video URLs in `video_urls` to access the generated Midjourney videos.

If you want to generate the corresponding integration code, you can directly copy the generated code. For example, the CURL code is as follows:

```shell
curl -X POST 'https://api.acedata.cloud/midjourney/videos' \
-H 'accept: application/json' \
-H 'authorization: Bearer {token}' \
-H 'content-type: application/json' \
-d '{
  "action": "generate",
  "prompt": "A cat sitting on a table",
  "image_url": "https://cdn.acedata.cloud/jgo1cw.jpg"
}'
```

## Extend Video Function

If you want to continue generating an already created Kling video, set the parameter `action` to `extend` and input the video ID to continue generating. The video ID can be obtained from the basic usage above.

Here, the video ID from the previous example is:

```
"video_id": "1751816807896311"
```

> Note that the `video_id` here is the ID of the generated video. If you don't know how to generate a video, refer to the basic usage section above.

Next, you must fill in the prompt for the next step of extending the video to customize the generated video, specifying the following:

- `video_index`: select the index of the video to extend, this index corresponds to the `video_urls` generated above, starting from 0, default is 0.
- `video_id`: the video ID to extend.
- `action`: the behavior for this extension, set to `extend`.
- `prompt`: the prompt.

Example input is as follows:

<p><img src="https://cdn.acedata.cloud/855hnj.png" width="500" className="m-auto" /></p>

After filling in, the code is automatically generated as follows:

<p><img src="https://cdn.acedata.cloud/p58w39.png" width="500" className="m-auto" /></p>

Corresponding Python code:

```python
import requests

url = "https://api.acedata.cloud/midjourney/videos"

headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json"
}

payload = {
    "action": "extend",
    "prompt": "A cat sitting on a table",
    "video_id": "1751816807896311",
    "video_index": 1
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

Click run, and you will get a result like this:

```json
{
    "image_url": "https://storage.fonedis.cc/upload_1751817471047011172.png",
    "image_width": 560,
    "image_height": 688,
    "progress": 100,
    "video_id": "1751818094559027",
    "video_urls": [
        "https://storage.fonedis.cc//video/a4bd2f43-b925-462d-9725-8aef98403133/0_0.mp4",
        "https://storage.fonedis.cc//video/a4bd2f43-b925-462d-9725-8aef98403133/0_1.mp4",
        "https://storage.fonedis.cc//video/a4bd2f43-b925-462d-9725-8aef98403133/0_2.mp4",
        "https://storage.fonedis.cc//video/a4bd2f43-b925-462d-9725-8aef98403133/0_3.mp4"
    ],
    "task_id": "da3bdcd0-9c21-4b40-877a-2c36e5f479e5",
    "success": true
}
```

You can see the result is consistent with the previous one, which realizes the video extension function.

## Asynchronous Callback

Because the Midjourney Videos API generation takes relatively long, about 1-2 minutes, if the API does not respond for a long time, the HTTP request will keep the connection open, causing extra system resource consumption. Therefore, this API also supports asynchronous callbacks.

The overall process is: when the client initiates a request, it additionally specifies a `callback_url` field. After the client sends the API request, the API immediately returns a result containing a `task_id` field representing the current task ID. When the task is completed, the generated video result is sent via POST JSON to the client-specified `callback_url`, including the `task_id` field, so the task result can be associated by ID.

Let's understand the specific operation through an example.

First, the Webhook callback is a service that can receive HTTP requests. Developers should replace it with the URL of their own HTTP server. For demonstration, a public webhook sample website https://webhook.site/ is used. Open the website to get a Webhook URL, as shown below:

<p><img src="https://cdn.acedata.cloud/lali6d.png" width="500" className="m-auto" /></p>

Copy this URL to use as the Webhook. The example URL here is `https://webhook.site/556e6971-b41f-4fa8-9151-6e91acd0399f`.

Next, set the `callback_url` field to the above Webhook URL and fill in the corresponding parameters, as shown below:

<p><img src="https://cdn.acedata.cloud/vk0l0a.png" width="500" className="m-auto" /></p>

Click run, and you will immediately get a result like this:

```
{
  "task_id": "b726a27a-f379-4d91-b569-cfe4b7b299ee"
}
```

After a moment, you can observe the generated video result at `https://webhook.site/556e6971-b41f-4fa8-9151-6e91acd0399f`, as shown below:

<p><img src="https://cdn.acedata.cloud/7hcuw8.png" width="500" className="m-auto" /></p>

Content is as follows:

```json
{
  "image_url": "https://storage.fonedis.cc/upload_1751818513244368774.png",
  "image_width": 560,
  "image_height": 688,
  "progress": 100,
  "video_id": "1751818512924054",
  "video_urls": [
    "https://storage.fonedis.cc//video/9ff3783e-bcf6-4f11-b738-09aa52318e6e/0_0.mp4",
    "https://storage.fonedis.cc//video/9ff3783e-bcf6-4f11-b738-09aa52318e6e/0_1.mp4",
    "https://storage.fonedis.cc//video/9ff3783e-bcf6-4f11-b738-09aa52318e6e/0_2.mp4",
    "https://storage.fonedis.cc//video/9ff3783e-bcf6-4f11-b738-09aa52318e6e/0_3.mp4"
  ],
  "task_id": "b726a27a-f379-4d91-b569-cfe4b7b299ee",
  "success": true
}
```

You can see the result includes a `task_id` field, and other fields are similar to above. This field allows task association.

## Error Handling

When calling the API, if an error occurs, the API returns the corresponding error code and message. For example:

- `400 token_mismatched`: Bad request, possibly due to missing or invalid parameters.
- `400 api_not_implemented`: Bad request, possibly due to missing or invalid parameters.
- `401 invalid_token`: Unauthorized, invalid or missing authorization token.
- `429 too_many_requests`: Too many requests, you have exceeded the rate limit.
- `500 api_error`: Internal server error, something went wrong on the server.

### Error Response Example

```json
{
  "success": false,
  "error": {
    "code": "api_error",
    "message": "fetch failed"
  },
  "trace_id": "2cf86e86-22a4-46e1-ac2f-032c0f2a4e89"
}
```

## Conclusion

Through this document, you have learned how to use the Midjourney Videos API to generate videos by inputting prompts and first-frame reference images. We hope this document helps you better integrate and use the API. If you have any questions, please feel free to contact our technical support team.