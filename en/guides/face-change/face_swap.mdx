---
title: "Integration and Usage of Face Swap API"
description: "Face Transformation Integration Guide - Ace Data Cloud"
---

The main function of the Face Swap API is to swap the face in the source image with the face in the target image by inputting a source image and a target image.

This document provides a detailed guide on integrating the Face Swap API to help you easily integrate and fully leverage the powerful features of this API. With the Face Swap API, you can effortlessly swap the face in the target image onto the face in the source image.

## Application Process

To use the Face Swap API, you need to first apply for the corresponding service on the application page [Face Swap API](https://platform.acedata.cloud/documents/d10df07a-66a6-4c37-b114-3a003fc9bd55). After entering the page, click the "Acquire" button as shown below:

![Application Page](https://cdn.acedata.cloud/rci31i.png)

If you are not logged in or registered, you will be automatically redirected to the [login page](https://platform.acedata.cloud) to register and log in. After logging in or registering, you will be automatically returned to the current page.

A free quota is granted upon the first application, allowing you to use the API free of charge.

## Request Example

We use two images as examples to demonstrate how to use the API. Suppose the source image is shown below:

<p><img src="https://cdn.acedata.cloud/n1lmd8.png" width="500" className="m-auto" /></p>

The target image is:

<p><img src="https://cdn.acedata.cloud/3np95r.png" width="500" className="m-auto" /></p>

Next, we demonstrate how to swap the face in the target image onto the face in the source image.

### Setting Request Headers and Body

**Request Headers** include:

- `accept`: specifies receiving response results in JSON format, here set to `application/json`.
- `authorization`: the API key for calling the API, selectable from a dropdown after application.

**Request Body** includes:

- `source_image_url`: URL of the uploaded source image.
- `target_image_url`: URL of the uploaded target image.
- `timeout`: optional, processing timeout in seconds; the request will return immediately if timeout occurs.

Set as shown below:

<p><img src="https://cdn.acedata.cloud/g7tnn1.png" width="500" className="m-auto" /></p>

### Code Examples

You can see that various language code snippets are automatically generated on the right side of the page, as shown below:

<p><img src="https://cdn.acedata.cloud/t8gsls.png" width="500" className="m-auto" /></p>

Some code examples are as follows:

#### CURL

```bash
curl -X POST 'https://api.acedata.cloud/face/swap' \
-H 'accept: application/json' \
-H 'authorization: Bearer {token}' \
-H 'content-type: application/json' \
-d '{
  "source_image_url": "https://cdn.acedata.cloud/n1lmd8.png",
  "target_image_url": "https://cdn.acedata.cloud/3np95r.png"
}'
```

#### Python

```python
import requests

url = "https://api.acedata.cloud/face/swap"

headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json"
}

payload = {
    "source_image_url": "https://cdn.acedata.cloud/n1lmd8.png",
    "target_image_url": "https://cdn.acedata.cloud/3np95r.png"
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

### Response Example

Upon a successful request, the API returns information about the swapped face image result. For example:

```json
{
  "image_url": "https://platform.cdn.acedata.cloud/face/4b13bdeb-1b19-4ea5-bddf-c2da14ba72e3.png",
  "image_width": 2008,
  "image_height": 1942,
  "image_size": 4006213,
  "task_id": "4b13bdeb-1b19-4ea5-bddf-c2da14ba72e3"
}
```

As you can see, the result contains an `image_url` field with the link to the image where the face in the target image has replaced the face in the original image. Other information is as follows:

- `image_url`: URL of the generated image.
- `image_width`: width of the generated image.
- `image_height`: height of the generated image.
- `image_size`: size of the generated image.
- `task_id`: ID of this generation task.

The generated image result is:

<p><img src="https://cdn.acedata.cloud/soa3lk.png" width="500" className="m-auto" /></p>

It is clearly visible that the face from the target image has been successfully swapped onto the face in the source image.

## Asynchronous Callback

Since the Face Swap generation takes relatively long, about 1-2 minutes, if the API does not respond for a long time, the HTTP request will keep the connection open, causing extra system resource consumption. Therefore, this API also supports asynchronous callbacks.

The overall process is: when the client initiates the request, it additionally specifies a `callback_url` field. After the client sends the API request, the API immediately returns a result containing a `task_id` field representing the current task ID. When the task is completed, the Face Swap result will be sent via POST JSON to the client-specified `callback_url`, which also includes the `task_id` field, allowing the task results to be correlated by ID.

Let's understand the specific operation through an example.

First, the Webhook callback is a service that can receive HTTP requests. Developers should replace it with the URL of their own HTTP server. For demonstration convenience, a public Webhook sample site https://webhook.site/ is used here. Opening this site will provide a Webhook URL, as shown below:

![](https://cdn.acedata.cloud/r8wyb7.png)

Copy this URL to use as the Webhook. The sample here is https://webhook.site/3b76eba5-4573-432a-b607-3000b87afc06.

Next, we set the `callback_url` field to the above Webhook URL and fill in the corresponding parameters, as shown below:

<p><img src="https://cdn.acedata.cloud/xx08i2.png" width="500" className="m-auto" /></p>

Click run, and you will immediately get a result as follows:

```json
{
  "task_id": "9cba9d36-3b14-43c9-85b6-86f6dfc3b096"
}
```

After a short wait, you can observe the Face Swap generation result on https://webhook.site/3b76eba5-4573-432a-b607-3000b87afc06, as shown below:

![](https://cdn.acedata.cloud/col42s.png)

Content as follows:

```json
{
  "success": true,
  "task_id": "9cba9d36-3b14-43c9-85b6-86f6dfc3b096",
  "image_url": "https://platform.cdn.acedata.cloud/face/9cba9d36-3b14-43c9-85b6-86f6dfc3b096.png",
  "image_width": 2008,
  "image_height": 1942,
  "image_size": 4006481
}
```

You can see the result contains a `task_id` field, and other fields are similar to those mentioned above. This field allows task correlation.

## Error Handling

When calling the API, if an error occurs, the API will return the corresponding error code and message. For example:

- `400 token_mismatched`: Bad request, possibly due to missing or invalid parameters.
- `400 api_not_implemented`: Bad request, possibly due to missing or invalid parameters.
- `401 invalid_token`: Unauthorized, invalid or missing authorization token.
- `429 too_many_requests`: Too many requests, you have exceeded the rate limit.
- `500 api_error`: Internal server error, something went wrong on the server.

### Error Response Example

```
{
  "success": false,
  "error": {
    "code": "api_error",
    "message": "fetch failed"
  },
  "trace_id": "2cf86e86-22a4-46e1-ac2f-032c0f2a4e89"
}
```

## Conclusion

Through this document, you have learned how to use the Face Swap API to swap the face in the target image onto the face in the source image. We hope this document helps you better integrate and use this API. If you have any questions, please feel free to contact our technical support team.