---
title: "Luma Video Generation API Integration Guide"
description: "Luma Video Generation Integration Guide - Ace Data Cloud"
---

With the widespread application of AI, various AI programs have gradually become popular. AI has increasingly penetrated all aspects of people's work and life. The industries involved in AI are also expanding, from initial writing, to healthcare and education, and now to video.

Luma is a professional high-quality video generation platform where users only need to upload materials to automatically generate high-quality videos according to different styles and effects. This AI video generator is developed by team members from well-known technology companies, aiming to enable everyone to easily create excellent videos without complex editing tools.

However, Luma does not officially provide an API. AceDataCloud offers a set of Luma APIs that simulate integration with Suno's official service, allowing convenient and fast generation of desired videos.

## Application and Usage

To use the Luma Videos API, first visit the [Luma Videos Generation API](https://platform.acedata.cloud/documents/5bd3597d-1ff8-44ad-a580-b66b48393e7f) page and click the "Acquire" button to obtain the credentials required for requests:

![](https://cdn.acedata.cloud/nyq0xz.png)

If you are not logged in or registered, you will be automatically redirected to the login page to register and log in. After logging in or registering, you will be automatically returned to the current page.

A free quota is granted upon first application, allowing free use of the API.

## Basic Usage

To generate any video, you can input any text. For example, if I want to generate a video about astronauts shuttling between space and volcanoes, I can input `Astronauts shuttle from space to volcano`, as shown below:

<p><img src="https://cdn.acedata.cloud/yub02j.png" width="500" className="m-auto" /></p>

The generated code is as follows:

<p><img src="https://cdn.acedata.cloud/ieq7yn.png" width="500" className="m-auto" /></p>

Main request parameters:

- `prompt`: The prompt for generating the video.
- `aspect_ratio`: Video aspect ratio, default is 16:9.
- `end_image_url`: Optional, specify the end frame.
- `enhancement`: Optional, clarity enhancement toggle.
- `loop`: Whether to generate a looping video, default is false.
- `timeout`: Optional, timeout in seconds.
- `callback_url`: Asynchronous callback URL.

You can click the "Try" button to directly test the API. After waiting 1-2 minutes, the result is as follows:

```json
{
  "success": true,
  "task_id": "e4018a99-1522-4f24-9330-62c2a9b50b59",
  "video_id": "155838f8-7f1e-44d8-b387-192f3b4b509d",
  "prompt": "Astronauts shuttle from space to volcano",
  "video_url": "https://storage.cdn-luma.com/dream_machine/af94e7ca-da35-4b5f-a636-2d7254184d0d/watermarked_video0585de3737db946e5a0ac895384ecd180.mp4",
  "video_height": 752,
  "video_width": 1360,
  "state": "completed",
  "thumbnail_url": "https://platform.cdn.acedata.cloud/luma/e4018a99-1522-4f24-9330-62c2a9b50b59.jpg",
  "thumbnail_width": 1360,
  "thumbnail_height": 752
}
```

You can see that we have obtained relevant information about the video, including the video ID, video link, video thumbnail, and other content.

Field descriptions:

- success: Whether generation was successful; `true` if successful, otherwise `false`.
- task_id: The unique ID of the video generation task.
- video_id: The unique ID of the video generated by the task.
- prompt: The keyword for the video generation task.
- video_url: The resulting video URL from the task.
- video_height: Height of the generated video thumbnail.
- video_width: Width of the generated video thumbnail.
- state: The status of the video generation task; `completed` if finished.
- thumbnail_url: URL of the generated video thumbnail.
- thumbnail_width: Width of the generated video thumbnail.
- thumbnail_height: Height of the generated video thumbnail.

## Custom Start and End Frame Generation

If you want to generate a video by customizing the start and end frames, you can input the image URLs for the start and end frames:

At this point, the video start frame `start_image_url` field can be passed the following image as the video's start frame:

![Start Frame](https://cdn.acedata.cloud/r9vsv9.png)

Next, to customize video generation based on the start and end frames and keywords, you can specify the following:

- action: The behavior of the video generation task, usually `generate` for normal generation and `extend` for extended generation, default is `generate`.
- start_image_url: Specify the start frame of the generated video.
- end_image_url: Specify the end frame of the generated video.
- prompt: The keyword content for video generation.

Sample input is as follows:

<p><img src="https://cdn.acedata.cloud/zvzydx.png" width="500" className="m-auto" /></p>

After filling in, the code is automatically generated as follows:

<p><img src="https://cdn.acedata.cloud/tx80pu.png" width="500" className="m-auto" /></p>

Corresponding code:

```python
import requests

url = "https://api.acedata.cloud/luma/videos"

headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json"
}

payload = {
    "start_image_url": "https://cdn.acedata.cloud/r9vsv9.png",
    "action": "generate",
    "prompt": "Astronauts shuttle from space to volcano"
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

The result is as follows:

```json
{
  "success": true,
  "task_id": "12a18694-fd4b-47e7-9c50-34f30862cff6",
  "video_id": "0105c090-03a5-425a-8026-523341cd575b",
  "prompt": "Astronauts shuttle from space to volcano",
  "video_url": "https://platform.cdn.acedata.cloud/luma/12a18694-fd4b-47e7-9c50-34f30862cff6.mp4",
  "video_height": 656,
  "video_width": 1552,
  "state": "completed",
  "thumbnail_url": "https://platform.cdn.acedata.cloud/luma/12a18694-fd4b-47e7-9c50-34f30862cff6.jpg",
  "thumbnail_width": 1552,
  "thumbnail_height": 656
}
```

The final result is similar to the above; the generated video's start frame includes the image we passed in. Of course, you can also pass in both start and end frame image URLs to generate a video. Just add an end frame image on top of the above. The end frame image information is as follows:

![End Frame](https://cdn.acedata.cloud/0iad3k.png)

Sample input is as follows:

<p><img src="https://cdn.acedata.cloud/20igwi.png" width="500" className="m-auto" /></p>

The final result is:

```json
{
  "success": true,
  "task_id": "d1cb723a-e554-4775-94a4-bb6ae8c7ea67",
  "video_id": "6bebd0d2-f793-472e-9326-38528a9273bb",
  "prompt": "Astronauts shuttle from space to volcano",
  "video_url": "https://platform.cdn.acedata.cloud/luma/d1cb723a-e554-4775-94a4-bb6ae8c7ea67.mp4",
  "video_height": 656,
  "video_width": 1552,
  "state": "completed",
  "thumbnail_url": "https://platform.cdn.acedata.cloud/luma/d1cb723a-e554-4775-94a4-bb6ae8c7ea67.jpg",
  "thumbnail_width": 1552,
  "thumbnail_height": 656
}
```

The result is similar to the above, with the generated video containing both the start and end frame images, completing custom start and end frame video generation.

## Video Extension Feature

If you want to continue generating a video, set the parameter `action` to `extend` and input the ID or URL of the video to be extended. The video ID and URL can be obtained from the basic usage as shown below:

<p><img src="https://cdn.acedata.cloud/fwknj4.png" width="500" className="m-auto" /></p>

Here, the video ID is:

```
"video_id": "0105c090-03a5-425a-8026-523341cd575b",
"video_url": "https://platform.cdn.acedata.cloud/luma/12a18694-fd4b-47e7-9c50-34f30862cff6.mp4"
```

> Note that the `video_id` and `video_url` here are the ID and link of the generated video. If you do not know how to generate a video, refer to the basic usage above.

To continue generating a video, you must upload the video URL or video ID. Below is a demonstration using the video ID for extension. You must fill in keywords to customize video generation, specifying the following:

- action: The behavior for video extension, here it should be `extend`.
- prompt: Keywords for the video extension.
- video_url: URL of the video to be extended.
- video_id: Unique ID of the video to be extended.
- end_image_url: Optional, specify an end frame image URL for the extended video.

Sample input is as follows:

<p><img src="https://cdn.acedata.cloud/vv0rxk.png" width="500" className="m-auto" /></p>

After filling in, the code is automatically generated as follows:

<p><img src="https://cdn.acedata.cloud/woapxi.png" width="500" className="m-auto" /></p>

Corresponding Python code:

```python
import requests

url = "https://api.acedata.cloud/luma/videos"

headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json"
}

payload = {
    "action": "extend",
    "video_id": "0105c090-03a5-425a-8026-523341cd575b",
    "prompt": "Astronauts shuttle from space to volcano"
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

Click run, and you will get a result as follows:

```json
{
  "success": true,
  "task_id": "c6e529d1-a06d-4c12-91b2-c855135131c3",
  "video_id": "36908c49-c2bb-4a11-bd5a-b8512b004818",
  "prompt": "Astronauts shuttle from space to volcano",
  "video_url": "https://platform.cdn.acedata.cloud/luma/c6e529d1-a06d-4c12-91b2-c855135131c3.mp4",
  "video_height": 656,
  "video_width": 1552,
  "state": "completed",
  "thumbnail_url": "https://platform.cdn.acedata.cloud/luma/c6e529d1-a06d-4c12-91b2-c855135131c3.jpg",
  "thumbnail_width": 1552,
  "thumbnail_height": 656
}
```

It can be seen that this video is extended based on the video to be extended, and the result content is consistent with the above, thus realizing the video continuation generation feature.

Of course, you can also specify the video URL to extend generation by filling in the following information:

<p><img src="https://cdn.acedata.cloud/0cv0hg.png" width="500" className="m-auto" /></p>

After running, the following result is obtained:

```json
{
  "success": true,
  "task_id": "1dcb5902-a7be-4b77-ba5d-dd8ec82b26ca",
  "video_id": "f0187dc2-339f-4a08-a435-c3a3341f620a",
  "prompt": "Astronauts shuttle from space to volcano",
  "video_url": "https://platform.cdn.acedata.cloud/luma/1dcb5902-a7be-4b77-ba5d-dd8ec82b26ca.mp4",
  "video_height": 656,
  "video_width": 1552,
  "state": "completed",
  "thumbnail_url": "https://platform.cdn.acedata.cloud/luma/1dcb5902-a7be-4b77-ba5d-dd8ec82b26ca.jpg",
  "thumbnail_width": 1552,
  "thumbnail_height": 656
}
```

The result shows that video extension can also be achieved based on the video URL.

Finally, you can specify an end frame image for the extended video. Below is the end frame image information:

![End Frame](https://cdn.acedata.cloud/0iad3k.png)

Next, add the end frame image information on top of the above, as shown below:

<p><img src="https://cdn.acedata.cloud/9p1vrj.png" width="500" className="m-auto" /></p>

Click run to get the following information:

```json
{
  "success": true,
  "task_id": "b816b2b4-c345-4673-9e19-83e91f91b643",
  "video_id": "c5400053-63e6-4206-8082-31cf9dd1e7ed",
  "prompt": "Astronauts shuttle from space to volcano",
  "video_url": "https://platform.cdn.acedata.cloud/luma/b816b2b4-c345-4673-9e19-83e91f91b643.mp4",
  "video_height": 656,
  "video_width": 1552,
  "state": "completed",
  "thumbnail_url": "https://platform.cdn.acedata.cloud/luma/b816b2b4-c345-4673-9e19-83e91f91b643.jpg",
  "thumbnail_width": 1552,
  "thumbnail_height": 656
}
```

It can be seen that on top of the extended video above, you can also specify an end frame image for extension.

## Asynchronous Callback

Since Luma video generation takes relatively long, about 1-2 minutes, if the API does not respond for a long time, the HTTP request will keep the connection open, causing extra system resource consumption. Therefore, this API also supports asynchronous callbacks.

The overall process is: when the client initiates a request, it additionally specifies a `callback_url` field. After the client sends the API request, the API immediately returns a result containing a `task_id` field representing the current task ID. When the task is completed, the generated video result will be sent via POST JSON to the client-specified `callback_url`, which also includes the `task_id` field, allowing task results to be associated by ID.

Let's understand the specific operation through an example.

First, the webhook callback is a service that can receive HTTP requests. Developers should replace it with the URL of their own HTTP server. For demonstration, a public webhook sample website https://webhook.site/ is used. Open the site to get a webhook URL, as shown below:

<p><img src="https://cdn.acedata.cloud/q78okf.png" width="500" className="m-auto" /></p>

Copy this URL to use as the webhook. The sample here is https://webhook.site/0c87ca0e-cd74-4577-8d68-f2b80fbf8a13.

Next, set the `callback_url` field to the above webhook URL and fill in the `prompt`, as shown below:

<p><img src="https://cdn.acedata.cloud/n2fjvi.png" width="500" className="m-auto" /></p>

Click run, and you will immediately get a result as follows:

```json
{
  "task_id": "732f8282-7cf8-401c-95f2-42c33aa079a6"
}
```

After a short wait, you can observe the generated video result at https://webhook.site/0c87ca0e-cd74-4577-8d68-f2b80fbf8a13, as shown below:

![](https://cdn.acedata.cloud/1hwm5m.png)

Content is as follows:

```json
{
    "success": true,
    "task_id": "732f8282-7cf8-401c-95f2-42c33aa079a6",
    "video_id": "4d8013c3-5de0-41aa-966e-0b1a51d1c633",
    "prompt": "Astronauts shuttle from space to volcano",
    "video_url": "https://platform.cdn.acedata.cloud/luma/732f8282-7cf8-401c-95f2-42c33aa079a6.mp4",
    "video_height": 752,
    "video_width": 1360,
    "state": "completed",
    "thumbnail_url": "https://platform.cdn.acedata.cloud/luma/732f8282-7cf8-401c-95f2-42c33aa079a6.jpg",
    "thumbnail_width": 1360,
    "thumbnail_height": 752
}
```

You can see the result contains a `task_id` field, and other fields are similar to above. This field allows task association.