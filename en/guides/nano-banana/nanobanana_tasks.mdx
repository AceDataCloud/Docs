---
title: "Integration and Usage of Nano Banana Tasks API"
description: "Nano Banana Image Generation Integration Guide - Ace Data Cloud"
---

The main function of the Nano Banana Tasks API is to query the execution status of a task by inputting the task ID generated by the Nano Banana Images API.

This document provides a detailed explanation of the integration of the Nano Banana Tasks API to help you easily integrate and fully utilize the powerful features of this API. With the Nano Banana Tasks API, you can effortlessly query the execution status of tasks from the Nano Banana Images API.

## Request Example

The Nano Banana Tasks API can be used to query the results of the Nano Banana Images API. For how to use the Nano Banana Images API, please refer to the document [Nano Banana Images API](https://platform.acedata.cloud/documents/63e01dc3-eb21-499e-8049-3025c460058f).

We take a task ID returned by the Nano Banana Images API service as an example to demonstrate how to use this API. Suppose we have a task ID: 4d320ead-4af4-4a55-8f3e-f2afebdf4fd0, next we will demonstrate how to query by passing a task ID.

### Task Example Image

<p><img src="https://cdn.acedata.cloud/nuiuth.png" width="500" className="m-auto" /></p>

### Setting Request Headers and Body

**Request Headers** include:

- `accept`: Specifies that the response should be in JSON format, set to `application/json`.
- `authorization`: The API key for calling the API, which can be selected from a dropdown after application.

**Request Body** includes:

- `id`: The uploaded task ID.
- `action`: The operation type on the task.

Set as shown in the image below:

<p><img src="https://cdn.acedata.cloud/nuiuth.png" width="500" className="m-auto" /></p>

### Code Examples

You can see that code snippets in various languages are automatically generated on the right side of the page, as shown below:

<p><img src="https://cdn.acedata.cloud/r1u4vl.png" width="500" className="m-auto" /></p>

Partial code examples are as follows:

#### CURL

```bash
curl -X POST 'https://api.acedata.cloud/nano-banana/tasks' \
-H 'accept: application/json' \
-H 'authorization: Bearer {token}' \
-H 'content-type: application/json' \
-d '{
  "id": "4d320ead-4af4-4a55-8f3e-f2afebdf4fd0",
  "action": "retrieve"
}'
```

#### Python

```python
import requests

url = "https://api.acedata.cloud/nano-banana/tasks"

headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json"
}

payload = {
    "id": "4d320ead-4af4-4a55-8f3e-f2afebdf4fd0",
    "action": "retrieve"
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

### Response Example

Upon a successful request, the API will return detailed information about the task. For example:

```json
{
  "_id": "68bc7c3c550a4144a53d0e24",
  "id": "4d320ead-4af4-4a55-8f3e-f2afebdf4fd0",
  "api_id": "9d8a117e-31ca-4322-a0fd-1771296ec610",
  "application_id": "8afd681a-2a4e-4265-aecb-43970094c019",
  "created_at": 1757183036.787,
  "credential_id": "097b2987-62f4-4ac0-b0cc-aed41e372a07",
  "request": {
    "action": "generate",
    "prompt": "a white siamese cat"
  },
  "trace_id": "7ba1f1e8-0ef8-450d-8bb2-b5c3bf1ea319",
  "type": "images",
  "user_id": "b87f67c1-b04f-4332-99a1-7a5e651331c6",
  "response": {
    "success": true,
    "task_id": "4d320ead-4af4-4a55-8f3e-f2afebdf4fd0",
    "trace_id": "7ba1f1e8-0ef8-450d-8bb2-b5c3bf1ea319",
    "data": [
      {
        "prompt": "a white siamese cat",
        "image_url": "https://platform.cdn.acedata.cloud/nanobanana/7e7bd000-698a-4e14-bb2d-3db61237e4bb.png"
      }
    ]
  }
}
```

The response contains multiple fields. The `request` field is the request body sent when initiating the task, and the `response` field is the response body returned after the task is completed. Field descriptions are as follows:

- `id`: The ID generated for this task, used to uniquely identify this generation task.
- `request`: The request information of the queried task.
- `response`: The response information of the queried task.

## Batch Query Operation

This is for querying task details for multiple task IDs. Unlike above, the `action` should be set to `retrieve_batch`.

**Request Body** includes:

- `ids`: An array of uploaded task IDs.
- `action`: The operation type on the tasks.

Example:

```bash
curl -X POST 'https://api.acedata.cloud/nano-banana/tasks' \
-H 'accept: application/json' \
-H 'authorization: Bearer {token}' \
-H 'content-type: application/json' \
-d '{
  "ids": ["1ebe4f2b-59ba-4385-a4ea-0ce8a3fe12ed","1ebe4f2b-59ba-4385-a4ea-0ce8a3fe12ed"],
  "action": "retrieve_batch"
}'
```

### Response Example

Upon a successful request, the API will return detailed information for all batch tasks. For example:

```json
{
  "items": [
    {
      "_id": "68bc7c3c550a4144a53d0e24",
      "id": "4d320ead-4af4-4a55-8f3e-f2afebdf4fd0",
      "api_id": "9d8a117e-31ca-4322-a0fd-1771296ec610",
      "application_id": "8afd681a-2a4e-4265-aecb-43970094c019",
      "created_at": 1757183036.787,
      "credential_id": "097b2987-62f4-4ac0-b0cc-aed41e372a07",
      "request": {
        "action": "generate",
        "prompt": "a white siamese cat"
      },
      "trace_id": "7ba1f1e8-0ef8-450d-8bb2-b5c3bf1ea319",
      "type": "images",
      "user_id": "b87f67c1-b04f-4332-99a1-7a5e651331c6",
      "response": {
        "success": true,
        "task_id": "4d320ead-4af4-4a55-8f3e-f2afebdf4fd0",
        "trace_id": "7ba1f1e8-0ef8-450d-8bb2-b5c3bf1ea319",
        "data": [
          {
            "prompt": "a white siamese cat",
            "image_url": "https://platform.cdn.acedata.cloud/nanobanana/7e7bd000-698a-4e14-bb2d-3db61237e4bb.png"
          }
        ]
      }
    }
  ],
  "count": 1
}
```

The response contains multiple fields. The `items` field contains detailed information for all batch tasks. It is an array, and each element has the same format as the single task query response described above. Field descriptions are as follows:

- `items`: Detailed information of all batch tasks. It is an array where each element corresponds to a single task's detailed response.
- `count`: The number of tasks returned in this batch query.

## Error Handling

When calling the API, if an error occurs, the API will return the corresponding error code and message. For example:

- `400 token_mismatched`: Bad request, possibly due to missing or invalid parameters.
- `400 api_not_implemented`: Bad request, possibly due to missing or invalid parameters.
- `401 invalid_token`: Unauthorized, invalid or missing authorization token.
- `429 too_many_requests`: Too many requests, you have exceeded the rate limit.
- `500 api_error`: Internal server error, something went wrong on the server.

### Error Response Example

```json
{
  "success": false,
  "error": {
    "code": "api_error",
    "message": "fetch failed"
  },
  "trace_id": "2cf86e86-22a4-46e1-ac2f-032c0f2a4e89"
}
```

## Conclusion

Through this document, you have learned how to use the Nano Banana Tasks API to query detailed information for single or batch tasks. We hope this guide helps you better integrate and use the API. If you have any questions, please feel free to contact our technical support team.