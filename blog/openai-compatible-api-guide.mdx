---
title: "OpenAI 兼容 API 完全指南"
description: "通过 OpenAI 兼容接口，一个 SDK 调用 Claude、Gemini、DeepSeek、Grok 等 50+ AI 模型，只需更改 base_url。"
---

## 什么是 OpenAI 兼容接口

OpenAI 的 Chat Completions API 格式已成为行业事实标准。许多大语言模型都提供了与 OpenAI 兼容的接口，使得开发者可以用相同的代码接入不同模型。

Ace Data Cloud 为 Claude、Gemini、DeepSeek、Grok、Kimi 等模型都提供了 OpenAI 兼容的 Chat Completions 接口，只需修改 Base URL 和模型名称即可切换。

## 支持的模型

| 服务 | 端点 | 示例模型 |
|------|------|----------|
| Claude | `POST /v1/chat/completions` | claude-sonnet-4-6 |
| OpenAI | `POST /openai/chat/completions` | gpt-4o |
| Gemini | `POST /gemini/chat/completions` | gemini-2.5-flash |
| DeepSeek | `POST /deepseek/chat/completions` | deepseek-r1 |
| Grok | `POST /grok/chat/completions` | grok-3 |
| Kimi | `POST /kimi/chat/completions` | kimi-k2.5 |

## 统一调用方式

所有聊天模型使用相同的请求格式：

```python
import requests

def chat(endpoint, model, message):
    return requests.post(
        f"https://api.acedata.cloud{endpoint}",
        headers={
            "Authorization": "Bearer YOUR_API_TOKEN",
            "Content-Type": "application/json",
        },
        json={
            "model": model,
            "messages": [{"role": "user", "content": message}],
            "max_tokens": 1024,
            "temperature": 0.7,
        },
    ).json()

# 调用不同模型 - 只需改端点和模型名
claude = chat("/v1/chat/completions", "claude-sonnet-4-6", "你好")
gpt = chat("/openai/chat/completions", "gpt-4o", "你好")
gemini = chat("/gemini/chat/completions", "gemini-2.5-flash", "你好")
deepseek = chat("/deepseek/chat/completions", "deepseek-r1", "你好")
```

## 流式输出

所有聊天 API 都支持流式输出（Server-Sent Events）：

```python
data = {
    "model": "claude-sonnet-4-6",
    "messages": [{"role": "user", "content": "写一首诗"}],
    "stream": True,
}

response = requests.post(
    "https://api.acedata.cloud/v1/chat/completions",
    headers={"Authorization": "Bearer YOUR_API_TOKEN"},
    json=data,
    stream=True,
)

for line in response.iter_lines():
    if line:
        print(line.decode())
```

## 与 OpenAI SDK 兼容

由于接口格式兼容，你可以直接使用 OpenAI 官方 SDK，只需修改 base_url：

```python
from openai import OpenAI

client = OpenAI(
    api_key="YOUR_API_TOKEN",
    base_url="https://api.acedata.cloud/v1",  # Claude
    # base_url="https://api.acedata.cloud/openai",  # OpenAI
    # base_url="https://api.acedata.cloud/gemini",  # Gemini
)

response = client.chat.completions.create(
    model="claude-sonnet-4-6",
    messages=[{"role": "user", "content": "你好"}],
)
print(response.choices[0].message.content)
```

## 总结

通过 Ace Data Cloud 的 OpenAI 兼容接口，你可以：

- 用一套代码接入 6+ 种大语言模型
- 灵活切换模型，比较效果和成本
- 直接使用 OpenAI SDK，零迁移成本
- 统一的计费和监控

前往 [platform.acedata.cloud](https://platform.acedata.cloud) 免费试用。
