---
title: "How to use Claude API with Python"
seo: "Claude API Python tutorial — Quick start guide with code examples"
description: "Learn how to integrate Claude API into your Python application. Step-by-step guide with authentication, request examples, and error handling."
---

This tutorial shows you how to use the **Claude API** with Python through Ace Data Cloud's unified API platform.

## What you'll build

By the end of this tutorial, you'll have a working Python script that calls the Claude API. Key capabilities:

- streaming
- multi-turn
- vision
- code generation
- 128K context

## Prerequisites

- Python 3.8+
- An [Ace Data Cloud API key](https://platform.acedata.cloud) (free trial available)
- `requests` library (`pip install requests`)

## Step 1: Get your API key

<Steps>
<Step title="Create an account">
    Sign up at [platform.acedata.cloud](https://platform.acedata.cloud).
</Step>
<Step title="Subscribe to Claude">
    Go to the [Claude service page](https://platform.acedata.cloud) and click **Acquire**. You'll get free trial credits.
</Step>
<Step title="Copy your API key">
    Navigate to your credentials and copy the Bearer token.
</Step>
</Steps>

## Step 2: Make your first request

```python
import requests

API_KEY = "YOUR_API_KEY"
BASE_URL = "https://api.acedata.cloud"

response = requests.post(
    f"{BASE_URL}/v1/chat/completions",
    headers={
        "Authorization": f"Bearer {API_KEY}",
        "Content-Type": "application/json",
    },
    json={
  "model": "claude-sonnet-4-20250514",
  "messages": [
    {
      "role": "user",
      "content": "Hello, what can you do?"
    }
  ]
},
)

data = response.json()
print(data)
```

## Step 3: Handle the response

The API returns a JSON response. Here's how to parse it:

```python
if response.status_code == 200:
    result = response.json()
    print("Success:", result)
elif response.status_code == 401:
    print("Error: Invalid API key")
elif response.status_code == 429:
    print("Error: Rate limit exceeded, try again later")
else:
    print(f"Error {response.status_code}:", response.text)
```

## Step 4: Streaming (real-time response)

For chat models, you can stream responses token by token:

```python
import requests

response = requests.post(
    f"{BASE_URL}/v1/chat/completions",
    headers={
        "Authorization": f"Bearer {API_KEY}",
        "Content-Type": "application/json",
        "Accept": "text/event-stream",
    },
    json={
        "model": "claude-sonnet-4-20250514",
        "messages": [{"role": "user", "content": "Tell me a story"}],
        "stream": True,
    },
    stream=True,
)

for line in response.iter_lines():
    if line:
        decoded = line.decode("utf-8")
        if decoded.startswith("data: ") and decoded != "data: [DONE]":
            print(decoded[6:], end="", flush=True)
```

## Error handling

| Status code | Meaning | What to do |
|---|---|---|
| 200 | Success | Parse the response |
| 400 | Bad request | Check your request parameters |
| 401 | Unauthorized | Verify your API key |
| 403 | Forbidden | Content may violate usage policies |
| 429 | Rate limited | Wait and retry with exponential backoff |
| 500 | Server error | Retry after a short delay |

## Next steps

<CardGroup cols={2}>
<Card title="API reference" href="/api-reference/claude" icon="code">
    Full API specification with all parameters.
</Card>
<Card title="Detailed guide" href="/guides/claude/claude_chat_completions" icon="book">
    In-depth guide with advanced features.
</Card>
</CardGroup>
