---
title: "Face Change Python 快速入门"
---

## 简介

Face Change 是 Ace Data Cloud 提供的AI 图像服务，AI 人脸处理服务。通过 Ace Data Cloud 统一 API，你可以使用 Python 快速集成 Face Change，实现人脸替换、人脸美化、年龄变换、性别变换、动漫化、人脸分析等功能。

## 前置条件

- 拥有 [Ace Data Cloud](https://platform.acedata.cloud) 账号并获取 API Token
- Python 3.7 及以上环境
- 安装 requests 库：`pip install requests`

## 基础用法

调用 Face Change API 的主端点为：

```
POST https://api.acedata.cloud/face/swap
```



完整 Python 代码示例：

```python
import requests

url = "https://api.acedata.cloud/face/swap"
headers = {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json"
}
data = {
    "source_image_url": "https://example.com/source-face.jpg",
    "target_image_url": "https://example.com/target-face.jpg"
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result)
```

请将 `YOUR_API_TOKEN` 替换为你在 Ace Data Cloud 平台获取的实际 Token。

## 响应处理

成功调用后，API 返回 JSON 格式数据。建议检查 HTTP 状态码：

```python
if response.status_code == 200:
    result = response.json()
    print("调用成功:", result)
else:
    print(f"调用失败，状态码: {response.status_code}")
    print(response.text)
```

## 进阶用法

对于支持异步任务的 API，可以通过回调 URL 获取结果：

```python
data['callback_url'] = 'https://your-server.com/callback'
response = requests.post(url, headers=headers, json=data)
# 结果将通过回调 URL 推送到你的服务器
```

## 错误处理

常见错误码：

| 状态码 | 说明 |
|--------|------|
| 401 | 认证失败，请检查 API Token |
| 403 | 余额不足或无权访问 |
| 429 | 请求频率超限 |
| 500 | 服务器内部错误 |


## 其他端点

Face Change 服务还提供以下端点：

- `POST https://api.acedata.cloud/face/beautify`
- `POST https://api.acedata.cloud/face/change-age`
- `POST https://api.acedata.cloud/face/change-gender`
- `POST https://api.acedata.cloud/face/cartoon`
- `POST https://api.acedata.cloud/face/analyze`

## 下一步

- 前往 [Ace Data Cloud 控制台](https://platform.acedata.cloud) 获取 API Token
- 查看 [API 文档](https://docs.acedata.cloud/zh-CN/api-reference) 了解完整参数说明
- 查看 [定价方案](https://platform.acedata.cloud/services) 了解费用
