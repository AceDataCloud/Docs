---
title: "How to use Grok API with JavaScript"
seo: "Grok API JavaScript tutorial — Node.js quick start guide"
description: "Integrate Grok API into your JavaScript or Node.js application. Authentication, fetch examples, streaming, and error handling."
---

This tutorial shows you how to call the **Grok API** from JavaScript (Node.js or browser) using Ace Data Cloud.

## What you'll learn

- streaming
- real-time knowledge
- reasoning

## Prerequisites

- Node.js 18+ (for server-side) or a modern browser
- An [Ace Data Cloud API key](https://platform.acedata.cloud) (free trial available)

## Quick start

### Using fetch (Node.js 18+ / Browser)

```javascript
const API_KEY = "YOUR_API_KEY";
const BASE_URL = "https://api.acedata.cloud";

const response = await fetch(`${BASE_URL}/v1/chat/completions`, {
  method: "POST",
  headers: {
    "Authorization": `Bearer ${API_KEY}`,
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
  "model": "grok-3",
  "messages": [
    {
      "role": "user",
      "content": "What are the latest trends in AI?"
    }
  ]
}),
});

const data = await response.json();
console.log(data);
```

### Streaming

```javascript
const response = await fetch(`${BASE_URL}/v1/chat/completions`, {
  method: "POST",
  headers: {
    "Authorization": `Bearer ${API_KEY}`,
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    "model": "grok-3",
    "messages": [{ "role": "user", "content": "Tell me a story" }],
    "stream": true,
  }),
});

const reader = response.body.getReader();
const decoder = new TextDecoder();

while (true) {
  const { done, value } = await reader.read();
  if (done) break;
  const chunk = decoder.decode(value);
  process.stdout.write(chunk);
}
```

### Using the OpenAI SDK

The fastest way to get started — use the official OpenAI SDK with Ace Data Cloud:

```bash
npm install openai
```

```javascript
import OpenAI from "openai";

const client = new OpenAI({
  apiKey: "YOUR_API_KEY",
  baseURL: "https://api.acedata.cloud/v1",
});

const completion = await client.chat.completions.create({
  model: "grok-3",
  messages: [{ role: "user", content: "Hello!" }],
});

console.log(completion.choices[0].message.content);
```

## Error handling

```javascript
if (!response.ok) {
  const error = await response.json();
  switch (response.status) {
    case 401: console.error("Invalid API key"); break;
    case 429: console.error("Rate limited — retry later"); break;
    default: console.error(`Error ${response.status}:`, error);
  }
}
```

## Next steps

<CardGroup cols={2}>
<Card title="Python tutorial" href="/tutorials/grok/python" icon="python">
    Same API, Python examples.
</Card>
<Card title="API reference" href="/api-reference/grok" icon="code">
    Full API specification.
</Card>
</CardGroup>
