---
title: "Suno JavaScript 快速入门"
---

## 简介

Suno 是 Ace Data Cloud 提供的AI 音频服务，Suno AI 音乐生成。通过 Ace Data Cloud 统一 API，你可以使用 JavaScript 快速集成 Suno，实现文本生成音乐、自定义歌词、歌曲续写、翻唱、人声分离、歌词生成等功能。

## 前置条件

- 拥有 [Ace Data Cloud](https://platform.acedata.cloud) 账号并获取 API Token
- Node.js 18+ 或现代浏览器环境

## 基础用法

调用 Suno API 的主端点为：

```
POST https://api.acedata.cloud/suno/audios
```

本示例使用 `chirp-v5` 模型。
可用模型包括：`chirp-v5`、`v4-5-plus`、`v4-5`、`v4`、`v3-5`。

完整 JavaScript 代码示例：

```javascript
const response = await fetch("https://api.acedata.cloud/suno/audios", {
  method: "POST",
  headers: {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
  "action": "generate",
  "prompt": "A cheerful pop song about summer vacation",
  "model": "chirp-v5",
  "custom": false
}),
});

const result = await response.json();
console.log(result);
```

请将 `YOUR_API_TOKEN` 替换为你在 Ace Data Cloud 平台获取的实际 Token。

## 响应处理

建议检查响应状态码并处理错误：

```javascript
if (response.ok) {
  const result = await response.json();
  console.log("调用成功:", result);
} else {
  console.error(`调用失败，状态码: ${response.status}`);
  const error = await response.text();
  console.error(error);
}
```

## Node.js 封装

推荐封装为可复用函数：

```javascript
async function callSuno(data) {
  const response = await fetch("https://api.acedata.cloud/suno/audios", {
    method: "POST",
    headers: {
      "Authorization": `Bearer ${process.env.ACE_API_TOKEN}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify(data),
  });
  if (!response.ok) throw new Error(`API error: ${response.status}`);
  return response.json();
}
```

## 错误处理

常见错误码：

| 状态码 | 说明 |
|--------|------|
| 401 | 认证失败，请检查 API Token |
| 403 | 余额不足或无权访问 |
| 429 | 请求频率超限 |
| 500 | 服务器内部错误 |

## 下一步

- 前往 [Ace Data Cloud 控制台](https://platform.acedata.cloud) 获取 API Token
- 查看 [API 文档](https://docs.acedata.cloud/zh-CN/api-reference) 了解完整参数说明
- 查看 [定价方案](https://platform.acedata.cloud/services) 了解费用
